# MTP 当前状态与困惑（2026-09-24）

## 最新续做状态

用户要求在正确性恢复后继续优化，当前工作分支为 `work/mtp-correctness-first`。恢复分支与原 WIP 已推送并保留；续做没有整包合入 WIP。

- 已先处理 `origin/rerot-wip` 报告的共享 Vulkan skinny MMVQ 输入身份错误（`ef48a9d40`），真实 GPU 回归先红后绿；不把历史／当前 Vulkan 对 CPU 的其他差异冒称已修复。
- 已完成 W1 的 Meta 内部 rank-local pinned 回读（`2f5bcbafc`），不新增上层输出 API，也不改采样或 GDN 状态策略。五卡 byte/lifetime 测试通过；真实 MTP 日志覆盖 accepted prefix 0…6，grammar／penalty／截断／取消／缓存及长输出均验过。新增 non-MTP 保护烟测冷请求＋两次重复也全为171/171、逐字正确、自然停止。
- 正式同二进制五组 ABBA：40 次稳态＋20 次首请求全部正确自然停止。稳态 `predicted_ms` 中位数 **2362.217→2302.145**；五组配对平均 **72.2498→74.2730 tok/s（+2.80%，95% 比值区间1.02044–1.03561）**。所有组均改善，client wall 也改善；**仍未到100 tok/s**。完整请求／响应与配对数据已入库 `artifacts/tp5-mtp-readback-20260924/`。
- W2 greedy 及 W4 双图 catch-up 仍在保存分支隔离；W4 已知分配断言未假装解决。完整证据与同二进制读回开关的 ABBA 复跑命令见 `TP5.md`“正确性优先的 MTP 续做”。这不等于 >100 tok/s 已实现，后文历史数字也不视为本次成绩。

## 后续正确性复核覆盖说明

本日后续对 `e4510fe0a` non-MTP 的“185 token／255 截断／16 处岔错”完成复核：临时客户端漏传关闭 thinking 的参数，实际 prompt71 而非原验收 prompt31；它保存的 40 字符预览也恰好截在正确答案的 `17` 中间，不能证明数错。恢复原请求后，重建主工作树二进制的最终五卡默认 non-MTP 验收 **6/6 逐字正确、171/171、自然停止**；默认 thinking 使用足够预算也精确自然停止（71 prompt＋220 completion）。缓存、混合请求、取消后恢复另有完整响应证据，详见 [TP5.md 的事故复核](TP5.md#non-mtp-正确性事故复核2026-09-24覆盖-wave-4-的故障归因)。

此次正确性恢复开始时的全部未提交改动已保存到 `wip/tp5-before-correctness-recovery-20260924`（`b396f7be8`）；恢复分支为 `fix/tp5-non-mtp-correctness`，主 server 当时重建为干净推理基线。该阶段未继续性能优化；之后的续做以本文最新状态为准。WIP 分配崩溃仍隔离、未合入。原始恢复证据：`/var/tmp/tp5-correctness-recovery-20260924/`。

## 目标与结论

目标仍是五张 RX 6800 上，`1..60` **逐字正确、自然停止、171/171 token** 的完整响应，server 报告的独占、无 profiler `predicted_ms < 1710`，即 **Decode >100 tok/s**。**尚未达到；不要标记完成，也不要将单个 GPU kernel、首图计时或 client 墙钟冒充此指标。** 生产默认仍是纯 Target；MTP 仅显式 opt-in。当前已恢复的五卡 F32/RELAY target＋Vulkan0 单卡 Q4_K_M draft，n=6，预热后两次完整响应为 `predicted_ms=2370.901／2376.041`（**72.12／71.97 Decode tok/s**），均正确且自然停止。原始响应：`/var/tmp/tp5-restored-after-greedy-rejection-unprofiled.json`。流式请求断连后，日志确认 `cancel task`，同一 server 又正确完成 171/171：`/var/tmp/tp5-restored-after-greedy-cancel-recovery.json`。

工作树原有大量未提交的 TP5/MTP 工作，**不可整体 reset**。本轮留下的源码修复只有：`src/llama-graph.cpp::llm_graph_context::build_sampling` 在 logits 为零行（MTP deferred catch-up）时不再生成非法采样图；`ggml/src/ggml-backend-meta.cpp::ggml_backend_meta_graph_compute` 删掉无条件、跨线程无同步的静态调试计数与 stderr 输出。此前已有的五 rank native-hidden 显式 opt-in／真实输入驻留修复仍在；它不是默认晋级。实验性五 rank GPU ArgMax、哈希缓存替换、临时分相计时和 shader 尝试均已撤销。`TP5.md` 当前验收节是详细试验账。

最近重建后，`test-meta-reduce-boundary`、`test-mtp-workspace`、`test-predefined-capacity`、五卡 F32 `test-vulkan-tp5-mesh --sync relay --rounds 8 --check-all` 均通过，mesh 四轮真实 graph-producer 且 FD 27→27；`git diff --check` 通过。watchdog active、内核 warning 无新增、无残留 llama-server。完整 `test-arg-parser` 曾被外部 GET／网络失败打断，**不能写成全套通过**；设备不匹配的 native-hidden 启动前拒绝另行验证过。

## 已量出的账，不可相加或误读

- 一个同请求、开启分相账的正确 n=6 响应 `predicted_ms=2437.917`：约 25 个 cycle 的 draft **514.051 ms**、target **1885.206 ms**、catch-up **35.891 ms**，剩余边界约 2.769 ms。draft 的 `common_sampler_sample` **445.386 ms** 包含 GPU 完成等待，**不是 CPU 纯采样**；target 的图区间 **1700.078 ms** 嵌套在 target 时间内，不能再加一遍。无 profiler 的正式完成时间以上一节为准。
- 同二进制五卡 F32 RELAY 的 GPU timestamp 诊断：七行 target 链 rank0 compute **46.257 ms**、P2 **8.256 ms**、整体 **54.523 ms**；单行分别 **20.422／5.129／25.564 ms**。这些是设备图内账，不等于端到端 Decode。首张七行 graph 还有大约 **0.11 s** scheduler 分配，实际主要是约 99–110 ms Meta 张量绑定，不是 backend fence；即便全省，也不足以填平总缺口。
- n=10 单卡 Q4 draft 虽保持高接受率（已有样本 `draft_n=160`、accepted 约 156–158），后续 draft 步 GPU sampler wait 从 n=6 约 **2.2 ms** 增至约 **7.5 ms**。同形状量化 GEMV 在诊断中同步变慢；尚未证明具体哪个 BO 被迁移。n=10 目标 compute scratch 相比 n=6 每卡约多 **416 MiB**；Vulkan0 同时承载 draft 时观测到 GTT 约 **978 MiB**，但 GTT 相关性不等于已确定权重页的迁移身份。`-c 256→208` 加载试验没有减少这项 GTT。Q3 重量化的 n10 可到约 70 tok/s，但改变 draft 模型且远未过门，未晋级。

## 退选的真正原因

- 五卡 draft 的 native device-hidden 行交接和取消后恢复均已跑通，但正式完整 Decode **54.26／54.32 tok/s**，同构 host-hidden 五卡草稿约 **56.70／56.80**；这是接口正确性证明，不是融合或收益。
- 针对五卡 axis-0 分片 logits，临时实现过 rank-local Vulkan ArgMax，再以全局词表 offset／最小全局 id 合并；不等长分片与平局的模型外测试通过，真实 `1..60` 也正确。串行两趟 GPU→CPU 回读仅 **52.99／52.93 tok/s**，五 rank 并行分两趟回读仍仅 **53.93／53.38**，低于 CPU fallback；已完全撤销其 lowering、启用门和专属测试。真正的一趟候选值＋索引设备发布仍需新契约，但单靠采样也无法让 target 低于 1710 ms。
- 五卡按层切分的纯 target（不带 MTP）完整正确响应只有 **18.05 Decode tok/s**，不能拿来替换 TP5 target。Vulkan per-op query 会禁用重放，测得的 24 s 响应只可用于算子类别诊断，不可作性能对照。以前的 Q5/Q8 几何、HC／GDN 融合、BAR/F16、n=7、n=10 草稿位置／Q3/Q2 量化等失败臂见 `TP5.md`；不要原样重试或把短烟测写为收益。

## 真正的困惑／下一班需先证明的事

1. **执行程序而非更多旋钮**：target 的稳态七行计算和 P2 加起来约 54.5 ms／图，实际 target 时间本身已超过 1710 ms。需要量化并实现跨多个投影／MoE／通信边界的端到端缩短，同时降低串行 draft 成本；单个 shader 或仅消除首图税不够。不能用不同进程的 GPU timestamp 直接从同请求 ledger 扣除。
2. **可否以 checkpoint＋少数拒绝时重放，取代每个候选前缀的完整 GDN F32 snapshot？** n=10 scratch 增长与 `GATED_DELTA_NET` 逐行输出及 recurrent rollback 相关，但 snapshot 是现有 `seq_rm` 的正确性契约。直接丢弃会污染已接受前缀；若改为仅保存起点／终点并在拒绝时从起点重跑接受前缀，必须先证明每层 GDN／KV 恢复、F32 数值、目标 hidden、取消和 graph buffer 生命周期可精确闭合，还要算真实拒绝率与重放成本。当前只是**待证设计，不是已实现优化**。
3. **device-resident 多步 draft 的物理可行性**：若把 greedy token、embedding、MTP 递归与 EOG 门控留在 GPU，可去掉逐步 CPU 等待；但 Vulkan0 n=6 请求后 VRAM 约 **16.861/17.163 GB**，余量约 302 MB，不能天真复制 6–10 套 graph／scratch。多步程序须共享峰值 scratch、处理每步 recurrent KV、代际戳和受拒时回滚，不得借用过期 Meta tensor 指针。读只读研究代理报告时要核实际模型形状／显存：它曾误把约 80GB 的 target 当成 3.8B 参数模型并猜测充裕显存。
4. **如何避免每请求首个七行图的两类冷税**：一次约 0.11 s 的 Meta graph allocation 和另一次 GPU 首图偏慢分别测得；复用 graph 身份不等于安全复用 Vulkan CB/descriptor、KV/GDN 状态与已释放 scratch。需要 Program/RunInputs 分离及寿命证明；缓存 UID 或虚假预热都不算修复。即便解决两项，也需稳态 compute／draft 收益才能过 100。

请以后只在独占五卡、watchdog 保持 active、完整正确自然停止 171/171 且 `predicted_ms<1710` 的复跑出现时报告 >100。此交接按用户要求写完即停，不在这里假装已经达到目标。
