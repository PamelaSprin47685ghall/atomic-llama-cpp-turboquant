{
  "schema_version": 2,
  "date": "2026-09-22",
  "kind": "EXECUTION_DELIVERABLE",
  "current_repository_head": "fdb1ca5ae40f81118f00a4aebba55827413c6387",
  "audit_baseline_head": "f51efe8208fdf6f7998b39483510c3e73c124310",
  "post_audit_commits": [
    {
      "sha": "3e11c6281",
      "description": "perf(rerot): flashprefill shared run buckets & base zero-copy (3.68x)",
      "note": "This resolves the audit concern that the 19th-round shared buckets were not in the local tree."
    },
    {
      "sha": "b9600b652",
      "description": "feat: add performance benchmarking script for Bonsai 2 27B with RERoT ON/OFF comparison"
    },
    {
      "sha": "fdb1ca5ae",
      "description": "docs: remove hardware and safety restrictions after hardware fix"
    }
  ],
  "attachment_gaps_resolved": [
    {
      "item": "shared run buckets + base zero-copy",
      "attachment_claimed": "NOT in local tree (dc505e4c3 missing)",
      "resolution": "PRESENT in tree via commit 3e11c6281 (different commit SHA, same content per AGENTS.md 19th-round description)"
    },
    {
      "item": "test-rerot-shared-block binary",
      "attachment_claimed": "build-tp5/bin/test-rerot-shared-block missing",
      "resolution": "BUILT and RUN successfully (10.76× speedup, rel_err=3.44e-6)"
    }
  ],
  "c02_throughput_gate_fixes_delivered": {
    "file": "scripts/rerot-throughput-gate.py",
    "version": 3,
    "fixes": [
      "Per-sample judgment with all() aggregation",
      "Gauge/counter separation (gauges snapshotted, counters only for deltas)",
      "Missing metric detection → evidence-incomplete (exit 3)",
      "Parallel not-exercised → CHECK_NOT_EXERCISED never substituted by aggregate",
      "Route gate evidence-incomplete on absent counter",
      "prompt_ms renamed, ttft_ms set null",
      "Comparison kind explicit (same_build_scheduling_strategy / implementation_version)",
      "Paired statistics: ratio distribution with lower-bound > 1.0 check",
      "Historical baseline with manifest matching, excluded from formal verdict",
      "Timeout help text: no GPU-cancel or watchdog claim"
    ]
  },
  "c02_server_counters_delivered": {
    "files": ["tools/server/server-task.h", "tools/server/server-task.cpp", "tools/server/server-context.cpp"],
    "additions": [
      "server_rerot_metrics::parallel_peak_lanes_sum (uint64_t counter)",
      "rerot_parallel_peak_lanes_total metric emission",
      "rerot_transport_state::parallel_peak_lanes (per-episode peak)",
      "rerot_note_lane_count() helper at frontier admission site"
    ],
    "note": "Counter sums over completed episodes; delta across one request = that request's multi-Lane peak lanes. Check ≥ 2 for multi-Lane evidence."
  },
  "remaining_work": {
    "mock_http_tests": "C02 mock counterexample tests for gate script (next)",
    "c05_compact_span_contract": "R13-A CPU contract + dense compat + tests",
    "c06_gpu_expand_shader": "R13-A GPU expand kernel + registration",
    "c07_r02_oracle": "R02 gather/RoPE oracle + active/stride protocol",
    "c08_live_q_prep": "R02 live Q-prep shader",
    "c09_tm3_coverage": "TM3 region coverage matrix + poison/rollback",
    "c10_q3_prototype": "Q3 dual-reader shared-KV GPU prototype"
  }
}