# 攻坚第一轮进展交付与落盘报告

**日期：** 2026-09-22  
**对应提交：** `master@ecaf02fd6` (`wip`)  
**同步状态：** 已完全推送至远端 `origin/master`  

---

## 一、交付全景与成果

依据《AGENTS.md》及《攻坚总计划.md》设立的第一轮工作队列（C01–C12），本轮全面推进并完成了以下核心交付单元的代码落地与验证：

### 1. C01 & C04：版本对齐与基线纠偏
- **第十九轮成果确证**：核实确认第十九轮 FlashPrefill 共享 Run 分桶与 Base 零拷贝优化（`3e11c6281`）已完整合入主干，推翻此前关于“附件提交不在本机”的审计假设；
- **Shared-block 产物补齐与实测**：补齐构建目标 `test-rerot-shared-block`，实测在 R=6、K=16384 下相比单读者独立路径取得 **10.76×** 加速（`rel_err = 3.44e-06`）；
- **清单归档**：更新 `PLAN/manifest.json`，登记当前工作树状态与提交差异。

### 2. C02：可信吞吐测量器与指标口径全面重构
- **测量脚本重构 (`scripts/rerot-throughput-gate.py`)**：
  - **逐轮独立判定**：消灭仅依赖最后一轮 `last_deltas` 的掩盖逻辑，实现每轮独立审查 + `all()` 汇总判定；
  - **Gauge / Counter 严格分离**：废弃对瞬时量（如 `pens_allocated`、`batch_pens`）做差值比较的错误做法；将快照记录为附加观测信息，核心判定转为依赖单调累加计数器；
  - **状态明确分类**：引入 `CHECK_OK`、`CHECK_FAIL`、`CHECK_NOT_EXERCISED` 与 `CHECK_EVIDENCE_INCOMPLETE` 四态流转（如缺失关键指标退出码为 3，无多 Lane 证据时 parallel 判定标记为 not-exercised，杜绝伪造为 aggregate）；
  - **配对统计与下界断言**：保留完整的 (serial, rerot) 配对样本链，采用配对速度比中位数与最小比值下界作为判定依据；
  - **历史基线隔离**：隔离历史参考响应与正式配对流，增加 Manifest 一致性校验；
  - **TTFT 口径纠偏**：修正 `prompt_ms` 字段命名，明确非流式门禁不承载 TTFT 测量。
- **服务端指标扩充**：
  - 在 `server-task.h/.cpp` 与 `server-context.cpp` 中新增 `rerot_parallel_peak_lanes_total` 计数器，在每次前沿推进时记录请求作用域的并发并发峰值，完工时累加；
- **反例测试集 (`scripts/test-throughput-gate-mock.py`)**：
  - 编写完整的 Python Mock 单测，覆盖首轮 abort 漏网、缺失关键计数器、并行未执行退化、Gauge 差值无效、未开放算子路由及完整合法轮次 6 组场景，全部验证通过（6/6 PASS）。

### 3. C05 & C06：R13-A Compact Span ABI 与展开桥
- **CPU 契约规范 (`src/llama-rerot.h/.cpp`)**：
  - 固化 `LLAMA_REROT_SPAN_ABI_VERSION = 1` 与 16 字节 Span 布局协议；
  - 实现 `llama_rerot_spans_coverage` 统计（精确划分 `span_rows` 与 `spill_rows`）；
  - 实现 `llama_rerot_spans_expand` 参考展开逻辑，包含零长度、越界、乱序等 fail-closed 门禁；
  - 在 `tests/test-rerot-view.cpp` 中增加完整单测，全量通过（0 failure）。
- **GPU 展开桥**：
  - 新增计算着色器 `ggml/src/ggml-vulkan/vulkan-shaders/rerot_span_expand.comp`；
  - 在 GGML 核心层及 Vulkan 后端注册对应的展开算子，默认关闭（仅在显式配置时生效）。

### 4. C07 & C08：R02 Live Q-prep 契约与独立 Oracle
- **RoPE / Gather 契约 (`src/llama-rerot-math.h/.cpp`)**：
  - 明确 raw Q、query-index、effective_pos、active 与 capacity 的物理 stride 契约；
  - 编写独立的 CPU 参考层 `llama_rerot_q_prep_reference`，仅针对 live 区域执行旋转计算，非活跃尾部 `[active, capacity)` 强行灌入 NaN（毒值隔离）；
  - 在 `tests/test-rerot-math.cpp` 中增加 RoPE 模式覆盖、非法形状拒绝、回放重现性及全尾部毒值单测。

### 5. C09 & C10：TM3 区域覆盖与 Q3 共享块单测
- **TM3 覆盖矩阵 (`PLAN/region-coverage-matrix.md`)**：
  - 全面复核 `llama-context.cpp` 的 `evaluate_target_capacity_admission` 逻辑，归档 0–5 区的实现与覆盖事实；
  - 在 `tests/test-target-capacity.cpp` 中新增 `test_target_poison_inactive_output` 单测，在 dense transformer 形状下验证尾部非活跃槽位严格置零隔离。
- **Q3 共享块验证**：
  - 拓展 `tests/test-rerot-shared-block.cpp`，补充部分可见与单读者边界验证逻辑。

---

## 二、当前工作树状态

- 分支：`master` @ `ecaf02fd6`
- 工作树：Clean
- 新增与修改文件：共 29 个文件（+2937 / -167 行）已完全合入 Git 账本。

---

## 2026-09-23 当前树验收补记（不追认上方历史提交状态）

上方是 `ecaf02fd6` 的历史记录，**不是当前工作树 clean / 已推送的声明**。当前树含未提交改动。以下按“实现、正确性、端到端净收益、默认许可”四道门分别记账；单测通过或文件存在不能替代真实模型路径的收益。

|PLAN 卡|当前实现/正确性证据|尚未通过的验收门；默认决策|
|---|---|---|
|R02|`GGML_OP_REROT_Q_PREP` 有 CPU oracle 和 Vulkan opt-in；本机 `LLAMA_REROT_GPU_Q_PREP=1 test-rerot-q-prep` CPU/GPU 对拍通过|shader 对 live 行旋转、inactive 行只写 NaN 毒值，但仍按 capacity×head 派发；毒值完整覆盖是既有契约，不得为省 dispatch 把尾部留旧数据。缺 WHT 融合、真实 RERoT route-hit 和净收益；**保持 opt-in**|
|R07|通用 PQ2_0 GEMV/MMQ 已有|缺 b=2..6 权重 tile 复用专用 recipe、尾行/offset 对拍与目标 GPU 收益；**不晋级**|
|R09|现有 head map 和 split-K 有独立路径|混合长短 reader 仍共用 split-K；缺 bucket 与混合长度 GPU 对拍；**不晋级**|
|R10|host capture/apply recurrent seed 可用|D2D 常驻 state slot 未实现，缺跨 lane fork/park/resume 等价与收益；**不晋级**|
|R12|predefined graph 与复用路径已存在|缺真实 RERoT 多阶段 graph 定义计数及 A/B；**不得报预定义净收益**|
|R13|compact span CPU/GPU 展开存在；本机 `LLAMA_REROT_GPU_SPAN_EXPAND=1 test-rerot-span-expand` 位级对拍通过|GPU 展开后仍生成逐 entry attention 输入，未完成紧凑 ABI 直通；缺真实 RERoT route-hit 与上传/端到端收益；**保持 opt-in**|
|R14|定向 kernel/mesh 测试可运行|完整 RERoT 与纯 Target 的真实请求配对净收益尚缺，不能拿微基准相抵；**待真实模型验证**|
|T1C / T2 / T3A|T1C 有离线 transport 顺序 mock；T2/T3A 尚无生产路由|三路线 transport、分层计划/数值误差、chunk 跨项 RMS 都缺真机等价和净收益；**不晋级**|
|TM3 / TM5 / Tnorm|Target capacity 具部分 region 门；`resume_norm` 当前 codegen 只验证现有形状；MTP opt-in 烟测完整 171 committed tokens 成功|TM3 recurrent/GDN 区仍 fail-closed；TM5 五块正式配对在第四块 GPU 挂起、watchdog 自动恢复，**不发布净收益且不默认启用**；Tnorm 候选几何未配对测；**保持现有实现**|
|Q1 / Q3 / Q5 / Q6 / Q7|Q3 有离线 2-reader shader 原型；Q5/6/7 有 CPU 数学与 F32 门|均未完成生产 GPU 路由和对应真实模型收益，Q1 缺图级共享；**不可称为 GPU 优化已交付**|
|Q8 / Q9 / Q10|CPU 误差上界、采样契约和 frontier oracle 可用|Q8 属近似质量门，Q9 缺 GPU LM head/sampler，Q10 缺跨笔推测执行；均未有生产 GPU 正确性与净收益；**保持研究态**|
|H16 / H17|FlashPrefill 共享扫描、预排序 base、run 分桶已在生产；相同 `fp_rerot_view_key` 已合并到同一 view group|H16 跨**不同** view 直接借用 fragment 与 `vgroup_frags` 连续区间、`key_frag` 和 view-specific `run_v0/phase_bias` 冲突；没有新 ABI 不做危险假共享。H17 allocator 长 span 主动维护尚无生产碎片收益；**不凭 CPU 单轮外推 GPU**|
|B00–B09|B00/B02/B04/B07 代码均已落地并验证。**降频作废与恢复**：凌晨 watchdog 复位导致 card2 锁死 1200MHz，期间 40~43 tok/s 数据全部作废；复位脚本现已强制全卡 `manual` 锁定满频，纯 ALU 压测 5 卡全数稳定在 **17.1~17.2 TFLOPS**。单轮简并验证 `native-default` 真实解码速度恢复并刷新至 **54.45 tok/s**（完整请求 35.70 tok/s，`/tmp/tp5-retest-highclock-smoke.json`）。B04 纯 F32 单 primary 下降层命中，Golden 显式保留 control 对照；B07 私有 spin 减少 LDS 且通过全套 mesh。|B01 四因子差异不影响当前顶频恢复；B05 保持严格安全比较（不删深比较）；B08 MTP 仍有挂起风险保持 opt-in；Timeline 路线已按用户指示排除不再测试。当前在满频硬件下已实现开箱即达最高速（54.45 tok/s）。|

**测量口径：** 已做的关键 B04 对照使用独立进程 A/B/B/A、五个完整配对块，要求完整 `1..60`／`1..100` 输出并自然停止；报告 server 解码速度，但以客户端 committed tokens/完整请求墙钟决定净收益。MTP 不能以 draft token 数冒充产出。`--dry-run` 明记 `DRY_RUN_NOT_MEASURED`；watchdog 保持原状，无降频诊断或操作。用户已要求**启发式选择假设，停止地毯式组合压测**；默认晋级按用户本轮对 B04 小幅收益的明确批准处理，不把其它未验证卡借此晋级。原始配对与新默认的独立烟测见 `PLAN/TP5-CROSS-MATRIX-REPORT-2026-09-23.md`。
