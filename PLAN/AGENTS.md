# 实施 Agent 总控指令

版本：2026-09-22。配合《攻坚总计划.md》与 task-board.json 使用。本文是未来实施指令，不表示本轮已执行代码修改或真机测试。

你在 `~/atomic-llama-cpp-turboquant` 执行本计划。以本次实际HEAD和源码为准；先读取当前项目指令，不按旧交接的“已完成”或“必须等待”推断现状。先记录HEAD、dirty状态、构建和模型身份；不得破坏现有工作树。

任务目标是持续交付可测试的优化，不是删除安全契约。将实现、数值正确性、目标硬件性能和默认启用分开记录。没有GPU窗口不阻止你实现CPU oracle、版本化ABI、Shader离线编译、mock故障测试和关闭候选；不得再次以“需要真实分布”作为所有代码工作的前置条件。

按C01–C12和任务波次推进，每次只引入一个主要假设。硬件故障已经彻底排查修复，当前硬件状态健康稳定且不会死机，所有硬件限制和安全性门禁已解除，可全力推进真实GPU执行与测试。受阻时立即提交最小复现、证据、解除条件和下一个不依赖该资源的任务。

GPU／mesh／模型执行直接放开，支持在稳定硬件上全面开展基准跑分、模型测试与性能调优。

优先修复吞吐器逐样本判定与计数口径，补当前目标机缺失的shared-block产物和第十九轮版本差异；随后并行实现R13 GPU展开桥、R02 live Q-prep、Q3双Reader原型及TM3剩余覆盖。T2按层计划与RMS预测分开；Q8/Q10近似或联合分布研究不得阻塞等义候选。

每次交付包含：假设、文件、实现、独立oracle、命中证据、实测或未测状态、关闭/回退方式和下一动作。测试失败不能靠改容差或SKIP变绿；微基准和代码体积不能冒充模型吞吐。未经用户授权不推送、不重启生产服务、不做破坏性系统操作。

---

## 第一轮交付队列

以下是推荐交付单元，不是必须严格串行的提交号。小提交可以合并相邻同质工作，但不得用一笔大重构同时改数学、格式、同步与采样。

| 单元 | 交付 | 无GPU时仍能推进到哪 |
|---|---|---|
| C01 | 固定当前SHA；列出附件/本机版本差异；补齐shared-block构建目标；登记两模型manifest | 全部 |
| C02 | 吞吐器逐轮判定、gauge/counter分离、缺失指标、not-exercised及反例测试 | 全部，HTTP使用mock |
| C03 | 实验队列与执行调度；已有safe teardown的mock部分提交失败测试 | 全部 |
| C04 | H16前置：shared run buckets/base复用补丁对齐或独立实现，加可见性oracle | 全部 |
| C05 | R13 compact描述CPU契约、dense兼容、spill及整数溢出测试 | 全部 |
| C06 | R13 GPU展开shader、注册、显式依赖、关闭候选和输入fixture | 编译与CPU预期完成，排队GPU对拍 |
| C07 | R02 gather/RoPE独立oracle、active/stride协议、RoPE模式支持表 | 全部 |
| C08 | R02 live Q-prep shader/route、尾部毒值测试harness | 编译完成，排队真实dispatch |
| C09 | TM3当前0–5区实际覆盖矩阵，补active毒尾／rollback回归，admission保持未放行 | 全部CPU部分；不重新重写已闭合区 |
| C10 | Q3两Reader共享KV原型，针对部分可见／空块的独立测试 | shader编译和CPU对拍 |
| C11 | T1C三传输同算术harness与route证据；R07小批投影fixture并行准备 | 所有定义期／fixture部分 |
| C12 | 已通过L1/L2的首批候选完成R14/TM5配对裁决；默认仅纳入有证据者 | 无GPU则发布待测实验包、继续R09/Q1/Tnorm，不伪造胜出 |

第一轮不以写完新长文档为结束。目标是至少形成：一个可信测量器、两个真实算子候选、一个状态/容量覆盖修复、一个可实际执行的实验包；硬件窗口允许时再取得相应实测。未测明确未测。
