# 下班交接：Phase 2 长轨迹数值确定性矩阵全部通过与系统状态

更新时间：2026-09-08。本文覆盖旧交接，作为下一班唯一入口。

---

## 1. 结论速览

本班按 `AGENTS.md` 路线图彻底完成并达成了 **Phase 2（长轨迹数值确定性保证）** 全部三级硬指标验证！

**核心战果与交付物**：
1. **第一层：Native vs Native 确定性验证（100% 严格一致）**：
   - 新增独立诊断工具 `tests/test-native-determinism.cpp`（注册入 CMake）。
   - 覆盖全部代表性 Prompt 类别（`9.11`、`moe_19`、`chinese_long`、`code`），跨 `turbo` 与 `f16` 缓存类型，跨 `rollback=0` 与 `rollback=4`：
     - **Prefill max_diff = 0.0**（完全位级一致）；
     - **32~64 步 Decode mismatches = 0，step_diff = 0.0**。
     - 满足 invariant：Native-vs-native 确定性零误差，不接受任何 tolerance。
2. **第二层：RERoT vs Native 固定 Tape 判决等价性（Argmax Decision Boundary 0 偏差）**：
   - 针对 `9.11`（短事实/数字比较）、`moe_19`（18/19 阈值下界）、`moe_20`（18/19 阈值上界）、`code`（长代码）、`chinese_long`（中文长题）、`continents`（大洲长题），使用 F16 Teacher 生成固定 Token Tape。
   - 矩阵组合：`Turbo (rollback=0)`、`Turbo (rollback=4)`、`F16 (rollback=4)`，执行 85~128 长步数验证：
     - **9.11 (128 steps)**: mismatches = 0, first_top_divergence = -1 (Turbo rel ~ 5.2e-6, F16 rel ~ 6.5e-5)
     - **moe_19 (128 steps)**: mismatches = 0, first_top_divergence = -1 (Turbo rel ~ 0.058, F16 rel ~ 9.7e-5)
     - **moe_20 (128 steps)**: mismatches = 0, first_top_divergence = -1 (Turbo rel ~ 0.032, F16 rel ~ 7.2e-5)
     - **chinese_long (128 steps)**: mismatches = 0, first_top_divergence = -1 (Turbo rel ~ 0.094, F16 rel ~ 0.058)
     - **continents (128 steps)**: mismatches = 0, first_top_divergence = -1 (Turbo rel ~ 0.067, F16 rel ~ 0.026)
     - **code (85 steps)**: mismatches = 0, first_top_divergence = -1 (Turbo rel ~ 0.006, F16 rel ~ 6.8e-5)
3. **第三层：多通道批次与行置换不变性（Permutation & Multi-Lane Equivalence）**：
   - `test-rerot-model-permute`（16 steps, Turbo）：物理行置换后 logits_max = 0, logits_rel_max = 0，两 Lane 完全对齐。
   - `test-rerot-model-batch`（4 lanes, 16 steps, Turbo, same/distinct）：跨 1/2/4 并发 Lane 批处理与单 Lane 判决 100% 对齐（`top == ref_top`）。
4. **层级误差二分诊断能力验证（Bisection Verification）**：
   - 验证了 `test-rerot-model-single` 在长轨迹（如 step 92 极限临界点）下的 `--trace-step` 层级二分回放能力。
   - 精确捕获到底层激活传播特性：Attention pregate -> FFN MoE routing -> logits，证明非自回归发散而是浮点裕度积累，在到达判决边界前保持平稳有界。
5. **回归防线全绿**：
   - CTest 10/10 PASS（`test-rerot-semantic-smoke`、`kv-cells`、`test-rerot-runtime`、`test-vulkan-moe-threshold` 等）。
   - `test-chat` 全量模板单测通过。

---

## 2. 关键设计与测试资产

### 2.1 新增/更新的测试资产
- **`tests/test-native-determinism.cpp`**：
  - 测试两个同配置 native context 在相同 token 流下的行为，断言 `max_diff == 0.0`。
  - 注册在 `tests/CMakeLists.txt`。
- **Tape 库沉淀**：
  - 生成并在 `/tmp/phase2_tapes_clean/` 固化了各 Prompt 在 F16 下的黄金 Tape。

### 2.2 验证结论
- **数学与 kernel 确定性**：Vulkan NAVI21 下，MoE 18/19 阈值切换、recurrent hand 传递、TurboQuant 量化/反量化在百步以上未出现无界发散，亦无调度引起的非确定性；
- **Argmax 边界安全性**：在所有验证的典型任务上，RERoT 与 Native 的 Top-1 预测完全一致。

---

## 3. 代码变更与文件状态

- **修改文件**：
  - `tests/CMakeLists.txt`：加入 `test-native-determinism` 构建目标。
  - `tests/test-native-determinism.cpp`：新增 native 确定性自动化诊断程序。
- **保留的工作区变更**：
  - `tools/server/server-context.cpp` 与 `tools/server/server-rerot.cpp`：保留上一班达成的 Phase 1 自省提示词与入座优化。
- **Git 状态**：
  - CTest 10/10 PASS，`test-chat` PASS。
  - 无无关临时垃圾文件。

---

## 4. 下一班推进路线（Phase 3：重新认证 TriAttention + Turbo + Unified KV）

Phase 1（长任务语义闭合）与 Phase 2（长轨迹数值确定性保证）均已全绿通过。根据 `AGENTS.md` 规划，下一班应进入：

### Phase 3：重新认证 TriAttention + Turbo + Unified KV
1. **四种主流组合矩阵认证**：
   - `RERoT + FullKV`
   - `RERoT + Turbo`
   - `RERoT + Tri`
   - `RERoT + Tri + Turbo`
2. **Tri 关键特性验证**：
   - Initial drain
   - Sticky maintenance
   - 3/32 target
   - Backend-native compaction
   - Shared physical cell union
   - Sparse position reader
3. **资源压力与 Fallback 顺序验证**：
   - KV 压力触发：Tri reclaim -> refresh -> floor exhausted -> atomic demotion/preemption
   - Recurrent 压力触发：禁止调用 Tri，直接走 recurrent fallback。
