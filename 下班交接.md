# 下班交接：纠偏定级回归 Phase 1（核心长任务闭环）

更新时间：2026-09-09。本文覆盖所有历史交接，作为当前 master 分支的唯一事实源。

---

## 1. 严肃定级：彻底纠偏，回归 Phase 1

必须纠正此前好高骛远、将阶段虚报为 Phase 8 或 Phase 9 的错误：
- **历史错误复盘**：在底层多 Lane 物理可见性存在穿透漏洞的前提下，上层接班者同时叠加上了 `TriAttention`、`MTP 投机`、`131K 极端大窗口` 以及业务补丁（`<answer>` 标签、生硬祈使句），导致机制互相踩踏（MTP 出现 `M-RoPE position X < Y` 崩溃、TriAttention 抢占显存高频压缩、模型元反思死锁）；
- **当前真正所处阶段**：**Phase 1：RERoT 核心真实语义闭环（生产长任务验证）**。
- **《AGENTS.md》Phase 1 硬门要求**：
  > 剥离一切干扰项（`TriAttention OFF`、`MTP OFF`、`RAM checkpoint OFF`），只验证纯粹的 RERoT 核心生命周期：
  > ```text
  > root planner
  > → child admitted
  > → 每个 child 自然 exact random-ID close
  > → 全部 retired
  > → exactly one final survivor
  > → final fence prepare
  > → close-token replay exactly once
  > → serial final answer
  > → HTTP 200, finish_reason=stop
  > ```

---

## 2. 本阶段已锁死的纯净基底（DoD 基础）

1. **彻底斩断 Vulkan / FlashPrefill 同拍穿透**（`src/llama-rerot.cpp`）：
   - `llama_rerot_cell_visible_public_full` 严格统一为 `meta.frontier < reader.frontier`，物理上禁止同拍 peer 未提交 token 进入 attention。
2. **提示词回归第一人称自然内省**（`tools/server/server-rerot.cpp`）：
   - 彻底清除外部监工祈使句，避免模型在 `<think>` 中发生元反思和复述。
3. **Slot 切换采样器初始化**（`tools/server/server-context.cpp`）：
   - 规范补齐 `slot.init_sampler()`，重复惩罚正常生效。
4. **Grouped Recurrent State 序列化映射**（`src/llama-memory-recurrent.*`）：
   - 修复 brain row 越界缺陷，回滚与状态往返通过。
5. **生态干扰项完全剥离**：
   - 评测启动参数完全关闭 `TriAttention` 与 `MTP`，专注于纯粹 RERoT 调度。

---

## 3. Phase 1 当前唯一下班验收任务

运行标准长任务端到端闭环验证（“大洲国家”长多 child 任务）：
```bash
python3 scripts/rerot-continents-benchmark.py \
  --base-url http://127.0.0.1:8080 \
  --output reports/phase1-continents/
```

验收指标：
- [ ] 8 个大洲 / 地区组织合理，无串洲；
- [ ] 每个 child 能够自然 exact random-ID close 并安全退场；
- [ ] 无死循环、无 prompt echo、无内部协议标签暴露；
- [ ] 经历 exactly-once final fence 后自然进入 serial final answer；
- [ ] HTTP 200，`finish_reason=stop`。

通过此项验收，方可正式宣告 **Phase 1 成功交付**！
