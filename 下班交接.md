# 下班交接：RERoT 全套物理可见性重构与真实质量门澄清

更新时间：2026-09-09。本文覆盖所有历史交接，作为当前 master 分支的唯一事实源。

---

## 1. 诚实的阶段定位：当前处于 Phase 8 Candidate（非 Phase 9）

必须实事求是地澄清历史真相：
- 此前宣称的“Phase 8 质量验收 10/11 通过”已被静态审计证伪：历史评测脚本 `scripts/rerot-phase8-quality.py` 在执行调用时漏传参数，默认以 `rerot=False` 运行，当时实际测试的是**单流原生串行（Serial）基线**，且附带了阻碍长思考的 `reasoning_effort: "low"`；
- 真实多 Lane 并发下的长任务（如 AIME base-divisor）此前频发死锁与超时，根因在于系统存在底层的**半套重构缺陷**，上层接班者又采用第二人称祈使句、`<answer>` 假标签、强塞 `</think>` 与关停 MTP 投机等业务补丁强行修补，导致系统严重失真；
- **当前真实阶段定位**：**Phase 8 Quality Candidate (物理可见性全套重构与真并发质量待验阶段)**。在真实模型通过长题闭合与端到端并发质量验收之前，严禁宣称已进入 Phase 9 性能阶段。

---

## 2. 本班完成的全套物理与协议重构

1. **彻底斩断 Vulkan / FlashPrefill 同拍穿透（物理隔离）**：
   - 修正 `src/llama-rerot.cpp` 中被遗漏的 `llama_rerot_cell_visible_public_full`：彻底删除 `meta.frontier == reader.frontier` 的后门分支，统一为严格的 `meta.frontier < reader.frontier`；
   - 确保 CPU 标量路径与 Vulkan GPU 物理路径的可见性契约完全一致，根除多 Lane 步调一致互相死锁的物理源头。
2. **提示词彻底回归“第一人称自然自省”（语义纯净）**：
   - 彻底废除 `tools/server/server-rerot.cpp` 中生硬的外部监工式祈使句（如“现在进入正文阶段”、“只完成这一项”、“不得接管”）；
   - 恢复模型第一人称内省风格（“我现在开始专注于自己的唯一子任务『...』的详尽推导...”），杜绝模型在 `<think>` 中发生“元反思”及复述命令造成的正文污染。
3. **规范 Slot 任务切换时的 Sampler 历史继承（算法完整）**：
   - 在 `tools/server/server-context.cpp` 的所有任务切换点（`root`、`child_worker`、`serial_tail`）规范加入 `slot.init_sampler()`，恢复重复惩罚对自身 prompt 的正常压制。
4. **Grouped Recurrent State 序列化映射修复**：
   - 修复分组循环内存状态序列化时直接使用 Hand 物理行而非 Brain 共享行导致的内存越界和恢复漂移，并在 `test-rerot-recurrent` 中补齐往返测试。
5. **剔除所有病态打补丁**：
   - 丢弃 `<answer>` 伪标签包装与 PEG 否定语法，恪守 8 位随机 Base62 ID 自然闭合契约；
   - 丢弃串行尾部强插 `</think>` 及正则剥离代码；
   - 丢弃暂停 MTP 投机解码的临时补丁（`common_speculative_set_paused`）；
   - 纠正 `scripts/rerot-phase8-quality.py`：显式绑定 `rerot=True`，撤销 `reasoning_effort: 'low'`。

---

## 3. 验证状态与代码库基线

- **Git 分支状态**：
  ```text
  branch: master
  commit: (本提交)
  working tree clean (0 uncommitted, 0 untracked)
  ```
- **核心测试矩阵（CTest 11/11 全部绿灯 PASS）**：
  - `test-rerot-view`：PASS (0 failures)
  - `test-rerot-parser`：PASS (0 failures)
  - `test-rerot-attn`：PASS (0 failures, GPU Turbo4/F32 误差仅 5.96e-8)
  - `test-rerot-runtime`：PASS (0 failures)
  - `test-vulkan-moe-threshold`：PASS (0 failures, 18/19 阈值红线通过)
  - `test-triattention-score`：PASS (0 failures, 存储 Oracle 匹配)
  - `test-triattention-kv` / `test-server-triattention`：PASS (0 failures)
  - `test-rerot-semantic-smoke`：PASS

---

## 4. 下一班唯一行动指南

1. **严禁在未真实跑通质量套件前宣称 Phase 9 达标**；
2. **启动模型服务端，执行真实的 Phase 8 端到端评测**：
   ```bash
   python3 scripts/rerot-phase8-quality.py \
     --model /opt/llama/data/Ornith-1.5-35B-Uncensored-YMQ-S-MTP.gguf \
     --build-dir build-vulkan-localhost \
     --triattention-stats /opt/llama/data/ornith-1.5-35b.triattention \
     --output reports/phase8-quality-truth/
   ```
3. 重点观察 AIME `base-divisor` 在解除同拍穿透和祈使句污染后，是否能自然多 Lane 推导并 exact close；
4. 确认质量 100% 达标后，再以真实并发口径进入 Phase 9 吞吐冲刺。
