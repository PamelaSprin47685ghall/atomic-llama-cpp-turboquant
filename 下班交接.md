# 下班交接：RERoT 当前真实状态

更新时间：2026-09-08（已完成 Vulkan MoE 18/19 阈值 hermetic 回归测试与 API 响应层清洁重构并 commit/push 至 master）。本文覆盖旧交接，作为下一班唯一入口。

## 1. 结论速览

**底层数值阻断已经彻底解决，API 响应层（`reasoning/content` 分层与 prompt echo）已经完全清洁收尾，语义回归矩阵多类题型全部通过！**

- **9.11 题**（`9.11 和 9.9 哪个大？简要回答并说明理由。`）：
  - 证据目录：`/tmp/rerot-semantic-szhjq29v`
  - 耗时 **9.83 秒**，HTTP 200，`finish_reason="stop"`，`completion_tokens=350`。
  - `content` 与 `reasoning_content` 干净分层：
    - `content` 只有最终精炼答案，**0 prompt echo、0 `</think>` 泄漏、0 重复答案**。
    - 逻辑与数值完全正确：明确指出 9.9 大于 9.11，十分位 9 > 1。
- **数学运算题**（`计算 123 * 456 的结果，并简要写出计算步骤。`）：
  - 证据目录：`/tmp/rerot-semantic-xvac0nng`
  - 耗时 **10.48 秒**，HTTP 200，`finish_reason="stop"`，`completion_tokens=393`。
  - 最终结果 $56,148$，拆分 $123\times 400 + 123\times 50 + 123\times 6$ 步骤与验证完全正确。
- **Python 编程题**（`用 Python 写一个反转单链表的函数，包含 ListNode 定义。`）：
  - 证据目录：`/tmp/rerot-semantic-akn29mfq`
  - 耗时 **16.48 秒**，HTTP 200，`finish_reason="stop"`，`completion_tokens=1075`。
  - 最终回答包含完整 `ListNode` 定义、O(1) 迭代法、测试辅助函数与递归法对比，代码语法、指针反向逻辑完全正确。
- **Vulkan MoE 18/19 阈值 Hermetic 回归**：
  - 测试目标：`test-vulkan-moe-threshold`（CTest `#41`）。
  - 严格覆盖 `18 -> 19 -> 20 -> 18 -> 19 -> 18 -> 21` 跨越 vector-tiling 与 generic routing 阈值，多次反复测试与缓存复用验证通过，NMSE 保持在 $10^{-8}$ 级别，GPU 重复执行差异严格为 0。

长题 **`世界上每个大洲有哪些国家` 观察**：
- 8 个子任务项并发展开，多笔持续生成达 111 秒。在 manual 8192 context 下由于 8 个任务展开的详尽思考超出了 8192 context 硬限制，触发了 `rerot_resource_exhausted: episode token budget exceeded`，证明硬资源边界保护正常生效。在全量生产配置（131k/262k context）下可容纳全量展开。

---

## 2. 本班新增核心修复与改动

### 2.1 补齐 Hermetic Vulkan MoE 18/19 阈值回归测试

- 新增独立测试文件 `tests/test-vulkan-moe-threshold.cpp`，并在 `tests/CMakeLists.txt` 中注册为 CTest 测试项。
- 不依赖全模型下载，直接构造 `ggml_mul_mat_id` 图，比较 Vulkan GPU 与 CPU 标量参考在跨越 `mul_mat_vec_id_hybrid_max_cols = 18` 时的行为。
- 测试 `18 -> 19 -> 20 -> 18 -> 19 -> 18 -> 21` 序列及多次重复运行，确保以后任何改动都不会使 `mode 1` 丢失 `mode 0` 清零 workgroup。

### 2.2 清理 reasoning / content 展示层分层与 prompt echo

- **问题**：之前在非流式模式下，`server_task_result_cmpl_final::to_json_oaicompat_chat` 优先使用了未经解析的原始 `content`，覆盖了 `update()` 中已经分离开的 `oaicompat_msg`；且在 content-only parser 场景下残留了合成的 `</think>` 标记。此外，`rerot_serial_resume_prompt` 中原有自我问答句式诱导了模型在串行开头重述用户问题。
- **修复**：
  1. `tools/server/server-task.cpp`：在 `to_json_oaicompat_chat()`、`to_json_oaicompat_resp()` 和 `to_json_anthropic()` 中统一优先使用 `oaicompat_msg`。
  2. `tools/server/server-task.cpp`：在 `update_rerot_msg()` 中增加前缀剥离逻辑，若 parser 没有把内部闭合标记 `</think>` 剥离，则自动剔除合成的 `</think>\n` 前缀。
  3. `tools/server/server-context.cpp`：精简 `rerot_serial_resume_prompt` 为纯正指令提示（“并发推导完成。现在直接输出最终回答，不要重述问题，不要再输出标题：”）。
  4. `tests/test-chat.cpp`：增加单元测试 `test_rerot_nonstream_content_reasoning_clean()`，并在 main 中运行回归。

---

## 3. 测试与验证数据

- **CTest 回归套件**：
  ```bash
  ctest --test-dir build-vulkan-localhost -R 'rerot|kv-cells|triattention-score|test-vulkan-moe-threshold' --output-on-failure
  ```
  **10/10 PASS**（100% 通过）。
- **Chat 解析测试**：
  ```bash
  build-vulkan-localhost/bin/test-chat
  ```
  **[chat] All tests passed!**
- **代码格式与差异校验**：
  ```bash
  git diff --check
  ```
  **通过，0 issues**。
- **服务状态**：
  `systemctl status llama-server.service` 为 `inactive`，无开发后台进程占用显存或端口。

---

## 4. 下一班建议工作

1. **生产级大上下文下的长题闭合验收**：
   在 `--total-kv auto`（或 $c \ge 32768$）的配置下验证“世界上每个大洲有哪些国家”全部 8 个 child 自然闭合并在 final fence 串行收敛。
2. **性能与并发吞吐压测**：
   数值和展示层现已完全就绪，可以放心开展多笔并发吞吐（>= 500 tok/s）与 multi-reader 共享扫描的深度性能优化。
