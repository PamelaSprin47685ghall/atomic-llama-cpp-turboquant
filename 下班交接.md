# 下班交接：RERoT 当前真实状态

更新时间：2026-09-08。本文覆盖旧交接，作为下一班唯一入口。

## 1. 先看结论

当前原题 **`世界上每个大洲有哪些国家` 还不能算通过**。

最新隔离验收：

```text
证据目录：/tmp/rerot-semantic-a150w08w
HTTP harness：240.0185 s 后 TimeoutError
开发 server：正常退出，exit 0
forks：1
nodes started：9（root + 8 children）
nodes retired：1
final fence：0
最终 public document：未形成
```

这不是崩溃，也不能因为“思考很久”本身判错。真正未过的原因是：在这次 240 秒验收窗口内，8 个 child 只有 1 个完成自己的随机 `</ID>`，其余 7 个还在生成，因此没有进入 final acquire fence，也没有最终答案可验收。

最新 metrics：

```text
PUBLIC tokens   = 18653
PRIVATE tokens  = 2486
PENDING tokens  = 2769
frontiers       = 3492
final_fences    = 0
completed model tokens = 0   # episode 尚未完成，所以 completed counter 不结算
```

node 1 已在约 328 sampled tokens 后自然输出随机 close 并退出；node 2–8 在客户端超时时各约 2976 sampled tokens，仍在各自任务上继续。亚洲、非洲、欧洲、北美、南美、大洋洲、南极洲的 task identity 已明显比旧版稳定，但若干 Lane 仍会重复标题、列表或同一章节结构，尚未闭合自己的随机 ID。

因此下一班不得写“质量已过”“自然 stop 已过”“500 tok/s 已过”。性能阶段还没到。

---

## 2. 随机 ID 结束协议是正式规范，不要再改

child 使用随机 8 位 base62 control id：

```text
open  = <AbCdEfG0>
close = </AbCdEfG0>
```

硬规则：

- ID 属于 logical child，不属于 slot/pen；排队、resume、save/load 都保持原 ID。
- 只有当前 owner 的**完整精确随机 `</ID>`**结束 child。
- 不得改成 EOG 自动关、长度 stop、重复检测、固定 `</task>`、任意 8 位 ID close。
- 不得恢复“一行 child”限制；正文允许多行、多段、HTML、代码块。
- PENDING 只处理随机 close 的不完整 tokenizer 字节候选，匹配失败按原顺序释放。
- 最后 child 的 close 仍走已经实现的 acquire fence：恢复 pre-marker local recurrent、移除对应 attention KV suffix、用原 token/pos/run 在正常 server batch 中 replay，再进入 serial tail。

本班曾短暂试过 `EOG -> PRIVATE close`，已按用户要求**完整撤回**。源码当前仍以随机 ID 为唯一 child completion delimiter。

---

## 3. 已经确认并修掉的低层根因

### 3.1 Parallel Delta / GDN 数学

已修：

- `N=1` 显式走原生 recurrence，不靠近似单位 key + eps 假装等价。
- `beta=0` 为精确 no-op，不 clamp 到 `1e-6`。
- 多 writer 用 `sqrt(beta)` 对称缩放系统处理端点。
- CPU 有独立 double Cholesky oracle。
- Vulkan 原先只跑 N 次 CG，把“N 维精确算术 N 步收敛”误用到 FP32；真实 Ornith captured tensor 出现 brain 最大误差 `0.00918055`。现改为 4N 有界迭代，每 N 步重算真实残差并 restart；同 captured block brain 误差降到 `4.76837e-7`，output 降到 `7.45e-9` 量级。

### 3.2 重复 writer 的宽度放大

旧 block 对 N 个相同 writer 会产生：

```text
beta_eff = N*beta / (1 + (N-1)*beta + eps*beta)
```

例如 beta=.3、N=8 会放大到约 .774。已加入 coherence/evidence-density normalization，使重复证据不因为有 8 支笔就 8 倍“确信”；正交信息仍可独立写入。

### 3.3 时序/阶段混同

已直接改默认，不再做消融：

```text
read stage:
    own causal history
    own current K/V
    peer 已提交 memory

write stage:
    本 frontier 的各 Lane 产生自己的 K/V / recurrent transition

barrier

next frontier:
    peer 本轮 PUBLIC write 才可见
```

STRONG 现在表示“barrier 后下一 frontier 立即可见”，不再表示 same-forward peer 穿透；LAG1 再额外延迟一个已提交 frontier。

共享 RBB 也不再让当前 token 即时读 peer 本步 recurrent write；当前 output 必须按自己的 native transition 产生，block state 只能给未来 frontier。

### 3.4 shared recurrent 默认已降级

关键因果结论：DDVR 已经把 peer PUBLIC 文本读进 hidden，后续 GDN q/k/v/g/beta 又是该 hidden 的非线性函数；再把这些 transition merge 到 global recurrent brain，会把同一公共信息第二次广播，而从现有 q/k/v 无法唯一恢复“创新项”。

因此正式默认现在是：

```text
Full Attention KV：DDVR 跨 Lane 精确共享
GDN recurrent：child lane-local native recurrence
conv：lane-local
hidden / Q / sampler：lane-local
```

`LLAMA_REROT_RBB_ABLATION=shared-rbb` 只保留为科研模式，不是 release 默认，也不是质量差时自动 fallback。

2026-09-08 又修掉一个关键实现不一致：旧 10795 只有在**整个 recurrent microbatch 都是 child row**时才走 lane-local；只要 scheduler 同批混入 root/PRIVATE/另一 person，child 就会静默掉回 grouped/shared 路径。也就是说同一 logical child 的方程会随 batching shape 改变，这是实现 bug，不是数学定义。

现在 graph input 显式携带两套 row 集合：

```text
public_rows          = 所有 PUBLIC writer，供显式 shared-RBB 科研模式使用
default_shared_rows  = 默认允许推进 shared brain 的非-child rows
```

因此默认 child recurrence 是**逐 row 属性**，与 scheduler 如何拼 ubatch 无关。新增 mixed root+child 回归同时跑 CPU/Vulkan：

```text
child shared-brain change = 0
child output vs native oracle ~= 4.04e-8
child F16 hand error       ~= 4.76e-4
unrelated root brain       正常推进
```

### 3.5 Attention 没有被做成一样

F16 实际 A3 attention distribution 审计：

```text
observation 128: pair cosine mean ~= 0.0409, top-key overlap = 0
observation 192: pair cosine mean ~= 0.0354, top-key overlap = 0
observation 256: pair cosine mean ~= 0.0347, top-key overlap = 0
```

即不同笔确实在看不同位置。当前问题不能再归因于“所有 Q/attention 一样”。

---

## 4. 当前语义协议

### 4.1 Child 有 PRIVATE exact-title 身份锚点

每个 child admission 现在默认 PRIVATE 注入 exact title task identity：

```text
当前 Lane 唯一任务 = 自己的标题
其他公开章节只能参考，不得接管
正文阶段 ordinary <ol>/<li> 只是内容，不具有调度含义
完成后只关闭自己的随机 </ID>
```

这不是题目专用 prompt，而是 logical worker 身份协议。

### 4.2 Child 默认直接 worker；recursive planner 不再是 mandatory phase

旧版 child `planner_armed=true`，正文里随便一个 `<ol>` 都可能被 scheduler 解释为递归 fork，哪怕模型根本没收到 planner prompt。随后 10795 又走到另一个极端：每个 child 都强制再做一轮 PRIVATE planner，N=1 才进 worker。真实模型上这会把轻微漂移放大成 `planner -> <ol> -> child -> planner` 的递归反馈。

当前默认收敛为：

```text
child open / task identity
PUBLIC heading
direct terminal worker state
owner-specific random-ID close grammar
PRIVATE worker phase prompt
正文 worker phase
```

`arm_planner()` 与 child planner parser/runtime 能力仍保留，用于显式科研/未来协议；但**默认 admission 不自动调用**。正文 ordinary `<ol>` 永远只是内容，不再偷偷改 topology。

### 4.3 真实效果

最新固定短题 smoke：

```text
证据目录：/tmp/rerot-semantic-p5xc1xd1
prompt：9.11 和 9.9 哪个大？简要回答并说明理由。
temperature=0, seed=424242, max_tokens=512
dev config：8K ctx / 8K explicit KV / 2 pens / Turbo KV
root fork：3 children
90 s：TimeoutError
```

重要的是失败形态已经变化：重建每个 Lane 的真实 sampled token 后，node 1/2 都自然生成自己的随机 `</ID>`；每 Lane 的去空白 12-char 滑窗没有任何出现 3 次以上的重复片段，**没有旧式字符/句子循环坍缩**。node 3 在 90 秒窗口尚未结束。

剩余明显问题是语义重叠：node 1 与 node 2 文本 SequenceMatcher ratio 约 `0.864`，并出现 `9.11 -> 3.11/3.21` 数字漂移。这个问题应继续查 DDVR reader/task identity 与 Turbo 数值敏感性，不要再用字符级 recurrence 坍缩解释。

real-model fixed-token 4-step counterfactual仍保持每步 top token 与 native 相同：F16 最大 logits relative L2 约 `3.35e-4`；Turbo 最大约 `4.84e-2`。Turbo execution-shape sensitivity 仍是 P0 数值审计项，但目前没有短轨迹 top-token divergence 证据。

---

## 5. OpenAI reasoning effort 已落实

OpenAI Chat Completions：

```json
{"reasoning_effort":"low|medium|high|xhigh|max"}
```

Responses：

```json
{"reasoning":{"effort":"low|medium|high|xhigh|max"}}
```

两者进入同一链路。

原则：

1. 原生支持 `reasoning_effort` 的 chat template：原样透传 effort，不转 token quota。
2. 显式 `reasoning_budget_tokens` / `thinking_budget_tokens`：用户精确 budget 永远优先。
3. 只有旧式/本地 backend **只有 token-budget 控制**，且请求有有限 output cap 时，才做 compatibility mapping。
4. 没有有限 output cap 时不凭空制造 absolute token budget。
5. RERoT 私有 child 生命周期仍由随机 ID 管，reasoning budget 不替代随机 close。

查 Anthropic 官方与开源实现后的最终 fallback：

```text
none    = 0
low     = 20%
medium  = 50%
high    = 80%
xhigh   = 95%
max     = 100%   # 受 budget < max_tokens 约束，实际最多 max_tokens-1
```

说明：Anthropic 原生 effort 是 soft/adaptive behavior，不是固定 quota。OpenRouter 的公开 budget-only fallback 为 20/50/80/95/95%；Cline 的完整 ladder 为 20/50/80/95/100%。因为本项目 API 同时暴露 xhigh 和 max，为保留两个档位的语义差异，budget-only fallback 采用 distinct max。

`max_completion_tokens=8192` 时：

```text
low     1638
medium  4096
high    6553
xhigh   7782
max     8191
```

另有 128k reasoning-budget ceiling，并在可行时应用 1024 下限。

`test-chat` 已覆盖：五档 template 透传、budget mapping、Responses 转换、显式 budget override、非法 effort 拒绝。

---

## 6. Hand checkpoint / final fence /状态持久化

已修并有回归：

- hand restore 验证完整 blob 后才写，避免半写。
- restore 会恢复 recurrent cell position。
- shared recurrent tail restore 会 COW，不污染 sibling。
- snapshot capture 使用 `cells[tail].src` / `rs_idx`，不是盲读物理当前行。
- root PRIVATE/PUBLIC brain basis 切换时做 hand 坐标变换，保持有效 state 连续。
- final close 保存 pre-marker hand + 原始 tokenizer token/pos/run tape。
- final survivor 确认后 restore pre-marker recurrent、删自己的 attention suffix、正常 server batch replay 原 close rows。
- 不 nested `llama_decode()`，不覆盖其他 slot logits。
- episode state version 已升到 v3，旧 v2 明确拒绝。

---

## 7. 最新测试状态

最近一次完整相关回归：

```text
CTest related suite: 8/8 passed
test-rerot-parser: 0 failures
test-rerot-runtime: 0 failures
test-rerot-attn: 0 failures
test-rerot-recurrent: 0 failures
test-chat: All tests passed
git diff --check: passed
```

真实模型数值工具：

- `scripts/rerot-semantic-smoke.py`
- `scripts/rerot-audit-replay.py`
- `scripts/rerot-attention-audit.py`
- `test-rerot-model-batch`（需显式本地 model，不属于 hermetic CTest）

生产安全状态：

```text
llama-server.service = inactive
无残留开发 llama-server 进程
本轮 SOTA 修正将单独 commit；最终 hash 以 git log / 交班回复为准
未 push
未 deploy
```

---

## 8. 版本考古结论：不要再把 build number 当源码 commit

这次已经把 `10794 -> 10795` 的关键歧义查清。以后做 A/B 必须同时保存 `git HEAD + dirty status + server --version + 每个 runtime .so SHA`。

### 8.1 3.04 秒 / 2-Lane 的真实正常证据属于 build 10788

journal 中 `task=29054, episode=4` 的真实记录：

```text
prompt = 9.11 和 9.9 哪个大？简要回答并说明理由。
root fork children = 2
HTTP 200
wall = 3.041207 s
aggregate = 141.391 tok/s
```

它对应 2026-09-04 保存至 `/opt/llama/bin` 的 artifact：

```text
llama-server                 9fcd58b10b9b26c80482f1087b1e53a46933818ed81098aca06b664edc7e7694
libllama-server-impl.so      2ca88ec059bcba725a1c96a6b832d72ddad2ffd7897485c3f69c6be5e1bdb93d
libllama.so.0.0.10788        700d75c9551742ad042ae6fe429b81b63db90a02f573a236a90c96ac5dabce07
```

build-count 10788 对应 commit `260af4501b414f2c5fea9069a7a39d35978f95b3`。该版本使用旧 same-frontier STRONG，child admission 是 heading 后 direct worker；它**没有验证**后来的 snapshot-aware hand seed、barrier-after STRONG 或 mandatory child planner。

### 8.2 交班时名为 10794 的 `250e...` 不是 clean `0aede756`

`0aede756da9133d5d85be8ed1bcfa1390f7cd01e` 的 commit count 确实是 10794；但 fresh clean build 得到：

```text
libllama.so.0.0.10794 SHA = 132281ecfba9e9406411952929d3af4f74f6ee3f424ed37cece2e5ab45ff1ddb
```

而交班时磁盘 artifact：

```text
libllama.so.0.0.10794 SHA = 250e9f47e770b212080dca602bf3d1ecf24d6c02d76721dcac032d40134ed6dd
```

其机器码已经包含 10795 才加入的 root coordinate transform、snapshot-aware hand seed 与 `shared-rbb/raw-redundant/is_child_row` 等符号；配套 `libllama-server-impl.so=599ce790...` 也已经导出 `server_rerot_child_planner_prompt`、`server_rerot_child_worker_prompt`、`arm_planner`。

结论：这是一套**配置版本号仍为 10794、源码却经过后续 dirty 增量重编的混合 artifact**，不能归属 clean `0aede756`。所谓“重新 build 10795 后变坏”至少有一部分其实是“从不一致的 dirty artifact 集合切换到 coherent fresh build”。

### 8.3 root planner 2 -> 3/5 也不是纯数值回归门

build 10791 的 `4fc5c2213` 已经改过 root planner prompt：从“按依赖、遮住其他项仍可独立完成才拆”变成“可同时展开的并列对象/章节尽量拆”。fresh clean `0aede/10794` 用固定 `temperature=0, seed=424242` 的 9.11 请求，root 已拆成 3 项；因此不能拿 10788 的 2 项与 10795 的 4/5 项直接做 recurrent 数值 A/B。

真正的数值回归门应该使用固定 token tape / next-logits equivalence，而不是 planner 项数。

---

## 9. 下一班优先级

### P0：先查 Turbo / reader-view 的固定 token 数值漂移，不再用 planner 项数代替数值门

最新 evidence：

```text
/tmp/rerot-semantic-p5xc1xd1
/tmp/rerot-sota-model-single-f16.log
/tmp/rerot-sota-model-single-turbo.log
```

已知：F16/Turbo 的 4-step fixed-tape top token 都与 native 相同，但 Turbo logits relative L2 可到约 `4.84%`，远高于 F16 的 `3.35e-4`。下一步应把 fixed tape 拉长，并在第一次 top-token divergence 前后逐层比较 DDVR Q/K/V、indexed attention 与 recurrent output；不要从自然语言输出倒推 kernel。

同时继续验证：同一个 logical child 在 child-only、mixed root+child、不同 row permutation 三种 batching shape 下必须得到同一 recurrence/output（允许明确的浮点容差），这应成为正式 regression gate。

### P1：查跨 Lane 语义重复与数字漂移

最新 9.11 smoke 已不见字符级复读，但 node1/node2 文本相似度约 `0.864`，且出现 `9.11 -> 3.11/3.21`。优先区分：

- child 的 owner-private exact-title/task anchor 是否在 reader view 中一直稳定；
- DDVR 是否给 sibling 公共正文过高权重，导致两支笔做同一份工作；
- 数字漂移是否先出现在 attention readout / Turbo cache，而不是 worker prompt；
- PRIVATE control 若出现在 PUBLIC 文本里，用 trace 的 `injection=0` sampled token 判断模型复读，不能凭字符串认定 transport 泄漏。

不要为了压相似度重新启用 mandatory child planner；它是已知的拓扑放大器。

### P2：再跑长题、验收完整 final fence 与最终正文

直到全部 child close 后，才检查：

- final fence replay 是否只执行一次；
- serial tail 是否自然生成最终用户可见答案；
- 国家列表是否正确、完整、无严重重复/串洲；
- usage/max_tokens 语义是否符合接口约定。

### P3：最后才做性能

质量过门后再优化 parallel decode 到目标 >= 500 tok/s。当前任何低于/高于该数的中间测量都不能覆盖语义失败。

---

## 10. 明确不要做的事

- 不要恢复 child 一行限制。
- 不要用 EOG/长度/repetition 自动替代随机 `</ID>`。
- 不要把 shared RBB 静默设回默认。
- 不要把 same-frontier peer K/V 同拍穿透重新叫做 strong。
- 不要用 topic stop、固定洲名 heuristic、字符串截断伪造 PASS。
- 不要因为短窗口 timeout 就宣布模型“死循环”；先看 child 是否仍在语义推进。
- 不要因为单元测试全过就宣布真实原题已过。
- 不要在质量没过前部署或报 500 tok/s 完成。

---

## 11. 最后一句

当前 SOTA 基线已经把 child recurrence 的数学定义从“batch shape 决定”修成逐 row 不变量，并取消默认 mandatory recursive planner。最新 9.11 smoke 中旧式字符/句子循环未复现，但仍有跨 Lane 内容高度相似、数字漂移和 90 秒未全部闭合。**所以低层稳定性显著改善，但真实质量仍未 PASS；下一步应继续 fixed-token 数值定位，而不是再改随机 ID 或停止条件。**
