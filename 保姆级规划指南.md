# RERoT 保姆级实施规划指南

> 项目：`atomic-llama-cpp-turboquant`
>
> 目标模型：当前生产基线 Ornith / Qwen3.5 hybrid（Gated Delta Net + 周期性 Full Attention）
>
> 目标：在不重新训练模型的前提下，实现 Recursive Elastic Ring-of-Thought（RERoT）+ Dynamic Dense Virtual RoPE（DDVR），让模型在推理期动态 fork 多个 Lane，实时共享公开 KV，并在最后一个自然结束的 Lane 上直接恢复串行输出。
>
> 本文是实施手册，不是概念讨论。默认后续开发者不需要重新阅读完整设计对话，只要严格按本文的阶段、接口、不变量和验收门推进即可。
>
> 2026-09-04 修订：recurrent 由“lane-local/COW 私有”改为“GDN matrix 全局共享黑板（RBB/RSV）+ conv 全员私有”；KV 侧增加 multi-query 共享扫描与 N-row batch 执行口号。旧 lane-local/COW 全文降级为 Scheme 0 对照基线（见 §14.2），除过渡研究用的 transition-sharing 问题定义外，不再是正式默认。

---

## 0. 一句话总设计

RERoT 在这个项目里的正确实现不是“多开几个 server completion”，也不是“把多条 CoT 最后 merge”，而是：

```text
普通单线程思考
    ↓
PRIVATE planner prompt
    ↓
PUBLIC 问题规模分析
    ↓
完整 <ol> 原子提交
    ↓
N = 1 ───────────────→ 当前 Lane 继续串行思考
    │
    └─ N > 1
          ↓
      parent freeze
          ↓
      child descriptors
          ↓
   有槽位的立即运行
   没槽位的 FIFO 排队
          ↓
   sampled/PUBLIC/PENDING：每 Lane 每 frontier 一个 token
   连续 PRIVATE control：最多 32 token causal microbatch
          ↓
   PUBLIC KV 实时共享
   recurrent matrix 全局共享黑板 + conv 私有（RBB/RSV，见 §14）
          ↓
   Lane 可以递归再次 <ol> fork
          ↓
   Lane 私有 </think> 自然退出
          ↓
   queue 为空 + 最后一个 writer 退出
          ↓
   final </think> acquire fence
          ↓
   不 merge、不回 main
          ↓
   从最后 Lane 直接串行 tool call / 正文
```

核心内存模型（2026-09-04 修订，替代旧“recurrent lane-local”默认）：

```text
PUBLIC Full-Attention KV = shared memory（精确结构化白板，DDVR reader view）
Global GDN recurrent S  = shared fuzzy blackboard（一人三笔同一个脑，RBB/RSV）
Lane-local conv tail    = 每支笔手部运动缓存（kernel=4，绝不共享）
Lane hidden/residual/Q/sampler = 每支笔私有
PRIVATE planner / think = thread-local control state
DDVR view               = reader-specific virtual address space（只管 KV，不管 recurrent 顺序）
```

这与当前 Qwen3.5 hybrid 结构天然吻合：

```text
R0 → R1 → R2 → A3 → R4 → R5 → R6 → A7 → ... → A39
               ↑                   ↑
          shared-memory       shared-memory
             load                load
```

Full Attention 层从共享 KV 读取其他 Lane 的最新公开内容；它的输出继续进入后面的 recurrent 层，因此后续 recurrent state 会自然吸收共享信息。旧 v1 曾规定“recurrent lane-local/COW、不做 foreign-token replay、不直接 merge”，现降级为 Scheme 0 对照基线（见 §14.2）；正式 v1 改为 §14 的共享黑板：GDN matrix 全局一份、conv 全员私有、frontier 对称提交。顺序与结构交给 DDVR，recurrent 只做模糊共享工作记忆。

---

## 1. 当前项目事实：开发前必须接受的现实

### 1.1 这不是 stock llama.cpp

当前 fork 已经有以下重要基础，RERoT 必须复用，禁止重新造平行体系：

- unified KV；
- `llama_kv_cells` 是唯一 physical KV metadata owner；
- sparse position / TriAttention reclaim；
- backend-native KV compaction；
- recurrent / hybrid memory；
- MTP speculative path；
- Turbo K/V；
- Vulkan 是生产主 backend；
- server continuous batching；
- 多 sequence batch；
- recurrent `seq_cp()` 已具有共享 tail / COW 行为。

因此：

**不要新增第二套 KV cell registry。**

RERoT 所有 physical-cell 相关 metadata 必须扩展进 `llama_kv_cells`，或通过它唯一可验证地派生，不能再维护一份会与 compaction / TriAttention / restore 漂移的 cell 数组。

### 1.2 当前 Ornith 是 hybrid，不是纯 Transformer

Qwen3.5 在本项目中：

- full-attention layers：`3, 7, 11, ..., 39`；
- 其余主干层为 Gated Delta Net recurrent layers；
- recurrent state 在 stock 语义下是每个 sequence 的私有状态；
- full-attention KV 可以成为共享内存；
- GDN 每层另有 kernel=4 causal depthwise conv state，与 recurrent matrix 独立保存。

所以旧设计中“v1 不支持 recurrent/hybrid”的限制在本项目必须废弃。

新的正式语义（2026-09-04 修订）：

```text
attention memory  = shared（DDVR 精确共享）
recurrent matrix  = episode-global shared blackboard（RBB/RSV，一人三笔同一个脑）
conv state        = lane-local（每支笔手部运动缓存，绝不共享）
```

旧“recurrent lane-local fork-time COW”保留为 Scheme 0 对照基线，不再是正式 v1 默认。Qwen3.6-35B-A3B 同构为 40 层、`10 × (3×GDN + 1×Full Attention)`，30 个 GDN + 10 个 full attention；每 GDN 约 `32 heads × 128 × 128 FP32 ≈ 2 MiB`，30 层约 60 MiB/Lane（见 §14 数学与成本核算）。

### 1.3 Qwen3.5 文本路径使用 IMRoPE / 4-position 输入

当前 `llm_graph_input_pos::set_input()` 对文本 token 做：

```text
p0 = text_position
p1 = text_position
p2 = text_position
p3 = 0
```

因此 DDVR 对 Qwen3.5 文本的虚拟平移不能只在文档里写成“1D offset”然后直接照搬。

对于 text-only RERoT v1，虚拟位置平移必须等价地作用于前三个 position coordinate，第四维保持 0。

在没有通过数值单元测试之前，禁止宣称 IMRoPE DDVR 正确。

### 1.4 server slot id 不能继续被理解成 logical Lane id

RERoT 允许：

- child 数不做协议上限；
- 物理运行槽位有限；
- 多余 child 排队；
- parent fork 后停止运行；
- slot 随后可被另一个 logical child 复用。

所以：

```text
server_slot.id != RERoT logical node id
```

必须把：

```text
HTTP request
physical execution slot
llama seq_id
logical thought node
```

四个概念拆开。

---

## 2. 冻结后的产品契约

### 2.1 RERoT OFF

必须满足：

```text
完全保持当前 atomic / TriAttention / MTP / unified KV / RAM swap / preemption 行为。
```

RERoT 代码不能改变普通请求的：

- mask；
- RoPE；
- KV layout；
- recurrent state；
- sampler；
- slot lifecycle；
- speculative decoding；
- TriAttention reclaim；
- metrics 语义。

所有新路径必须由明确的 `rerot_enabled` / episode-active gate 隔离。

### 2.2 RERoT ON

正式 v1 语义：

1. Root 正常进入思考。
2. Runtime 私下注入固定 planner prompt。
3. 模型公开分析规模、难度、依赖关系。
4. 模型输出完整 `<ol><li>...</li>...</ol>`。
5. 未闭合 `<ol>` 只对 owner 可见。
6. 闭合 `<ol>` 原子公开。
7. N=1：当前 Lane 继续，不 fork，不再递归 planning。
8. N>1：parent freeze，为全部 `<li>` 建立 logical child descriptor。
9. child 数不设置 planner-visible 上限。
10. 物理执行槽位有限；多余 child 全局 FIFO 排队。
11. queued child 不提前生成空 heading、不占 KV、不占 recurrent physical slot；共享黑板下只需 logical descriptor + 私有 conv tail（很小），admission 时直接接当前最新 GLOBAL recurrent brain + 最新 PUBLIC DDVR 白板，不冻结整个脑子；R0-R2 私有对照分支仍可用 parked recurrent seq/COW（见 §6/§14.2）。
12. child admission 时生成 `<hN>title</hN>`。
13. child 能看到父节点的 `<ol>` 规划；默认作为 worker 直接推导正文，不强制输出 `<ol>`；若 LLM 自身输出合法 `<ol></ol>` 则继续树形 fork。
14. PUBLIC 普通自然语言按 frontier 实时共享。
15. PRIVATE / PENDING 内容不进入其他 Lane 的 lexical attention view。
16. Full-Attention KV 是共享内存（精确结构化白板）。
17. GDN recurrent matrix 是 episode-global 共享黑板（RBB/RSV），conv 全员私有；PRIVATE/PENDING 永不参与 global commit；lane-local COW 仅为 Scheme 0 对照基线。
18. 每个 reader 使用 PAC-DFS + DDVR 构造不同的连续虚拟文档。
19. 不允许两个可见 token 占用同一个 virtual position。
20. 所有 span 共用一个 attention softmax。
21. 非最终 `</think>` 是私有 thread exit。
22. queue 非空时，哪怕只剩一个 RUNNING Lane，也不能进入 final。
23. 真正最后 Lane 的 `</think>` 在稳定共享内存上重新评估，充当 acquire fence。
24. 不做 merge agent。
25. 不返回 main。
26. 从最后 Lane 直接进入普通 tool call / assistant 正文。
27. 超出全局硬资源时，全 episode HARD_ABORT，不选择 survivor，不补答案。

---

## 3. 固定 Planner Prompt

先保持用户指定语义，不往里面塞硬件槽位、child 上限或“你负责第 i 项”。

这段文字虽然通过 PRIVATE ChatML control turn 注入，但措辞必须是**模型自己的
第一人称自然内省**，不要啰嗦（not wordy），也不要像外部调度器在发布繁复命令。

实现文本：

```text
我把可以并行思考的独立要点梳理成一个平面的 HTML 有序列表：以 <ol> 开头，每个独立要点写一个 <li>，以 </ol> 结尾。如果不需要拆解，我就只写一个 <li>。输出完 </ol> 后我再展开分析。
```

避免冗长的说教式禁止列表（如“严禁... 绝不... 禁止解释格式...”），也不预设特定
应用场景（如“问答”、“最终答案”等）。以自然、简明的思考意图直接引导模型。

PUBLIC `<li>` 是简洁、可独立推进的任务描述，不强制以“我”开头：

```html
<ol>
<li>计算 19×7 并核对乘积</li>
<li>确认日本首都的标准称呼</li>
</ol>
```

各条任务应是可以并行推进的互补部分；简单问答、一次运算或单一判断只输出一个
条目。公开推理引用其他部分时，使用章节标题、命题、公式或具体结论，不依赖
“上面、下面、前者、后一段”等版面相对指代。完整输出 `</ol>` 后立即停止规划，
不继续展开正文。

这段：

```text
owner lexical visible       = yes
foreign lexical visible     = no
final transcript            = no
causal influence on output  = yes
```

注意：这不是安全边界。

对于 hybrid 模型，parent recurrent state 也会受到 private planner prompt 影响，fork child 继承该 state；因此“PRIVATE”严格含义仍然只是 **lexically private**。

---

## 4. 文档语义与 Runtime 语义分层

### 4.1 公开文档层

最终可读 reasoning document 只由这些组成：

```text
自然语言分析
<ol>...</ol>
<h1>...</h1>
<h2>...</h2>
...
公开推导正文
```

拿掉 scheduler 后，这仍然必须是一篇人能正常读懂的结构化研究笔记。

### 4.2 私有控制层

以下内容不进入共享文档：

```text
<think>
固定 planner prompt
非最终 </think>
runtime parser state
queue state
logical node id
physical slot id
parked seq id
```

### 4.3 Heading 规则

父 `<li>` 文字机械转换成 child heading：

```html
<li>计算 19×7 并核对乘积</li>
        ↓
<h1>计算 19×7 并核对乘积</h1>
```

层级映射：

```text
root child depth 0 → <h1>
depth 1            → <h2>
...
depth >= 5         → <h6>
```

真实深度永远保存在 runtime tree metadata。

超过 h6 时可以选择显示机械章节路径：

```html
<h6>§2.3.1.4.2.1.3 低频 RoPE 相位性质</h6>
```

但路径只用于稳定引用，不用于告诉模型“你是 Lane 2.3.1”。

---

## 5. 新的数据模型

建议新增：

```text
src/llama-rerot.h
src/llama-rerot.cpp

tools/server/server-rerot.h
tools/server/server-rerot.cpp
```

### 5.1 Core：不要让 server 自己理解 physical KV

`src/llama-rerot.*` 负责：

- episode id；
- node / run metadata；
- cell visibility tag；
- reader span table；
- DDVR virtual position；
- RERoT attention graph input；
- parked recurrent seq helper；
- archive seq helper；
- hard invariants。

server 只负责：

- planner parser；
- tree；
- queue；
- admission；
- sampler；
- response owner；
- frontier scheduling。

不要让 `server-context.cpp` 直接遍历 `llama_kv_cells`。

### 5.2 `rerot_episode`

建议：

```cpp
struct rerot_episode {
    uint64_t id;

    int root_task_id;
    int response_task_id;

    uint64_t frontier;
    uint64_t publish_epoch;

    rerot_node_id root;

    std::deque<rerot_node_id> ready_queue;
    std::set<rerot_node_id> running;
    std::set<rerot_node_id> starting;

    llama_seq_id archive_seq;

    bool topology_barrier_pending;
    bool finalizing;
    bool hard_aborted;
};
```

### 5.3 `rerot_node`

```cpp
enum class rerot_node_state {
    planning,
    terminal_running,
    forked,
    queued,
    starting,
    running,
    retired,
};

struct rerot_node {
    rerot_node_id id;
    rerot_node_id parent;

    uint32_t depth;
    uint32_t child_index;
    std::vector<rerot_node_id> children;

    std::string title;

    rerot_node_state state;

    int physical_slot = -1;
    llama_seq_id exec_seq = -1;
    llama_seq_id parked_seq = -1; // 仅 Scheme 0 / R0-R2 私有对照；共享黑板正式路径改用私有 conv tail + 接入 global S
    // TODO(shared-RBB): queued child 私有 conv tail 字段（约 2–4 MiB/Lane 量级，30 层 kernel=4）

    llama_pos storage_pos_next = 0;

    std::vector<rerot_run_id> public_runs;
    std::vector<rerot_run_id> private_runs;
    std::optional<rerot_run_id> pending_record;

    rerot_planner_parser parser;
    common_sampler_ptr sampler;

    bool planner_armed = true;
    bool exit_intent = false;
};
```

### 5.4 `rerot_run`

一个 run 是连续使用同一 visibility、同一 owner、同一语义类型写出的 token 区间。

```cpp
enum class rerot_visibility : uint8_t {
    normal,
    public_live,
    private_control,
    pending_record,
};

struct rerot_run {
    rerot_run_id id;
    rerot_node_id owner;
    rerot_visibility visibility;

    llama_pos storage_pos0;
    uint32_t token_count;

    uint64_t publish_epoch;
};
```

### 5.5 Physical cell metadata

扩展 `llama_kv_cells`，不要新建平行数组。

建议至少增加：

```cpp
struct llama_kv_rerot_meta {
    uint64_t episode_id = 0;
    uint32_t node_id = 0;
    uint32_t run_id = 0;
    uint64_t publish_epoch = 0;
    rerot_visibility visibility = rerot_visibility::normal;
};
```

每个 non-empty cell 仍遵守现有：

```text
至少一个 seq ref
pos/ext/seq 由 llama_kv_cells 唯一拥有
compaction 必须同时搬 metadata
state save/restore 需要明确 RERoT episode 是否允许持久化
```

v1 最简单策略：

```text
RERoT active episode 禁止 slot save / prompt cache persist。
```

先不要同时解决跨请求持久化。

---

## 6. seq_id 规划：这是实现成败的关键

> 2026-09-04 修订：共享黑板下 queued child 与 recurrent seq-id capacity 解耦。§6.2–§6.3/§6.5 的 parked seq + COW + logical capacity 保留为 R0-R2 私有对照分支与 Scheme 0 所需；正式共享路径见本节末尾“共享黑板简化”。

### 6.1 三类 id（对照分支；共享路径可大幅简化）

RERoT 内部需要：

```text
exec seq ids       = 当前 physical server slots 对应的低位 seq ids
parked seq ids     = queued child 的 recurrent COW 引用（仅 Scheme 0 / R0-R2 私有对照需要）
archive seq ids    = 已 freeze 公共 KV 的保活引用
```

共享黑板简化：queued child 不需要 parked recurrent seq / recurrent COW / logical recurrent seq capacity，只需 logical tree node + title + sampler/parser state + 私有 conv tail；admission 时直接接当前 GLOBAL brain，因此 logical child count 与 recurrent seq-id capacity 脱钩。

### 6.2 为什么 queued child 不需要复制 recurrent state

当前 `llama_memory_recurrent::seq_cp()` 已经做：

```text
tail_src cell.seq_id += seq_id_dst
tail_dst = tail_src
```

也就是：

```text
parent state
     │
     ├── parked child A
     ├── parked child B
     └── parked child C
```

没有复制 state tensor。

等某个 child 真正开始写时，现有 `find_slot()` 发现共享 recurrent cell，再为它分配独占 physical recurrent cell。

这正是我们需要的 fork-time COW。

### 6.3 需要新增 recurrent-only sequence operation

当前 `llama_memory_hybrid::seq_cp()` 会同时复制：

```text
attention refs
recurrent refs
```

RERoT parking 只需要 recurrent。

建议给 memory interface 加内部扩展，而不是滥用完整 `seq_cp()`：

```cpp
virtual bool seq_cp_recurrent_only(...);
virtual bool seq_rm_recurrent_only(...);
virtual bool seq_cp_attention_only(...);
virtual bool seq_rm_attention_only(...);
```

对于不支持的 memory implementation 默认返回 false / unsupported。

`llama_memory_hybrid` 直接转发到对应子 memory。

这样（对照分支）：

```text
park child     → recurrent-only cp
freeze public  → attention-only cp to archive
release exec   → 分别清理 attn/recurrent exec refs
```

职责清晰，不会把 parked seq_id 污染到整条 KV history。共享黑板正式路径：park child 不做 recurrent-only cp，freeze public 仍做 attention-only cp to archive，global recurrent 无需 per-child lineage。

### 6.4 logical child 没有协议上限，但有硬资源上限

不能把 `LLAMA_MAX_SEQ=256` 写进 planner prompt。

正确行为：

```text
模型可以输出任意数量 child
runtime 尝试创建全部 descriptor + parked state
如果 seq-id arena / RAM / KV / context 等全局硬资源无法容纳
→ HARD_ABORT entire episode
```

绝不：

```text
只执行前 8 个 child
静默丢掉后面的 <li>
```

### 6.5 hybrid memory 的 logical seq capacity（对照分支需要；共享正式路径不需要）

当前 hybrid memory 构造时 recurrent `n_seq_max` 使用 `cparams.n_seq_max`，生产通常等于 physical slots 数。

对照分支（Scheme 0 / R0-R2 私有）RERoT 需要：

```text
physical recurrent state capacity 仍然 = rs_size / fitted slots
logical recurrent seq id capacity  = LLAMA_MAX_SEQ
```

不要把 physical state tensor 扩到 256 slots。

只扩大：

- `tails[]` / logical mapping capacity；
- seq-id validation range。

保持 physical recurrent capacity 不变。

---

## 7. Freeze / Archive：如何释放 parent slot 但保留公共 KV

### 7.1 为什么不能直接复用 parent exec seq

如果 parent 使用 slot/seq 2，fork 后直接把 slot 2 给 child，而不处理旧 KV：

- parent 和 child 会共用 seq 2；
- local positions 可能重叠；
- `seq_rm(2, ...)` 会误删 parent；
- normal causal mask / prompt cache 会误判连续性；
- 后续 reuse / TriAttention metadata 会混乱。

所以 parent freeze 必须把公开历史从“执行 seq ownership”转成“episode archive ownership”。

### 7.2 archive 流程

parent fork / retire 时：

```text
1. 找到 parent 的 PUBLIC runs
2. attention-only seq_cp(parent_exec_seq → episode_archive_seq, each public run)
3. PRIVATE / PENDING 不 archive
4. recurrent state（对照分支）：
   - fork 时先 recurrent-only cp 到所有 parked child
   - retire 时不需要 child snapshot
   共享黑板正式路径：无 per-child recurrent cp；global S 继续向前，
   PRIVATE/PENDING token 不参与 global commit 即可。
5. attention-only rm(parent_exec_seq)
6. recurrent-only rm(parent_exec_seq)（共享路径下仅清理 exec 私有部分如 R0-R2/conv lineage）
7. physical slot 可复用
```

archive seq 的用途只有：

```text
让 non-empty public cells 至少保留一个 seq ref
```

RERoT reader visibility **不能**再通过 archive seq membership 判断。

真正 visibility 由 RERoT metadata + view span table 定义。

### 7.3 Active Lane 的 public cell

Active Lane 尚未 freeze 时：

- cell 可以只有 exec seq ref；
- foreign reader 仍然允许读取它；
- RERoT mask 不再要求 `cells.seq_has(cell, reader_seq)`。

这就是共享内存和普通 llama.cpp sequence membership 的最大语义区别。

---

## 8. PAC-DFS：每个 reader 怎样排版整棵树

### 8.1 规则

对于 node `n`，children 为：

```text
c0 c1 c2 ... ck
```

若 reader 位于 `ci` 子树：

```text
render own public prefix
render c(i+1)...ck
render c0...c(i-1)
render ci recursively last
```

若 reader 不在这个 subtree：

```text
按原始 <ol> child 顺序稳定 DFS
```

### 8.2 关键不变量

对任意 reader：

```text
1. parent heading 永远在 child heading 前
2. 每个 public token 只出现一次
3. 当前 reader 在每一级都位于最后
4. off-path subtree 不因内部活跃 writer 改变而洗牌
5. 新 token 只导致后续 virtual position 后移
6. virtual position 全局连续、无重复
```

### 8.3 view builder 输出

server tree 不应该直接生成 physical KV indices。

建议先生成逻辑：

```cpp
struct rerot_view_run {
    rerot_run_id run_id;
    llama_pos virtual_pos0;
    uint32_t count;
};
```

core 再根据 `run_id` 解析当前 physical cells。

这样 TriAttention compaction 后 physical index 改变，server tree 不需要更新。

---

## 9. DDVR：位置层的正式实现

### 9.1 不移动 K

一个 run 在写入时使用 storage position：

```text
s, s+1, ..., s+L-1
```

缓存 K：

```text
K_m = R(s+m) K̄_m
```

reader 把它映射到：

```text
v, v+1, ..., v+L-1
```

query virtual position 为 `q`。

目标 score：

```text
< R(q) Q̄, R(v+m) K̄_m >
```

利用相对旋转：

```text
< R(q+s-v) Q̄, R(s+m) K̄_m >
```

所以：

```text
K 一字节不动
每个 source span 对 Q 使用不同 phase offset
```

### 9.2 绝对禁止 per-span softmax

错误：

```text
softmax(QK_span_A)VA + softmax(QK_span_B)VB
```

正确：

```text
scores_A | scores_B | scores_C | ...
              ↓
        one global softmax
              ↓
           all V
```

Vulkan 最终实现必须使用 online softmax 的 `(m, l, O)` 跨 span 累积。

### 9.3 Qwen3.5 IMRoPE

text-only 当前 input position 为：

```text
(p, p, p, 0)
```

所以 text RERoT span offset `Δ` 的 materialized baseline 必须使用：

```text
(p+Δ, p+Δ, p+Δ, 0)
```

而不是：

```text
(p+Δ, p, p, 0)
```

也不是给第四轴加 offset。

### 9.4 v1 范围

第一版只支持：

```text
text-only Qwen3.5 / Ornith
固定 episode 内 RoPE 参数
无 image/audio MRoPE
```

多模态请求若打开 RERoT：

```text
return unsupported / fallback serial
```

不要偷偷把图像 position 当文本 position。

---

## 10. Attention 实现：分三步，不要第一天就写 Vulkan fused kernel

### Phase A：Materialized 数学基准

先写测试 helper，不接模型：

```text
random Q/K/V
random storage positions
random virtual positions
```

Baseline：

```text
显式把 K 从 storage phase 重相位到 virtual phase
→ 普通 dense attention
```

DDVR reference：

```text
K 不动
按 span 转 Q
→ global softmax
```

要求 F32 数值只差浮点舍入。

这是整个项目的第一个硬门。

### Phase B：CPU correctness op

新增 experimental op，例如：

```text
GGML_OP_FLASH_ATTN_EXT_REROT
```

输入建议：

```text
src0 = Q
src1 = K
src2 = V
src3 = visibility/mask
src4 = sinks (如需要)
src5 = reader-relative cell/span metadata
```

或者将 metadata 拆成：

```text
physical index
storage position
virtual position / offset
visibility
```

具体 tensor layout 在 Phase A 数学测试通过后再冻结。

CPU op 只追求正确，不追求快。

### Phase C：Vulkan fused DDVR FlashAttention

最终生产路径不要真的复制 Q N_span 次。

应修改 Vulkan FA shader，使每个 KV block / cell group 读取：

```text
delta = query_virtual_pos + storage_pos_base - virtual_pos_base
```

然后在 Q load / dot-product 前应用 reader-relative RoPE。

要求：

- Turbo K/V 继续直接走现有 dequant path；
- global online softmax 不变；
- split-K 能工作；
- GQA 能工作；
- no extra full-K readback；
- 不破坏普通 `GGML_OP_FLASH_ATTN_EXT`。

推荐新增单独 pipeline variant，不在普通 FA shader 每个 inner loop 分支判断 `rerot`。

#### Phase C-2：multi-reader 共享扫描（2026-09-04 新增，一人三笔的 KV 白嫖）

PAC-DFS 让每 reader 虚拟顺序不同，但 attention 不要求按虚拟顺序物理扫描：softmax 对遍历顺序置换不变，只关心每 K 对该 reader 的 virtual position 与是否可见。因此所有 reader 按同一 physical KV tile 顺序扫白板：K/V tile 只从 HBM 读一次、Turbo 只解量化一次，同时喂给 A/B/C 三组 Q，各自维护 DDVR phase 与 `(m,l,O)`。Qwen Full Attention 是 16 Q / 2 KV（8:1 GQA），三笔后一次 KV load 服务 24 query heads。PRIVATE 不破坏共享：先 PUBLIC 联合扫得 `(m_P,l_P,O_P)`，每 Lane 私有部分得 `(m_L,l_L,O_L)`，取 max 后 online-softmax merge，仍是一个全局 softmax。当前 frontier 的 N 个新 K/V 可写入 persistent cache 的同时留片上 tile 供各 Q 当场互读，无需先回 VRAM。metadata 只存一份 resident run list + 每 reader per-run delta，Tri reclaim 后仍只有一份 resident-cell list。这部分是精确优化，不改语义，风险远低于 recurrent 共享。绝不共享 hidden/residual、conv tail、Q、attention output；不要做 average Q / shared attention result。

---

## 11. Graph input 设计

### 11.1 不要把 RERoT metadata 塞进 `llama_batch` ABI

`llama_batch` 是公共接口，直接加大量 tree/view 字段会污染普通 API。

推荐 context-side experimental state：

```text
server
  ↓ llama_rerot_set_frontier_view(...)
llama_context.rerot_state
  ↓
graph_params / graph input
  ↓
llm_graph_input_attn_rerot
```

### 11.2 建议 C API

最少暴露：

```cpp
LLAMA_API bool llama_rerot_episode_begin(...);
LLAMA_API void llama_rerot_episode_end(...);

LLAMA_API bool llama_rerot_set_write_tag(
    llama_context *,
    llama_seq_id,
    const llama_rerot_write_tag *);

LLAMA_API bool llama_rerot_publish_run(...);

LLAMA_API bool llama_rerot_set_frontier_views(
    llama_context *,
    const llama_rerot_reader_view *,
    size_t n_views);
```

如果不希望 public `llama.h` 永久承诺这些 API，可先放 experimental 命名空间/宏区，但 server 必须通过明确接口调用，不能 `reinterpret_cast` context internals。

### 11.3 Graph reuse

RERoT view 每 frontier 都变，但 graph topology 未必变。

目标：

```text
span metadata / offsets / visibility = input tensor data
不是 graph topology
```

这样 graph 可以在：

```text
same n_tokens
same capacity-shaped metadata tensors
```

条件下复用。

第一 CPU prototype 可以先 `graph_reuse_disable`，验证正确后再恢复 reuse。

---

## 12. Visibility：不要继续用普通 seq membership mask

当前普通 mask 有核心逻辑：

```cpp
if (!cells.seq_has(j, seq_id)) {
    mask_drop;
}
```

RERoT active query 不能使用它作为 public visibility 判断。

正式规则：

```text
NORMAL cells:
    走普通 seq membership

RERoT PUBLIC cell, same episode:
    reader view 包含 → visible

RERoT PRIVATE cell:
    owner node / current exec lineage 明确包含 → visible
    foreign → hidden

RERoT PENDING cell:
    owner → visible
    foreign → hidden
```

不要把“所有 same-episode public cells 都可见”直接写死在 mask。

原因：PAC-DFS view 还决定：

- token 是否出现；
- 出现一次；
- virtual position 是多少。

所以最终 attention 输入应以 **view-resolved cell set** 为准。

---

## 13. Strong Frontier

### 13.1 一轮语义

设上一轮共享内存为 `M[t-1]`。

采样：

```text
Lane A 从 logits_A[t-1] 选 a_t
Lane B 从 logits_B[t-1] 选 b_t
Lane C 从 logits_C[t-1] 选 c_t
```

联合 forward：

```text
a_t, b_t, c_t 同时进入同一个 batch
```

在 layer `l`：

```text
a_t^l 可以读取 b_t^(l-1), c_t^(l-1)
b_t^l 可以读取 a_t^(l-1), c_t^(l-1)
```

因此没有同层代数环。

提交后：

```text
M[t] = M[t-1] + all public frontier writes
```

下一轮 logits 已吸收本轮所有 public 写入。

### 13.2 当前 llama.cpp 为什么具备基础

当前 KV graph 在 attention 前已经把本 ubatch 的 K/V 写入 cache。

因此只要：

- cell tag 在 `apply_ubatch()` 时就正确；
- RERoT mask 允许 peer current-frontier public cell；
- DDVR 为每个 reader 提供正确 virtual position；

即可表达 same-frontier visibility。

### 13.3 必做消融

保留：

```text
rerot_frontier_mode = strong
rerot_frontier_mode = lag1
```

Lag-1 下，本轮 peer token mask 掉，到下一 frontier 才可见。

这是科研对照，不是 fallback bug workaround。

### 13.4 一人三笔执行口号：Shared Memory / Multi-Query Execution

不要把 Lane 当 N 个 sequence；把 Lane 当同一 memory 上的 N 个 query rows。Full Attention 是多 Q 读 shared K/V；GDN 是多 q 读 shared S。GPU 应 load memory once、serve N readers：历史 K/V tile load/dequant once，每 reader 独立维护 softmax 状态；shared S tile load once，一次矩阵乘服务 N 笔，而非 N 个 GEMV；concurrent delta update once、write once。一个 frontier 即一个 N-row batch：N 笔 rows 一起做 projection/MoE/RMSNorm/LM head，shared expert 天然 batch，routed experts 按 expert regroup，模型权重只从 HBM 拖一次。decode 常为 weight-bandwidth bound，此项已在现有 3-slot RERoT gate 中部分兑现（约 2.3–2.45x，见 §33；不能归因到单个 kernel，但证明第二、三支笔不是各付一份模型成本）。core backend 禁止把 frontier 又拆回 lane-by-lane decode。

---

## 14. Hybrid recurrent：共享黑板正式方案（2026-09-04 修订）

> 本节替代旧“v1 recurrent lane-local/COW、不共享、不 merge”默认。旧方案降级为 §14.2 的 Scheme 0 对照基线。目标不再追求等价于标准 serial prefill，只要求未经重训的 LLM 能靠模糊共享工作记忆仿佛理解并发 recurrent state，且便宜、省算力、好写。

### 14.0 数学与认识论前提（后续设计都依赖这组事实）

GDN 单步是状态仿射算子。官方 recurrence（先 decay 再 rank-1 delta update）可写成：

```text
Sbar_t = alpha_t S_{t-1}，alpha_t = e^{g_t} in (0,1)
S_t = Sbar_t + beta_t k_t (v_t - k_t^T Sbar_t)^T
    = A_t S_{t-1} + B_t
A_t = e^{g_t}(I - beta_t k_t k_t^T)，B_t = beta_t k_t v_t^T
```

固定该层本 token 的 `k,v,g,beta` 后，token 即 transition `T_t=(A_t,B_t)`。多 token run 可 compose：`T2∘T1 = (A2A1, A2B1+B2)`，结合但一般不交换。结论：**recurrent state 本身不是可重排对象，但 recurrent transition 可以成为可组合对象。** KV 是“保存内容、reader 决定怎么读”；GDN 可研究为“保存 writer 产生的状态变换、reader 决定按什么顺序 compose”。但正式 v1 不走完整 transition virtualization（工程贵），而走本节共享黑板。

时间感：GDN 没有 RoPE 式显式坐标时间。Qwen 的 RoPE 只在周期性 full-attention 层；GDN 是 causal-conv + gated delta recurrence。`g_t = -A·softplus(a_t+dt_bias)` 给出耗散时间，即每 token 按内容决定“这一拍过去多少时间”。没有 token 就没有 transition，空挡 `T_empty=(I,0)`，空 1 位与空 10000 位等价；不要做 gap-decay 实验（那是新动力学语义，非训练规则）。因此 recurrent 不需要 DDVR 式 position virtualization。

记忆分工：

```text
recurrent matrix S = key->value 联想表 / disposition，不是 proposition。
  o = q^T S'，q/k 做 L2 norm；gating = adaptive memory control，delta = targeted modification。
  保存的不是句子本身，而是“若未来 hidden 以相关方向询问，则倾向返回相关 latent value”的认知倾向。
conv state（kernel=4）= “我刚才正在怎么说”：最近 3 步 latent trajectory、
  局部句法/词形连续性/笔画运动惯性。
DDVR KV = 桌面上共同看到的文字/笔记（精确、有位置、章节、来源）。
```

“一人三笔”精确化：`recurrent=一个人的脑内工作记忆`，`conv=每支笔的手部运动缓存`，`DDVR KV=外部共享白板`。Russia 例子（看完问题内心已偏向俄罗斯、只差显式写出）说明全局模糊记忆反而比三人模型自然：A 写出“俄罗斯”只是同一个人把本来就在想的答案显式化，global working memory 被推向 Russia basin，另两支笔可直接继续首都/人口。

Conv 边界污染只有 3 token：run 换 predecessor 后，在冻结 layer-input 假设下前 3 token 的 conv 输出会变，第 4 token 起 kernel=4 receptive field 已完全落回 run 内部。这是 transition 方案的工程入口，但 v1 共享黑板不需要修它，因为 conv 保持私有。

存储警告：不要存 dense `(A,B)`。每层约 2 MiB，30 层约 60 MiB/Lane；segment dense `A+B` 约两倍。单 token `A_t` 是 scalar identity + rank-1，`B_t` rank-1，可研究 compact-WY / low-rank block / segment tree，但 rank 随长度涨到约 128 即止，无无限免费压缩。共享黑板的真正收益是只做 GDN state algebra、不重跑 3B active MoE + 全网。

### 14.1 正式 v1：RBB/RSV 共享黑板

冻结算法：

```text
Persistent:
    27 x GLOBAL GDN state（推荐首版；成功后再 30/30）
     3 x local GDN state / Lane（仅 R0-R2，见 14.1.1）
    30 x local conv state / Lane（全部私有，硬规则）

For every PUBLIC frontier, for each shared GDN layer:
    S_old = global state
    every Lane: candidate_i = normal_GDN_step(S_old, lane_i q/k/v/g/beta)
                output_i uses candidate_i（首版；终版见 Parallel Delta）
    global_state = mean(candidate_0 .. candidate_N-1)
    discard candidates

PRIVATE / PENDING: 永不参与 global mean commit。
Full Attention: 不变，仍走 DDVR。
```

规则细节：只混 `recurrent_state`，绝不混 `conv_state`；无新 forward、无 MoE、无 replay、无额外 KV，只是 GPU 上几 MB state 的 axpby/mean-reduce。完全对称，无 `A->B->C` 顺序；recurrent clock 一 frontier 只走一拍，Lane 数 2→8 不加速 decay。数值上 convex interpolation 不炸 norm，风险是语义 dilution/mode collapse 而非爆炸。`N=1` 时自动退化回原生 recurrence。scratch 不必保存 30 层：graph 在哪层只需该层 `N x 2 MiB`，reduce 后复用/释放。reader-view 定义为 `V_i^l = (S_l^{global}, C_i^l, q_i^l)`，输出 `o_i = GDNRead(S^{global}, C_i, q_i)`，对称于 DDVR。PAC-DFS 顺序不塞进 recurrent；DDVR 管精确结构，RBB 管模糊认知。PRIVATE token 仍可用自己 R0-R2 私有 state、私有 KV、私有 conv 影响自己 hidden；下一枚 PUBLIC token 自然成为 bridge，causal influence 最终出去但不粗暴广播 PRIVATE representation。

#### 14.1.1 首版只共享 27 层：R0-R2 私有

```text
R0 R1 R2       lane-local
      |
A3             DDVR shared attention
      |
R4..           GLOBAL recurrent
```

理由：R0-R2 是每 token 首次遇到 DDVR shared attention 之前唯一的窗口，留私有则 Lane 保留思维惯性、PRIVATE planner 因果不污染他人、R4 起输入已是 reader-specific（自己 hidden + 私有 conv + DDVR view）。内存：global 约 54 MiB + private 约 6 MiB/Lane，6 Lane 约 90 MiB vs 旧全私有 360 MiB。成功后再进 `30/30 shared + conv 全私有`。`30 shared + 极小 self-echo`（`S_i^{read}=(1-eps)S^{global}+epsS_i^{echo}`，echo 只留 R0-R2）可能是最终甜点；recurrent+conv 全共享永不做。

风险表（替代旧“绝不 merge”禁令）：

```text
R0-R2 private + 27 shared + conv private        4/10  最推荐首版
30 shared + conv private                        6/10  值得实验
30 shared + 极小 self-echo                      5/10  可能最终甜点
recurrent + conv 全共享                         9/10  不做
完整 PAC-DFS recurrent transition virtualization 8/10 工程风险  不进 v1
```

最大风险是 mode collapse；但有四重 anti-collapse（private conv、不同 token 历史、DDVR reader view、独立 sampler），且 full attention 每 3 GDN 重注 reader-specific。首版原型可用 COW candidate → frontier mean → collapse 回单 tail 实现；另有 Recurrent Gossip 极简版（每 frontier 结束对最前 3 GDN 做 `S_i'=(1-lambda)S_i+lambda·mean(S_j)`，只动 matrix 不动 conv、低频同步）可作更便宜的先行消融。

#### 14.1.2 终版数学形态：Parallel Delta（首版 mean 有效后再做）

Mean candidate 把完整 state 平均会稀释单 write。GDN 本是 fast-weight associative memory，多笔同 frontier 应为对同一旧记忆的并行 key->value 写入：forget 只发生一次，write 可多次。

```text
gbar = mean(g_i)；Sbar = e^{gbar} S     # 脑内时间只过去一拍；平均 log-decay 而非 alpha
r_i = v_i - k_i^T Sbar                   # 每笔的新信息
K=[k_1^T;..;k_N^T]，V=[v_1^T;..;v_N^T]，D=diag((1-beta_i)/beta_i)
S' = Sbar + K^T(KK^T+D+epsI)^{-1}(V-KSbar)  # order-free block DeltaNet
o_i = q_i^T S'                           # 先 concurrent write，再各自用自己 q 读同一新 S
```

`N=1` 时退化为原始 Delta Rule，是其天然 block/generalized version（ridge associative-memory update）。无代数环（本层 q/k/v/beta/g 已由层前 hidden+local conv 算完）。成本：inverse 是 `N x N`（3 笔即 3x3），非 `128x128`，相对 35B MoE 可忽略。此时 B 同 frontier 在本 GDN 层即可感受到 A 的写入，比 strong full-attention frontier 还自然。

### 14.2 旧 lane-local/COW 降级为 Scheme 0 对照基线（非正式 v1）

保留原语义仅作消融对照：fork 继承、运行后独立、foreign 经 `R0-R2 -> Shared A3 -> R4-R6 -> Shared A7 ..` 自然渗入 recurrent，不做 foreign replay/merge；R0-R2 当时不同步被接受。实验 ladder：

```text
方案 0  lane-local COW（旧 v1，即本基线）
方案 1  R0-R2 foreign state-only ingest（旧 14.4，O(N^2)，只作消融）
方案 2  R0-R2 frozen-transition composition
方案 3  30-layer frozen-transition composition
方案 4  full replay oracle
```

首轮只实验 R0-R2 virtualization 即可回答“前三层不同步是否为质量瓶颈”；若不是，不必为理论完整复杂化 30 层。

### 14.3 过渡研究分支：transition sharing（不进 v1，但保留问题定义）

三层级：`state sharing`（同 prefix COW snapshot）→ `transition sharing`（共享 writer-contextual transition，各自按 view compose）→ `reader-specific recurrent virtual memory`（writer transition runs + compose → reader state，与 DDVR 对偶）。中间插入示例：`S_old=T_C∘T_A(S_prefix)` 无法塞 B；若存 `T_A,T_B,T_C` 则 `S_new=T_C∘T_B∘T_A(S_prefix)`。重排一般不等价，且 `A_t,B_t` 是 writer-contextual，精确重排须 rewind 重算；但 RERoT 本就不要求 serial-equivalent，可定义 writer-contextual transition + reader-specific order 新语义。量化工具：recurrent commutator `C(A,B)=||S_AB-S_BA||_F/(||S_AB||_F+eps)`，在同主题/无关/检索/数学子证明/代码模块 sibling 上测。Oracle 对照（真实 trace 切 PREFIX/A/B/C，构造两种 PAC 顺序）：Serial replay vs Lane-local vs State-only ingest vs Transition recomposition，比 30 层 relative Frobenius、barrier 前后 hidden cosine、logits KL、top-k overlap、secret uptake、accuracy、topology-change FLOPs/bandwidth，重点看 `R0-R2 error -> A3 -> R4-R6 是否收敛`：若每过 A 层误差迅速收敛则 lane-local 已近最优，若残留则 transition virtualization 值得。

### 14.4 state-only ingest（降级，O(N^2)，不进 v1）

```text
foreign public token
-> 用 reader 自己 state 完整跑一次网络
-> 禁止重复写 public KV
-> 只 commit reader recurrent state
```

接近每 foreign token 多一次完整 forward，Lane 数 N 时趋近 O(N^2)。只作后续消融，不进入正式 v1。

---

## 15. Planner XML 原子发布

### 15.1 自然语言逐 token PUBLIC

planner 前面的规模分析：

```text
PUBLIC_LIVE
```

每 frontier 发布。

### 15.2 `<ol>` 从开始到闭合都 PENDING

不能把：

```html
<ol>
<li>研究 RoPE 的
```

暴露给 foreign reader。

否则 foreign Lane 的 heading / 正文可能被模型当成当前未闭合列表的一部分。

### 15.3 tokenization 边界

不能假设 `<ol>` 是单 token。

parser 必须维护字节级状态：

```text
PUBLIC text
  ↓
发现可能的 '<'
  ↓
短暂 hold / tag PENDING
  ↓
确认形成 '<ol>'
     → 整条结构记录保持 PENDING
确认不是 '<ol>'
     → 把 hold 内容重新 publish 成 PUBLIC
```

由于 token 已经写入 KV，发布/撤销必须通过修改 `llama_kv_cells` 的 RERoT visibility metadata，不移动 K/V。

### 15.4 grammar

推荐：

```text
自由分析阶段：普通 sampler
确认 <ol> open 后：切换严格 GBNF，仅允许 flat direct <li>
看到完整 </ol>：停止 planner decode
```

不允许 nested `<ol>` / nested tags 进入 fork parser。

### 15.5 N=1

完整列表只有一个 direct `<li>`：

```text
planner_armed = false forever for this node
pending <ol> → public
current node state → TERMINAL_RUNNING
继续当前 section 正文
```

不生成 child heading。

后续模型自然写出的 `<ol>` 都只是普通内容，不再触发 fork。

---

## 16. Queue：无限 logical work，有限 physical slots

### 16.1 QUEUED child 状态

queued child（共享黑板正式路径）：

```text
有 logical node descriptor
有 title text
有 sampler/parser state
有私有 conv tail（很小，见 §14）
没有 physical server slot
没有 heading KV
没有 planner KV
没有正文 KV
没有 parked recurrent seq（不再冻结整个脑子；对照分支才保留 parked seq，见 §6/§14.2）
```

父 `<ol>` 已经公开，所以其他 Lane 知道这个工作项存在。一支暂时没拿起来的笔不需要冻结整个脑子；拿起来时用现在这个人的脑子。

### 16.2 FIFO

排序键：

```text
(enqueue_frontier, tree_path_lexicographic)
```

同一 parent siblings 按 `<li>` 顺序入队。

禁止：

- 按标题打分；
- 按模型置信度；
- 按长度预测；
- 后生成孙节点插队老 sibling。

否则 scheduler 变成隐式 judge。

### 16.3 不抢占

v1：

```text
RUNNING Lane 一直占 slot
直到自然 exit 或 fork
```

不做时间片抢占。

理由：恢复 Lane 时共享内存已经改变，需要 refresh next logits，并显著增加复杂度。

### 16.4 admission

取得 physical slot 后（共享黑板正式路径）：

```text
1. 直接接入当前最新 GLOBAL recurrent brain + 私有 conv hand-state（无 parked→exec recurrent cp）
   对照分支（R0-R2 私有/Scheme 0）：parked recurrent seq → exec seq（recurrent-only cp），移除 parked ref
2. 设置 storage_pos_next = parent fork position 后继
3. PRIVATE <think>
4. PENDING heading tokens
5. heading 完整后原子 PUBLIC
6. PRIVATE planner prompt
7. node → PLANNING/RUNNING
8. topology barrier refresh
```

queued child 启动时读取的是 **最新共享 public KV**，不是 enqueue 时 snapshot。recurrent 则接入 admission 时刻的最新 global brain（对照分支才继承 fork 时 parent state）。KV 与 recurrent 的时间语义不同，是设计的一部分。

---

## 17. Topology Barrier / Refresh

### 17.1 哪些事件需要 barrier

普通 PUBLIC token append 不需要全局 refresh，因为 frontier forward 已经生成了所有 Lane 的新 logits。

以下事件需要：

```text
完整 <ol> 从 PENDING → PUBLIC
parent freeze
new heading publication
child admission
大规模 view topology change
```

### 17.2 为什么

某些既有 Lane 的当前 next-token logits 是在“新完整列表 / 新 heading 尚不可见”时生成的。

如果直接继续采样，它的第一个 token 会漏读新拓扑。

### 17.3 v1 推荐 refresh 实现

第一版优先实现明确的 **read-only logits refresh**。

要求：

```text
重新评估各 active Lane 的 frontier query
读取最新 RERoT view
不新增 public token
不重复写 KV
不改变 recurrent state（或者提供严格 restore）
只更新 next logits
```

如果现有 graph 很难做到真正 no-state-write refresh，则 Phase 1 可以采用：

```text
保存 recurrent checkpoint
执行 refresh query
恢复 recurrent checkpoint
```

但这只是 correctness prototype，不是生产优化。

更优雅的最终路径应为专用 `LLM_GRAPH_TYPE_REROT_REFRESH` / no-commit graph。

---

## 18. Server 集成：不要把全部逻辑堆进 `server-context.cpp`

当前 `server-context.cpp` 已超过 6K 行，而且还承担：

- prompt processing；
- sampling；
- MTP；
- TriAttention pressure；
- streaming；
- parent/child completion；
- slot lifecycle。

RERoT 再全部写进去会不可维护。

建议新增：

```text
tools/server/server-rerot.h
tools/server/server-rerot.cpp
```

`server_context_impl` 只持有：

```cpp
std::unique_ptr<server_rerot_runtime> rerot;
```

### 18.1 `server_rerot_runtime` 职责

```text
create episode
root adoption
planner injection
XML parser
tree mutation
queue
slot admission
parked seq allocation
archive seq allocation
frontier view construction
write-tag assignment
exit intent
final survivor
hard abort
metrics
```

### 18.2 `server_slot` 最少新增字段

避免把 tree 全放 slot。

slot 只要：

```cpp
bool rerot_internal;
uint64_t rerot_episode_id;
uint32_t rerot_node_id;
llama_seq_id rerot_exec_seq;
```

如果可以保持普通请求 `exec_seq == slot.id`，RERoT 才用 override，最好。

### 18.3 response ownership

HTTP response 属于 episode root request，不属于最终 physical slot。

所以必须有：

```text
episode.response_task_id
```

内部 Lane：

- 不单独发送 HTTP response；
- 不单独结束 SSE；
- 不创建外部 completion id；
- 已提交的 PUBLIC reasoning 通过 root response 的现有 thinking/reasoning 字段流出；
- PENDING 与 PRIVATE bytes 永不进入客户端 delta。

final survivor 切串行后，输出仍写入同一个 root response stream。

---

## 19. Batch / Frontier Scheduler

### 19.1 普通 decode path 不改语义

RERoT OFF：继续现有 `pre_decode → batch.render → llama_decode → post_decode`。

### 19.2 RERoT frontier

当 episode active：

```text
1. 从每个 RUNNING Lane 取得上一轮 sampled token
2. parser 判断 token 写入类型：PUBLIC / PRIVATE / PENDING / EXIT_INTENT
3. 为每个 exec seq 设置 write tag；PUBLIC 才参与 recurrent global commit，PRIVATE/PENDING 永不参与
4. PAC-DFS 构造每个 reader view（KV 用；recurrent 不用 view，只用同一 global S + 私有 conv/Q）
5. 一次 batch 放入全部本轮 Lane token（一个 frontier = 一个 N-row batch，权重/KV/S 只读一次）；连续 PRIVATE control 可合并为最多 32 个 causal rows
6. llama_decode；PRIVATE microbatch 只有最后确需采样的 row 请求 logits；shared GDN 层按 §14.1 mean commit（终版 Parallel Delta 则先 concurrent write 再各自读同一新 S）
7. decode 全部成功后 commit visibility / structural events + recurrent global_state（candidate 用完即弃）
8. sample next token per Lane（各 Lane 独立 sampler）
9. queue admission / fork / retire（admission 接入最新 global S + 最新 PUBLIC KV）
10. 若 topology changed → barrier refresh
```

### 19.3 一个 frontier 必须是原子预算单元

PRIVATE microbatch 对 peer 永久不可见，可以在一个 frontier 内推进多个连续 token；但其
physical decode 和 logical run extension 仍必须 all-or-nothing。任一 slice 失败时整集
HARD_ABORT，不得只 commit 已成功的前缀。

不能：

```text
Lane A forward 完成
Lane B 因硬资源耗尽没 forward
```

开始 frontier 前先检查：

- KV 可容纳；
- recurrent physical state 可容纳；
- metadata tensor 可容纳；
- global hard token/work limit 未越界。

不足则整个 episode HARD_ABORT。

---

## 20. 全局硬资源规则

用户要求：

```text
不做 per-child budget
不做 per-depth budget
不预留 final answer token
总预算超出 → 所有全停
```

因此只维护 episode-level counters。

建议统计：

```text
generated public tokens
generated private tokens
runtime forced heading tokens
planner prompt evaluated tokens
refresh equivalent work
active KV cells
parked logical seq ids
active recurrent cells
queue descriptors
```

硬资源触发：

```text
global token/work budget exhausted
KV capacity impossible
recurrent capacity impossible
seq-id arena exhausted
virtual context overflow
host metadata allocation failure
backend allocation failure
```

统一行为：

```text
cancel RUNNING
cancel STARTING
drop QUEUED descriptors
release parked seq refs
release archive refs
do not choose survivor
do not final fence
do not tool call
do not answer
finish_reason = rerot_resource_exhausted
```

若 chat template 必须闭合，可机械输出 `</think>` 到协议层，但不执行模型 forward。

---

## 21. Lane Exit 与最终 survivor

### 21.1 非最终 Lane

当 sampler 选到 `</think>`：

```text
标记 EXIT_INTENT
不作为 foreign-visible public token
完成本 frontier forward / commit
freeze public runs
archive public KV
release exec recurrent state
release physical slot
node → RETIRED
```

### 21.2 queue 非空时绝不是 final

自然 final 条件必须同时满足：

```text
QUEUED.empty()
STARTING.empty()
没有尚未 admission 的 logical child
RUNNING.size() == 1
```

此时唯一 Lane 未来采样 `</think>` 才有资格成为 global final closer。

### 21.3 simultaneous exits

同 frontier 多 Lane 同时 `EXIT_INTENT`：

```text
按 tree path 字典序稳定排序
排序最后者视为该 frontier 的最后退出者
```

这只是 deterministic tie-break，不是 judge。

### 21.4 final acquire fence

最终 Lane 采样 `</think>` 后不要直接使用旧 logits 开始正文。

流程：

```text
commit 同 frontier 所有 public writes
处理所有 exits
确认 queue / starting 为空
构造 final survivor PAC-DFS stable view
重新评估 global </think>
    ↓
该 token 作为 acquire fence
    ↓
禁用 RERoT parallel scheduler
    ↓
从 fence logits 继续普通串行 decode
```

---

## 22. 串行尾部

v1 最省开发：

```text
RERoT episode 已无并发 writer
但底层仍继续使用 segmented/DDVR attention
```

不急着重排物理 KV。

后续优化可做一次 final coordinate freeze：

```text
K storage phase → final survivor virtual phase
```

然后切回 stock attention。

但这不是 v1 必需。

注意：即使 final K rephase，V / recurrent hidden history 仍然是并发因果，不是标准单线程 prefill 的状态。

---

## 23. TriAttention 共存策略

### 23.1 第一 correctness prototype：强制 OFF

原因：

TriAttention 会主动淘汰 PUBLIC KV，而 RERoT 的核心科学问题首先是：

```text
完整 writer-contextual shared KV 是否有效
```

如果第一版就 3/32 reclaim，失败时无法区分：

- 并发语义失败；
- DDVR 失败；
- KV 太稀疏；
- scorer 选错共享信息。

所以 Phase 1：

```text
rerot && triattention → reject / warn unsupported
```

### 23.2 第二阶段：允许 lossy shared memory

DDVR view builder 必须只枚举当前 resident cells。

TriAttention reclaim 需要理解：

- archive seq 只是保活 owner，不代表一个真实 reasoning sequence；
- active Lane public KV 可能只有 exec ref；
- shared_keep / hard_keep 不能把 archive seq 误当作 N 个真实读者；
- PUBLIC RERoT cell 的价值可能与普通 sequence position 不同。

正式接入前必须做独立 A/B：

```text
RERoT FullKV
RERoT Tri 3/32
```

不要在 RERoT 内静默提升 Tri residency。

---

## 24. MTP / speculative decoding

### 24.1 v1 correctness：关闭

RERoT frontier 已经改变：

- visibility；
- virtual position；
- next logits validity；
- recurrent state lineage。

现有 MTP checkpoint / rollback 默认基于普通 sequence semantics。

所以 Phase 1：

```text
rerot episode active → n_draft_max = 0
```

### 24.2 后续支持

必须确保 draft checkpoint 包含：

```text
reader view epoch
publish epoch
recurrent state checkpoint
DDVR layout version
```

如果 draft 期间其他 Lane commit public token，旧 draft 的 target logits 语义已经改变。

因此可能需要：

```text
frontier-local speculative only
```

或共享 memory epoch mismatch 直接 reject 全 draft。

不要第一版一起解决。

---

## 25. Context shift / prompt cache / RAM swap

### 25.1 Phase 1 全禁

RERoT episode active 时：

```text
ctx_shift = off
prompt cache save = off
RAM demotion = off for participating lane state
manual slot restore = unsupported
```

原因是 tree/run/archive/parked seq metadata 还没有持久化格式。

### 25.2 后续如果支持

状态文件至少要保存：

```text
RERoT version
episode tree
run ids
visibility
publish epoch
storage positions
archive ownership
parked recurrent mappings
finalized/not-finalized
```

不能把 RERoT sparse/shared state 当普通 FullKV slot restore。

---

## 26. Tool calling

并发思考阶段：

```text
tool-call grammar disabled
```

只有 final acquire fence 后：

```text
恢复普通 tool call /正文协议
```

工具结果返回后：

```text
开启一个新的 RERoT episode
```

不要复活旧环。

---

## 27. 实施顺序：严格按这个顺序，不要跳

### Stage 0：实验开关与隔离

目标：RERoT 代码存在但完全不改变普通推理。

改：

```text
common/common.h
common/arg.cpp
include/llama.h
src/llama-cparams.h
src/llama-context.cpp
tools/server/server-common.cpp
```

增加：

```text
--rerot
--rerot-frontier strong|lag1
```

第一版 CLI gate：

```text
requires kv_unified
requires text-only
reject TriAttention
disable speculative
disable context shift
```

验收：

- RERoT OFF build；
- 现有 focused tests 不变；
- 同 seed 普通 greedy smoke 一致。

预计实质代码：**150–250 行**。

### Stage 1：Core metadata + view engine，完全不跑模型

新增：

```text
src/llama-rerot.h
src/llama-rerot.cpp
tests/test-rerot-view.cpp
```

实现：

- episode/node/run ids；
- PAC-DFS；
- virtual dense positions；
- no-overlap invariant；
- queue-independent render；
- publish epoch；
- span flattening。

测试随机树：

```text
每个 reader 自己路径 child last
off-path stable
每个 token exactly once
virtual positions = [0,L)
```

预计：**700–1,000 行（含测试）**。

### Stage 2：KV RERoT metadata

改：

```text
src/llama-kv-cells.h
src/llama-kv-cache.h
src/llama-kv-cache.cpp
tests/test-kv-cells.cpp
```

实现：

- per-cell episode/node/run/visibility；
- write tag；
- publish pending run；
- metadata compaction preservation；
- archive ref helper；
- physical lookup by run id；
- clear/restore invariants。

必须重跑现有 TriAttention cell tests。

预计：**500–800 行（含测试）**。

### Stage 3：Hybrid recurrent（共享黑板 + 对照 COW）

改：

```text
src/llama-memory.h
src/llama-memory-hybrid.h/.cpp
src/llama-memory-recurrent.h/.cpp
src/llama-model.cpp
src/llama-rerot-recurrent.*（新，global S + mean commit / block-delta）
tests/test-rerot-recurrent.cpp
```

实现（两轨）：

- 正式轨：episode-global GDN state、frontier mean commit、PRIVATE/PENDING 永不参与、`N=1` 退化、conv 全私有、queued child 无 parked seq（接入最新 brain）。
- 对照轨：recurrent-only / attention-only seq cp/rm、logical seq capacity、parked seq allocator、parent → many parked children 共用 tail、admission 后 COW（Scheme 0 / R0-R2 私有消融用）。

必须有测试：

```text
对照：parent state S；fork A/B/C；A update 后 B/C 不变
共享：同 S 出发 mean 对称、无顺序；PRIVATE 口令不泄漏；N=1 退化
```

预计：**600–950 行（含测试）**（共享黑板新增约 150–250 行）。

### Stage 4：DDVR 数学 reference

新增：

```text
tests/test-rerot-ddvr.cpp
```

先只做 F32 array / ggml CPU reference。

覆盖：

- 1D RoPE；
- Qwen3.5 text IMRoPE `(p,p,p,0)`；
- partial/interleaved rotary dim；
- 多 span；
- private gap；
- overlapping storage positions；
- unique virtual positions；
- global softmax。

预计：**400–650 行**。

### Stage 5：CPU RERoT attention op

改：

```text
ggml/include/ggml.h
ggml/src/ggml.c
ggml/src/ggml-cpu/*
src/llama-graph.h/.cpp
src/models/qwen35moe.cpp
必要时 src/models/qwen35.cpp
```

实现 correctness-only segmented DDVR attention。

要求：

- 普通 attention op 不变；
- RERoT graph 单独选择新 op；
- support GQA；
- support Qwen3.5 IMRoPE text；
- one global softmax；
- current frontier cell 可配置 strong/lag1。

预计：**900–1,400 行（含 op tests）**。

### Stage 6：Server RERoT runtime

新增：

```text
tools/server/server-rerot.h
tools/server/server-rerot.cpp
```

少量改：

```text
tools/server/server-context.cpp
tools/server/server-task.h/.cpp
tools/server/CMakeLists.txt
```

实现：

- episode；
- node tree；
- root adoption；
- private planner injection；
- byte-level XML pending parser；
- grammar arm/disarm；
- N=1 base case；
- recursive fork；
- queue；
- parked seq；
- slot admission；
- heading injection；
- frontier scheduler；
- exit intents；
- final survivor；
- hard abort；
- response owner。

这是最大的一块。

预计：**1,400–2,100 行（不含 tests）**。

### Stage 7：Topology refresh / final fence

改：

```text
src/llama-context.cpp
src/llama-graph.*
tools/server/server-rerot.cpp
```

实现：

- topology epoch；
- active Lane next-logits invalidation；
- read-only refresh graph；
- final `</think>` acquire fence；
- serial tail transition。

预计：**350–600 行（含测试）**。

### Stage 8：End-to-end CPU correctness

新增测试/脚本：

```text
tools/server/tests/unit/test_rerot_*.py
tests/test-rerot-*.cpp
```

覆盖：

1. N=1 不 fork。
2. N=2 两 Lane 实时 uptake。
3. N>physical slots，FIFO queue。
4. queued child admission 看到最新 public memory。
5. nested fork。
6. parent private planner foreign 不可 lexical read。
7. pending XML foreign 不可见。
8. title atomic publish。
9. same-frontier strong uptake。
10. lag1 晚一轮。
11. simultaneous exit deterministic。
12. queue 非空不 final。
13. final fence 看到倒数第二 Lane 最后一笔。
14. hard resource abort 不输出正文。

预计：**900–1,400 行测试**。

### Stage 9：Vulkan fused DDVR FA

改：

```text
ggml/src/ggml-vulkan/ggml-vulkan.cpp
ggml/src/ggml-vulkan/vulkan-shaders/flash_attn_base.glsl
ggml/src/ggml-vulkan/vulkan-shaders/flash_attn*.comp
ggml/src/ggml-vulkan/vulkan-shaders/CMakeLists.txt
必要的 shader generation / pipeline state
```

实现：

- RERoT-specific FA pipeline；
- reader-relative RoPE in shader；
- metadata buffer；
- Turbo K/V；
- GQA；
- split-K；
- online global softmax；
- strong frontier mask；
- CPU vs Vulkan numerical parity。

预计：**1,200–1,900 行（含 Vulkan tests）**。

### Stage 10：TriAttention / MTP compatibility

TriAttention：

- RERoT run metadata survive compaction；
- archive seq 不污染 scoring 语义；
- sparse resident runs view 正确；
- FullKV vs 3/32 A/B。

MTP：

- memory epoch-aware draft invalidation；
- checkpoint 包含 view epoch；
- frontier 内 draft 规则。

这一阶段不应与第一 correctness milestone 混在一起。

预计：**800–1,500 行**。

---

## 28. 推荐文件级改动表

| 文件 | 预计实质 LOC | 作用 |
|---|---:|---|
| `src/llama-rerot.h` | 180–260 | core types / view API |
| `src/llama-rerot.cpp` | 450–700 | PAC-DFS / span resolve / episode core |
| `src/llama-kv-cells.h` | 120–220 | RERoT physical metadata |
| `src/llama-kv-cache.cpp/.h` | 250–450 | tag/publish/archive/view resolve |
| `src/llama-memory*.{h,cpp}` | 180–320 | recurrent-only / attn-only seq ops（对照轨） |
| `src/llama-rerot-recurrent.*` | 150–350 | RBB global S + mean commit / block-delta + conv 私有 |
| `src/llama-model.cpp` | 20–60 | logical seq capacity wiring |
| `src/llama-graph.*` | 350–600 | RERoT input + graph selection |
| `src/models/qwen35moe.cpp` | 80–160 | raw/pre-RoPE Q / IMRoPE RERoT path |
| `ggml/include/ggml.h`, `ggml/src/ggml.c` | 100–180 | new experimental op |
| `ggml/src/ggml-cpu/*` | 300–500 | CPU reference op |
| `tools/server/server-rerot.*` | 1,400–2,100 | runtime / parser / tree / queue / frontier |
| `tools/server/server-context.cpp` | 200–400 | hooks only，避免继续膨胀 |
| common/server args | 100–180 | flags / request params |
| Vulkan backend/shader | 1,000–1,700 | fused DDVR + multi-reader shared scan + RBB mean-reduce |
| C++ tests | 900–1,400 | core/math/memory |
| server Python tests | 500–900 | E2E protocol |

---

## 29. 代码量结论

### 29.1 能跑的 CPU 科研原型

范围：

```text
recursive fork
queue
hybrid recurrent（共享黑板正式 + COW 对照）
PUBLIC/PRIVATE/PENDING（含 PRIVATE 不进 recurrent commit）
PAC-DFS
DDVR CPU attention + RBB CPU mean commit
strong/lag1 frontier + multi-query batch
final fence
no Tri
no MTP
```

预计：

```text
核心功能代码：约 4,000–5,400 行
测试：        约 1,400–2,200 行
合计：        约 5,400–7,600 行
```

### 29.2 可用于当前 Vulkan/Ornith 的正式 v1

在 CPU 原型基础上增加：

```text
Vulkan fused DDVR FA + multi-reader shared scan + RBB mean-reduce
Turbo K/V
frontier-wide weight batching 性能路径
完整 server E2E
metrics（含 RBB/commit/mode-collapse 观测）
```

预计总计：

```text
约 7,400–9,800 行新增/实质修改代码（包含测试）
```

### 29.3 再把 TriAttention + MTP 正式兼容进去

预计最终完整项目增量：

```text
约 8,400–11,300 行（包含测试）
```

这是比“只改一个 RoPE kernel”大的项目，因为真正复杂的部分不是旋转 Q，而是：

```text
递归 tree
queue / global recurrent commit lineage
visibility transaction
reader-specific view（KV 用；recurrent 用共享黑板）
frontier synchronization（N-row batch）
server response ownership
final linearization
```

如果实现过程中超过约 12.5K 行，优先怀疑架构过度耦合或在 `server-context.cpp` 重复造框架。

---

## 30. 哪些地方最容易把代码量炸掉

### 30.1 误把每个 Lane 做成完整 server request

会重复：

- task；
- SSE；
- chat parser；
- HTTP result；
- prompt cache；
- sampling lifecycle。

正确：一个 episode 一个外部 response，多 Lane 是内部 execution entities。

### 30.2 为 queued child 做 host recurrent snapshot / 无脑保留全量 parked seq

没有必要。共享黑板下 queued child 只需 descriptor + conv tail，admission 接当前 brain；`seq_cp()` shared-tail COW + parked seq 只为 Scheme 0 / R0-R2 私有对照保留。更不要每个 child 复制几十 MB recurrent tensor 到 host。

### 30.3 重复维护 physical cell index

TriAttention compaction 后一定出错。

只在 `llama_kv_cells` 存 metadata，server 只存 run id。

### 30.4 先写 Vulkan 再验证数学

最容易花一千行 shader 最后发现 IMRoPE offset 定义错。

必须先通过 materialized baseline。

### 30.5 第一版同时支持 Tri + MTP + RAM swap

会使 debug 维度爆炸。

先证明并发 shared-KV 本身有效。

---

## 31. Metrics

建议增加：

```text
rerot_episode_total
rerot_episode_completed_total
rerot_hard_abort_total{reason=...}

rerot_fork_total
rerot_nodes_created_total
rerot_nodes_started_total
rerot_nodes_never_started_total

rerot_queue_length
rerot_queue_max
rerot_active_lanes
rerot_peak_active_lanes

rerot_frontier_total
rerot_topology_barrier_total
rerot_refresh_total

rerot_public_tokens
rerot_private_tokens
rerot_pending_tokens

rerot_ddvr_spans
rerot_ddvr_visible_cells

rerot_parked_seq_used
rerot_archive_cells

rerot_rbb_commits
rerot_rbb_private_excluded
rerot_rbb_shared_layers
rerot_mode_collapse_score

rerot_final_survivor_depth
rerot_final_survivor_tokens
```

科研额外日志：

```text
node tree path
enqueue frontier
admission frontier
wait frontiers
fork parent
exit frontier
public tokens visible at admission
```

---

## 32. 正确性测试矩阵

### 32.1 PAC-DFS

- 单层 2/3/4 children；
- 3 层 nested tree；
- reader 在每个不同 leaf；
- off-path subtree writer 改变时 view 顺序不洗牌；
- 每 token 一次；
- virtual position 无重复。

### 32.2 DDVR

- equal-length spans；
- unequal spans；
- storage position overlap；
- private gaps；
- sparse physical indices；
- compaction before/after；
- Qwen3.5 IMRoPE text；
- GQA；
- Turbo types Vulkan parity。

### 32.3 recurrent（共享黑板 + 对照分支）

共享黑板正式门：

- 同一 S 出发的 N candidates mean 后对称、无顺序、`N=1` 退化原生；
- PRIVATE/PENDING 不参与 commit（合成 PRIVATE 口令不得经 recurrent 泄漏给 foreign 下一轮）；
- R0-R2 私有分支隔离，R4 起已带 foreign 信息；
- Parallel Delta（如实现）`N=1` 与原生 delta 一致；
- mode-collapse  smoke：多 Lane 长期跑不塌为同一输出（conv/Q/sampler/DDVR 保持分工感）。

对照分支（Scheme 0）：

- parent → 2 parked children；
- parent → 200 parked logical children（若 arena 容许）；
- admitted child COW；
- sibling state isolation；
- exec seq reuse 不污染 parked state；
- recursive fork 后 lineage 正确。

### 32.4 visibility

合成口令：

```text
PUBLIC:  R7K2
PRIVATE: P9M4
PENDING: X3Q8
```

foreign Lane：

- 下一轮必须能复述 PUBLIC；
- 不应直接 lexical 读取 PRIVATE；
- commit 前看不到 PENDING；
- commit 后能看到完整 PENDING record。

### 32.5 queue

2 slots，parent 产生 8 children：

- 8 个 descriptor 全创建；
- 只有能跑的 admission；
- 其余 FIFO；
- 后启动 child 看到等待期间的新 PUBLIC；
- 所有 child 完成前 subtree 不 collapse。

### 32.6 final fence

构造：

```text
A 最后一轮写 SECRET42
B 同 frontier 产生 exit intent
```

最终 B 的 fence logits 必须能使用 `SECRET42`。

---

## 33. 性能测试

CPU correctness 通过后再关心性能。

需要分开记录：

```text
tokens/s per frontier
total wall time
DDVR attention time
view build time
topology refresh time
queue wait
Vulkan shader time
KV bytes
recurrent bytes
metadata bytes
```

对照：

```text
Serial CoT
Independent parallel samples
Skeleton-of-Thought style independent expansion
RERoT lag1
RERoT strong
```

必须按等 token 或等 FLOP 预算比较，不能只比 wall-clock。

`usage.completion_tokens` 和 `timings.predicted_n` 只统计模型采样 token；RERoT 的固定
PRIVATE 控制 token 和 PENDING 记录 token 不在其中，因此不得再把
`predicted_n / predicted_ms` 称为 RERoT 总吞吐。进程级总吞吐统一按：

```text
RERoT request-wide aggregate =
    Δrerot_completed_model_tokens / Δrerot_completed_episode_seconds

RERoT multi-Lane aggregate =
    Δrerot_parallel_model_tokens / Δrerot_parallel_seconds

single-Lane baseline =
    timings.predicted_n / timings.predicted_ms * 1000
```

其中 `rerot_completed_model_tokens` 必须等于同一已完成 episode 的
`PUBLIC + PRIVATE + PENDING` committed token；失败或取消的 episode 不得混入分子或分母。
仓库内 `scripts/rerot-throughput-gate.py` 使用同一 prompt、seed、采样参数和非约束性输出预算，
同时校验精确 token 核算、无 HARD_ABORT、单一 final fence，并要求 request-wide 与
multi-Lane 两个口径都严格高于 single-Lane baseline。示例：

```bash
LLAMA_API_KEY=... scripts/rerot-throughput-gate.py \
  --request scripts/rerot-throughput-request.json \
  --output /tmp/rerot-throughput-gate.json
```

2026-09-04 在当前生产同构配置（Ornith-1.5-35B、RX 6800、Vulkan、Turbo4/Turbo2、
TriAttention、strong frontier、3 fitted slots）运行同 prompt/seed/采样参数 gate 两次：

```text
single-Lane baseline                      108.429 tok/s

run 1:
  completed model tokens                    3,996
  request-wide aggregate                  237.061 tok/s
  multi-Lane aggregate                    252.476 tok/s

run 2:
  completed model tokens                    4,568
  request-wide aggregate                  245.028 tok/s
  multi-Lane aggregate                    266.110 tok/s
```

两次均 HTTP 200、`finish_reason=stop`、`HARD_ABORT=0`、`final_fence=1`，且
`PUBLIC + PRIVATE + PENDING == completed_model_tokens`。本结果关闭“把 sampled-only
44.343 tok/s 误报为总吞吐”的验收缺陷；它只证明总吞吐门通过，不替代 Appendix A 的
质量、压力、持久化和长稳 release gates。

同日部署记录：

```text
code commit:                  a0ad22005
llama-server launcher SHA256: ac626f7726386977d28e6e9316c7444e16298d45b3854210ddb1ff9a6903c740
libllama-server-impl SHA256:  c64cfa5cd3f9710ffe8708ac4ed043143d3bd6a22116e6c375e6358effc0df51
libllama 0.0.10738 SHA256:    2d9e632feeee153fae536f0f0cdcecf1fbf6373a9968913393a96d04da005ad2
libggml-vulkan SHA256:        04a96f1e985a1f0a6f8d07f231649cfa7d8ba14772fd72de4cd4d258c1b171d6
calibration SHA256:           e95dae507d1f4a64e29be160c5281f8a4308a3332dc9c9176e1a3a0af32e50e2

deployed gate:
  single-Lane baseline        108.437 tok/s
  request-wide aggregate      237.237 tok/s
  multi-Lane aggregate        252.668 tok/s
```

生产进程已确认实际 mmap `libllama.so.0.0.10738` 和同批 Vulkan/server libraries；
gate 后 service 保持 `active`，`/health` 返回 `{\"status\":\"ok\"}`。

2026-09-04 公开 API 曾返回 HTTP 502。llama-server 当时始终 active，且故障请求没有进入
server journal；根因是 FRP `llama-api` 仍连接已经没有 listener 的
`127.0.0.1:80`，而生产 server 监听 `0.0.0.0:8080`。固定公开链路为：

```text
Caddy https://ai.kw92.cyou:10443
    -> VPS 127.0.0.1:10080
    -> FRP llama-api
    -> workstation 127.0.0.1:8080
```

`/etc/frp/frpc.toml` 已将 `llama-api.localPort` 从 `80` 改为 `8080` 并重启 frpc。
修复后公开 `/health` 返回 `{\"status\":\"ok\"}`，公开
`/v1/chat/completions` HTTP 200、`finish_reason=stop`、内容 `PROXY_OK`；重启后的
FRP journal 无新的 `connect to local service ... connection refused`。

---

## 34. Go / No-Go 门

### Gate A：数学

DDVR materialized baseline vs Q-side/span reference 数值通过。

不过：不进入模型。

### Gate B：共享 uptake

合成 public secret 能按预期被 peer 吸收。

不过：不做 Vulkan。

### Gate C：recurrent 共享正确性

共享黑板对称 commit + PRIVATE 不参与 + `N=1` 退化通过；对照分支 parked COW sibling state 不串。

不过：不做 recursive server。

### Gate D：CPU E2E

递归、queue、pending XML、final fence 全通过。

不过：不做 fused Vulkan。

### Gate E：科研价值

至少一个任务族在等计算预算下优于独立并行 baseline。

如果这里始终失败：

**暂停生产优化。**

不要因为已经写了 kernel 就继续投入。

### Gate F：Vulkan

CPU vs Vulkan parity + 真实 Ornith smoke。

### Gate G：Tri/MTP

最后再做。

---

## 35. 推荐的开发 commit 拆分

不要一口气一个 8K LOC commit。

建议：

```text
1. feat(rerot): add disabled experimental configuration
2. feat(rerot): add tree and PAC-DFS view model
3. feat(rerot): attach visibility metadata to KV cells
4. feat(rerot): add recurrent-only parked sequence COW (对照分支) + shared-RBB global state scaffolding
5. test(rerot): validate DDVR position equivalence
6. feat(rerot): add CPU segmented DDVR attention + CPU RBB mean commit
7. feat(server): add recursive RERoT runtime and queue
8. feat(rerot): add topology refresh and final fence
9. test(server): add RERoT end-to-end matrix (KV uptake + recurrent non-leak + collapse smoke)
10. feat(vulkan): add fused RERoT DDVR flash attention (multi-reader shared scan) + RBB mean-reduce
11. feat(rerot): integrate TriAttention sparse shared memory
12. feat(rerot): integrate MTP epoch-aware drafts
13. (optional, after mean proven) feat(rerot): parallel block-delta + merged-state read
```

每个 commit 都要保证 RERoT OFF 回归通过。

---

## 36. 第一轮真正开工时应该做什么

不要先碰 server。

第一轮只做下面四件事：

### Step 1

新增 `src/llama-rerot.{h,cpp}`：

- tree；
- runs；
- PAC-DFS；
- virtual positions。

### Step 2

新增 `tests/test-rerot-view.cpp`。

手工树：

```text
ROOT
├─ A
├─ B
│  ├─ B1
│  ├─ B2
│  └─ B3
└─ C
```

验证 B2 view。

### Step 3

新增 DDVR pure math test。

先证明：

```text
materialize K phase
==
span-specific Q phase
```

### Step 4

单独验证 Qwen3.5 text IMRoPE：

```text
(p,p,p,0)
```

三个 text coordinate 同步 shift。

四步全部通过以后，才开始改 KV metadata。

---

## 37. 第二轮才碰 memory

目标只验证（两轨）：

```text
对照轨：park child 不复制 recurrent tensor；admit 后 COW；archive 后 parent exec seq 可复用
共享轨：global S mean 对称 commit；PRIVATE 不参与；N=1 退化；admission 接最新 brain
```

先不做模型输出质量。

测试通过后再进入 server runtime。

---

## 38. 第三轮才碰 server

先做假模型 / deterministic token source 测 scheduler：

```text
root 输出 3 child
child 1 → exit
child 2 → fork 2 grandchildren
child 3 → queue
```

验证 tree/queue/final survivor。

不要一上来依赖模型真的稳定生成 XML，否则 scheduler bug 与模型行为 bug 混在一起。

---

## 39. 第四轮模型 E2E

先使用：

```text
temperature = 0
MTP = off
Tri = off
RERoT strong
2–3 physical slots
短 context
```

观察：

- 是否自然拆分；
- sibling 是否引用彼此标题；
- foreign 新发现是否被吸收；
- 是否 mode collapse；
- last survivor 是否真的综合得更好。

---

## 40. 最后才优化

优化优先级：

```text
1. frontier-wide weight batching（N-row batch，禁拆回 lane-by-lane）
2. multi-reader DDVR FA：K/V load + Turbo dequant once，每 reader 独立 (m,l,O)
3. global recurrent multi-query：S load/update once（mean-reduce / block-delta）
4. graph reuse
5. view metadata packing（单 resident list + per-reader delta）
6. topology barrier cost
7. archive lookup cost
8. Tri coexistence
9. MTP
```

不要先优化 XML parser、tree DFS 这类占比极低的 CPU 代码。

---

## 41. 最终架构图

```text
┌──────────────────────────── server ────────────────────────────┐
│                                                               │
│  HTTP root request                                            │
│        │                                                      │
│        ▼                                                      │
│  server_rerot_runtime                                         │
│        │                                                      │
│        ├─ thought tree / PAC-DFS                              │
│        ├─ planner XML parser                                  │
│        ├─ FIFO queue                                          │
│        ├─ physical slot admission                             │
│        ├─ parked seq pool                                     │
│        ├─ archive seq                                         │
│        └─ frontier / final fence                              │
│                                                               │
└───────────────────────────┬───────────────────────────────────┘
                            │
                            ▼
┌────────────────────── llama_context ──────────────────────────┐
│                                                              │
│  rerot_state                                                 │
│     ├─ write tags                                            │
│     ├─ reader views                                          │
│     └─ topology epoch                                        │
│                                                              │
│  hybrid memory                                               │
│     ├─ Full-Attn KV ─────────────── shared precise whiteboard│
│     │     └─ llama_kv_cells RERoT metadata                   │
│     │                                                        │
│     ├─ GDN recurrent S ───────────── episode-global shared   │
│     │                                blackboard (RBB/RSV)    │
│     └─ conv state ────────────────── lane-local (never share)│
│                                                              │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌──────────────────────── graph ────────────────────────────────┐
│                                                              │
│  shared GDN layers: same S_old → N candidates → mean commit  │
│  (final: one-decay + block-delta + all-q read merged S)      │
│  private conv/Q/hidden per Lane                              │
│                                                              │
│  full-attention layers:                                      │
│     raw/local Q                                               │
│        │                                                     │
│        ├─ reader-specific DDVR phase                         │
│        ▼                                                     │
│     shared physical K/V, one load serves N readers           │
│        │                                                     │
│        └─ one global online softmax per reader               │
│                                                              │
└───────────────────────────┬──────────────────────────────────┘
                            │
                            ▼
┌────────────────────── Vulkan / CPU ───────────────────────────┐
│                                                              │
│  ordinary FA path                  RERoT FA path              │
│  完全保持现状                       DDVR + visibility          │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 42. 最终“不许做”的清单

后续开发过程中发现自己准备做以下任何一条，先停下来重新看本文：

- [ ] 给每个 Lane 预留固定 KV 长度。
- [ ] 让不同 public token 使用相同 virtual position。
- [ ] 为了 DDVR 每次搬整个 K cache。
- [ ] 给每个 reader 复制一份完整 KV。
- [ ] 每个 span 独立 softmax。
- [ ] 在 RBB 对称 commit / Parallel Delta 之外把 recurrent state 直接平均/merge（如串行 A→B→C 写、跨 Lane 加和不归一、混 conv state、让 PRIVATE/PENDING 参与 commit）。
- [ ] 每个 foreign token 都 replay 一遍 recurrent，作为 v1 默认。
- [ ] queued child 保存几十 MB host recurrent snapshot（共享路径只需 conv tail；COW parked seq 只为对照分支保留）。
- [ ] server 维护一份独立 physical cell index registry。
- [ ] 把所有 RERoT 逻辑继续塞进 `server-context.cpp`。
- [ ] 用普通 `seq_has(reader)` 作为 RERoT PUBLIC visibility。
- [ ] queue 非空时把最后 RUNNING Lane 当 final。
- [ ] planner 输出太多 child 时静默截断。
- [ ] global resource 不够时只杀部分 Lane 继续答。
- [ ] RERoT 第一版同时打开 Tri + MTP + RAM restore。
- [ ] CPU 数学基准没过就写 Vulkan shader。
- [ ] 最后再加一个 merge/judge agent。
- [ ] 让模型收到“你是 Lane 2.3.1”这种机械角色提示。
- [ ] 把 private 控制信息宣传成安全隔离。
- [ ] RERoT OFF 时改变任何已有 atomic/Tri 行为。

---

## 43. Definition of Done：RERoT v1

只有下面全部成立，才可以称“RERoT v1 完成”：

1. RERoT OFF 自动化零回归。
2. PAC-DFS randomized tests 全通过。
3. DDVR materialized 数值基准通过。
4. Qwen3.5 text IMRoPE position translation 测试通过。
5. `llama_kv_cells` metadata 在 compact 后完全保持。
6. recurrent 测试通过（共享对称 commit + PRIVATE 不参与 + N=1 退化；对照 parked COW 隔离）。
7. active/public/private/pending visibility 合成测试通过。
8. strong frontier uptake 通过。
9. lag1 对照按预期晚一轮。
10. N=1 不 fork 且不递归 planner。
11. recursive N>1 tree 正确。
12. child > physical slots 时全部 descriptor 建立并 FIFO 排队。
13. queued child admission 读取最新 public memory。
14. queue 非空绝不提前 final。
15. simultaneous exits deterministic。
16. final acquire fence 能看到全部最终 public writes。
17. final survivor 无 merge 直接进入串行正文/tool call。
18. global hard abort 不生成伪 final answer。
19. CPU E2E 在真实 Ornith 上可稳定完成。
20. 至少一个任务族在等计算预算下显示研究价值后，才进入 Vulkan productionization。
21. Vulkan DDVR 与 CPU reference 数值一致。
22. Vulkan 真实 Ornith 不 OOM、不死锁、stream 不重复。

TriAttention 和 MTP compatibility 可以作为 v1.1 / production integration gate，不阻止“RERoT 核心科研 v1”定义，但不能在生产部署时隐瞒未兼容状态。

---

## 44. 最短执行摘要

如果只想按 checklist 开工：

```text
[ ] 加 rerot disabled flag，OFF 零回归
[ ] 写 llama-rerot tree + PAC-DFS tests
[ ] 写 DDVR pure math + Qwen3.5 IMRoPE tests
[ ] 扩 llama_kv_cells metadata
[ ] 加 RBB 共享黑板 + recurrent-only parking COW（对照）
[ ] 加 archive seq freeze
[ ] 写 CPU DDVR attention op + CPU RBB mean commit
[ ] 写 server-rerot runtime
[ ] 加 XML pending atomic parser
[ ] 加 FIFO queued child admission（接入最新 brain，无 parked seq）
[ ] 加 strong frontier + N-row batch
[ ] 加 topology refresh
[ ] 加 final acquire fence
[ ] 跑 CPU Ornith E2E（含 PRIVATE 不泄漏 + collapse smoke）
[ ] 和 independent parallel baseline 做科研 A/B
[ ] 有收益后写 Vulkan fused kernel（含 multi-reader shared scan）
[ ] mean 有效后再做 Parallel Delta（可选）
[ ] 最后接 TriAttention
[ ] 最后接 MTP
```

预计完整 CPU 科研原型约 **5.4K–7.6K 行（含测试）**；当前 Vulkan/Ornith 可用正式 v1 约 **7.4K–9.8K 行**；再完成 TriAttention + MTP 正式兼容后，总增量约 **8.4K–11.3K 行**。

---

# 附录 A：完整兼容现有机制的强制交付契约

> 本附录是对正文中“为了先验证科研核心而临时关闭部分机制”的**最终产品约束补丁**。
>
> 正文中类似“Phase 1 关闭 TriAttention / MTP / RAM restore / context shift”“production integration 可放到 v1.1”等描述，仍然适用于**开发顺序和故障隔离**，但**不再代表最终交付允许缺失这些能力**。
>
> 最终可部署版本的定义改为：**RERoT 必须完整兼容本 fork 当前已经存在并可正常工作的机制。不能通过要求用户关闭既有特性来宣称 RERoT 完成。**

## A.1 兼容性的两个层级

“完整兼容”必须拆成两个不同但同时强制的契约。

### A.1.1 RERoT OFF：绝对零回归

当：

```text
rerot = false
```

必须保持当前项目已有行为：

```text
atomic unified KV
TriAttention fill-first / drain / sticky maintenance / fallback
TurboQuant K/V
MTP / speculative decoding
hybrid recurrent memory
RAM prompt cache / slot save / slot restore
checkpoint / rollback
context shift / cache reuse
idle demotion / active preemption
shared-prefix / n_cmpl parent-child
streaming / SSE
tool calling / grammar / JSON schema
LoRA / aLoRA
multimodal
embedding / rerank
backend sampling / raw logits / n_probs
auto-fit / memory reserve
CPU / Vulkan / CUDA / Metal 等已有 backend 行为
```

RERoT OFF 时不得：

- 创建 RERoT tree/runtime；
- 分配 RERoT span table；
- 修改普通 KQ mask；
- 修改普通 RoPE；
- 改变 KV cell 生命周期；
- 改变 recurrent state 生命周期；
- 改变 batch split；
- 改变 sampler seed 或 penalty history；
- 改变 speculative acceptance；
- 改变 TriAttention reclaim 顺序；
- 改变 server slot scheduling；
- 增加常驻 GPU scratch；
- 引入可测吞吐回退。

这部分应继续沿用当前项目“Tri OFF 零回归”的工程标准：**新功能关闭时就是不存在。**

### A.1.2 RERoT ON：组合兼容

当：

```text
rerot = true
```

不能把下列做法当作最终实现：

```text
rerot && triattention  → reject
rerot && mtp           → force draft=0 forever
rerot && ram_restore   → unsupported
rerot && ctx_shift     → unsupported
rerot && turbo_kv      → force F16
rerot && flash_attn    → force non-FA forever
rerot && n_cmpl > 1    → reject
rerot && lora          → reject
rerot && multimodal    → reject
```

上述限制只允许作为中间开发 gate。

正式交付必须对所有当前可组合机制给出：

1. 明确语义；
2. 明确 ownership；
3. 明确 epoch / invalidation 规则；
4. 明确 save/restore 格式；
5. 明确压力与 fallback 顺序；
6. 自动化回归；
7. 至少一次真实 Ornith + Vulkan 端到端验证。

---

## A.2 总体原则：RERoT 是一层 execution semantics，不是第二套推理栈

为了兼容现有全部机制，最重要的架构原则是：

```text
不要复制现有能力；
不要绕过现有能力；
只给现有能力增加足够表达 RERoT 的 metadata / view / epoch。
```

最终结构应保持：

```text
server scheduler
      │
      ├── ordinary request runtime
      │
      └── RERoT episode runtime
                │
                ▼
        same llama_context
                │
        ┌───────┴────────┐
        ▼                ▼
 shared attention KV   recurrent state
        │                │
 same llama_kv_cells   same recurrent memory
        │                │
        └───────┬────────┘
                ▼
          same graph/backend
                │
      RERoT-specific reader view
```

因此：

- `llama_kv_cells` 仍是唯一 physical KV metadata owner；
- recurrent memory 仍由现有 recurrent memory implementation 管理；
- TriAttention 仍通过 `reclaim_kv()` 工作；
- MTP 仍走现有 speculative infrastructure；
- RAM state 仍走现有 state serialization 入口；
- server 仍使用现有 slot/batch loop；
- backend 仍走 ggml op / backend dispatch；
- RERoT 不应该建立 parallel llama_context per Lane。

新增的核心东西只有：

```text
RERoT logical tree
visibility metadata
publish/view epochs
reader-specific span table
DDVR position translation
RERoT scheduler state
```

---

## A.3 兼容性总矩阵

下表中的“最终要求”全部是 release blocker。

| 现有机制 | RERoT ON 的正式语义 | 最终要求 |
|---|---|---|
| unified KV | 所有 Lane 共用同一 physical KV pool | 必须支持 |
| shared physical cells | ancestor/shared prefix 可被多个 seq ref 引用 | 必须支持 |
| TriAttention | 对 RERoT PUBLIC KV 做合法 lossy reclaim | 必须支持 |
| sparse positions | DDVR 只渲染 resident cells，不假设连续 | 必须支持 |
| backend-native compaction | compact 后 RERoT metadata 与 cell 同步 | 必须支持 |
| TurboQuant K/V | DDVR attention 直接消费 Turbo K/V | 必须支持 |
| Flash Attention | RERoT 使用专门 DDVR FA variant | 必须支持生产 backend |
| hybrid recurrent | GDN matrix 全局共享黑板 + conv lane-local + shared full-attn KV（§14；lane-local COW 仅对照） | 必须支持 |
| recurrent rollback slots | speculative/checkpoint 语义保持 | 必须支持 |
| MTP | draft 绑定 view epoch，过期则 reject/rollback | 必须支持 |
| ordinary speculative | 若模型/配置已有支持，同样 epoch-aware | 必须支持 |
| prompt cache | 普通 prefix 可复用；RERoT episode state 可持久化 | 必须支持 |
| RAM demotion | 整个 episode 或可恢复子状态可 swap | 必须支持 |
| manual slot save/restore | 保存完整 RERoT tree + memory lineage | 必须支持 |
| checkpoint | 包含 tree/view/publish epoch | 必须支持 |
| partial rollback | 回滚对应 Lane 的 private state 与 draft | 必须支持 |
| context shift | reader view 与 tree metadata 同步裁剪 | 必须支持 |
| cache reuse / KV shift | 仅对普通线性区间或可证明安全区间执行 | 必须支持且不可误用 |
| idle demotion | episode-aware | 必须支持 |
| active preemption | episode-aware，不能留下孤儿 Lane | 必须支持 |
| auto-fit | 计入 RERoT scratch / seq/recurrent reserve | 必须支持 |
| recurrent-only pressure | 不错误触发 Tri KV reclaim | 必须保持 |
| `n_cmpl` | 每个 completion 有独立 RERoT episode | 必须支持 |
| shared prompt parent/child | 外层 shared prefix 与内层 RERoT 可嵌套 | 必须支持 |
| streaming | PUBLIC reasoning 按完整行的实际完成顺序流出；最终答案保持 stock SSE | 必须支持 |
| cancellation | 取消整个 episode，原子清理所有 Lane | 必须支持 |
| retry | 不泄漏旧 episode tree/seq refs | 必须支持 |
| tool calling | 并发思考后恢复 stock tool grammar | 必须支持 |
| JSON/grammar | planner XML 与用户 grammar 生命周期隔离 | 必须支持 |
| LoRA | 所有 Lane 继承 request adapter state | 必须支持 |
| aLoRA | invocation boundary 与 fork lineage 保持 | 必须支持 |
| multimodal | multimodal prompt 在公共 prelude，Lane 从其后 fork | 必须支持 |
| embeddings | 不进入 RERoT，保持 stock path | 必须零回归 |
| rerank | 不进入 RERoT，保持 stock path | 必须零回归 |
| raw logits | final serial output 行为不变 | 必须支持 |
| backend sampler | 每个 active Lane 独立 sampler row | 必须支持 |
| penalties | sibling token 不直接进入机械 penalty history | 必须保持设计 |
| metrics | 增加 RERoT metrics，不破坏已有 Tri metrics | 必须支持 |
| CPU | correctness reference | 必须支持 |
| Vulkan | 当前生产 backend | 必须支持 |
| CUDA | 若当前 build 对普通模型支持该路径，RERoT 不得永久架构性排除 | release scope 内补齐 |
| Metal | 同上 | release scope 内补齐 |
| pipeline parallel | view input 必须满足 graph reuse/synchronization | 必须无回归 |
| graph reuse | span shape/version 进入 reuse 判定 | 必须支持 |
| MTP hook context | target/draft memory lineage 不错配 | 必须支持 |

“完整兼容”不意味着每个机制都必须在 RERoT 并发阶段被激活。

例如：

```text
embedding / rerank
```

本身不是 autoregressive reasoning，因此正确兼容行为就是：

```text
不进入 RERoT；完全走旧路径；零回归。
```

而 tool calling 的正确兼容行为是：

```text
parallel reasoning phase：不执行工具
final fence 后：恢复现有 tool grammar / MCP / tool parser
工具结果回来：开启下一次新的 RERoT episode
```

这属于“语义组合”，不是“功能禁用”。

---

## A.4 TriAttention：必须成为 RERoT 的共享内存回收器

当前 TriAttention 的固定产品契约不能因 RERoT 改变：

```text
fill-first
→ first pressure drain to 3/32
→ sticky maintenance
→ floor exhausted
→ atomic idle demotion / active preemption
```

RERoT ON 时同样必须遵守。

### A.4.1 不能给 archive ownership 计重复重要性

RERoT 会引入：

```text
owner Lane
archive ownership
active execution ref
ancestor/shared refs
```

Tri scorer 的 logical sequence 统计不能因为一个 PUBLIC cell 被多个 RERoT bookkeeping ref 指向，就把它错误算成多份内容。

要明确区分：

```text
physical cell
semantic owner
reader visibility
retention refs
```

Tri 的 target 仍按**真实 logical history**计算，而不是按 reader 数量乘倍。

### A.4.2 reclaim 后 DDVR view 必须自然变稀疏

禁止：

```text
假定 PUBLIC run 的 storage position 连续
假定 virtual run 可以用一个 [p0,p1) 还原全部 cell
```

Tri reclaim 后：

```text
view builder
  ↓
枚举 resident PUBLIC cells
  ↓
按 PAC-DFS semantic order 排序
  ↓
重新 dense virtual pack
```

因此 DDVR 的“dense”是**reader virtual address dense**，不是 physical/history position dense。

### A.4.3 compaction 不允许破坏 RERoT metadata

现有 `llama_kv_cells` 是唯一 metadata owner 的约束继续有效。

RERoT 新增 per-cell 信息若存在，必须：

- 与 `llama_kv_cells` 一起 pack；或
- 只存稳定 logical id，不存 physical index；或
- 可从 `llama_kv_cells` + run table 重新派生。

禁止 server 保存：

```text
physical_cell_index -> rerot node
```

并把它当长期真相。

### A.4.4 Tri + RERoT 验收

至少需要：

```text
RERoT FullKV
RERoT Tri 3/32
RERoT Tri pressure + recursive fork
RERoT Tri pressure + queued children
RERoT Tri pressure + final fence
RERoT Tri floor exhausted + preemption
```

并验证原有：

```text
tri_drain_total
tri_maintenance_total
tri_cells_before/after/freed
tri_references_removed
tri_target_references
tri_hard_keep
tri_shared_keep
tri_score_seconds
tri_pack_seconds
tri_floor_exhausted_total
tri_atomic_fallback_total
```

仍然语义正确。

---

## A.5 TurboQuant：DDVR 不得退化到 F16 KV

当前生产使用：

```text
K = Turbo4
V = Turbo2
Vulkan full offload
```

因此“RERoT 只支持 F16/BF16 KV”只能是 CPU correctness 阶段限制，不能成为正式版本限制。

### A.5.1 Q rotation 必须发生在正确变换域

当前 fork 已经存在：

```text
TurboQuant graph-side Q WHT
Turbo K/V cache
attention output inverse WHT
```

DDVR phase translation 必须明确相对于：

```text
pre-RoPE Q
post-RoPE Q
pre-WHT Q
post-WHT Q
```

的执行顺序。

不得凭经验在量化后向量上随意套 rotation。

推荐冻结顺序：

```text
Q projection / norm
→ reader-relative IMRoPE/DDVR transform
→ Turbo WHT transform
→ Turbo K dot product
```

K 仍保持 writer storage phase + Turbo cache 表示。

### A.5.2 Turbo padding 必须保持

当前 head_dim 非 128 对齐时会 pad Q/K/V。

RERoT kernel 必须继续以实际 tensor：

```text
type
ne[]
nb[]
```

为准，不得假定：

```text
head_dim == storage head_dim
```

### A.5.3 验收

CPU materialized reference 至少对：

```text
F32/F16
Turbo4/Turbo2
partial IMRoPE
GQA
padding head_dim
```

建立数值对照。

---

## A.6 MTP / speculative decoding：使用 memory epoch，而不是永久关闭

RERoT 下 speculative 的核心问题不是“不能 speculative”，而是：

```text
draft 是在哪一个共享内存版本上产生的？
```

因此给每个 Lane 维护：

```cpp
struct rerot_view_stamp {
    uint64_t topology_epoch;
    uint64_t publish_epoch;
    uint64_t layout_epoch;
};
```

每个 MTP draft checkpoint 绑定这个 stamp。

### A.6.1 acceptance 规则

如果 draft 生成期间：

```text
没有影响 reader 的新 PUBLIC commit
且 topology 未变化
且 layout epoch 未变化
```

按现有 MTP 正常 verify/accept。

如果发生变化：

```text
invalidate old draft
restore existing checkpoint
drop uncommitted draft KV/recurrent state
从最新 reader view 重新 draft
```

这不是关闭 MTP，而是与普通 speculative decoding 遇到 target context 变化时同样的失效处理。

### A.6.2 strong frontier

同一 frontier 的 peers 在联合 forward 内互见时，MTP 不得跨越尚未确定的 frontier commit 边界。

最安全语义：

```text
MTP draft window ⊆ 当前稳定 publish epoch
```

可以一次 draft 多 token，但一旦 peer commit 导致该 reader view epoch 更新，剩余 draft 全部失效。

### A.6.3 checkpoint 内容

在现有 MTP checkpoint 上追加：

```text
rerot node id
reader view stamp
public frontier id
planner mode
pending-record state
```

recurrent state 仍复用现有 checkpoint，不复制一套。

### A.6.4 MTP 验收

至少：

```text
RERoT + MTP no peer update
RERoT + MTP peer update invalidates draft
RERoT + MTP fork barrier
RERoT + MTP rollback
RERoT + MTP Tri pressure
RERoT + MTP final fence
```

并继续统计原有 acceptance。

---

## A.7 Hybrid recurrent：完整兼容的正式语义（2026-09-04 修订为共享黑板）

这一点已经从“兼容障碍”升级为 RERoT 在 Ornith 上的核心语义；再从 lane-local 升级为共享黑板。

### A.7.1 定义

```text
full-attention KV = episode shared precise memory（DDVR）
GDN recurrent matrix = episode-global shared fuzzy blackboard（RBB/RSV）
conv state = lane-local（永远私有）
```

Ornith/Qwen3.5 的 full-attention 层为 `3, 7, 11, ..., 39`（Qwen3.6 同构 `10 x (3xGDN + 1xFull)`）；token forward 仍是 `R0 R1 R2 → Shared A3 → R4 R5 R6 → Shared A7 → ...`，但 recurrent 不再靠“各 Lane 私有 state 被 shared attention 输出慢慢渗透”同步，而是直接只有一份 global S（首版 27 层共享 + R0-R2 私有，成功后 30/30）。数学与分工见 §14.0–§14.1；N=1 退化原生；PRIVATE/PENDING 永不参与 commit。

### A.7.2 只允许对称 commit / block-delta，不允许野蛮 merge

允许（§14.1）：同 S 出发的 frontier mean commit；终版 one-decay + order-free block-delta + 全 q 读 merged S；极简先行版 Recurrent Gossip（低频 mean 最前 3 GDN matrix）。禁止：串行 A→B→C 写（clock 加速、顺序不对称）、无归一加和、混 conv state、PRIVATE/PENDING 参与 commit、把 PAC-DFS 顺序硬塞进 recurrent。state 本身不重排，transition 可组合但不进 v1（见 §14.3）。

### A.7.3 对照分支 fork 继续使用 COW

Scheme 0 / R0-R2 私有对照仍复用现有 `seq_cp()` + recurrent `find_slot()` COW：parent tail → children initially share → child 运行后 materialize 私有 state。这必须兼容 queued child（对照分支）、RAM demotion、checkpoint、MTP rollback、active preemption。正式共享路径无 per-child recurrent lineage。

### A.7.4 queued child 不允许 host snapshot 爆炸

正式路径排队 child 只存 logical descriptor + title + sampler/parser + 私有 conv tail，admission 接当前 global brain + 最新 PUBLIC KV；logical child count 与 recurrent seq-id capacity 脱钩。对照分支才用 parked seq/reference lineage；任何路径都不复制完整 recurrent tensor 到 host。若对照分支 `LLAMA_MAX_SEQ` 不足，做 seq-id virtualization / parked-state table，不限制 child 数。

---

## A.8 RAM swap / slot save / restore：保存 episode，而不是只保存一个 server slot

RERoT episode 的可恢复单位不再只是：

```text
seq_id + token tape
```

必须包含：

```text
episode id/version
tree topology
node states
parent/child order
queued FIFO order
active/parked/retired mapping
public/private/pending run metadata
publish epochs
topology epoch
layout epoch
sampler state per live Lane
planner state per live Lane
pending XML parser state
KV sequence refs
recurrent state lineage
MTP checkpoint + view stamp
global hard-abort counters/state
finalization state
```

### A.8.1 state format 必须 versioned

增加：

```text
REROT_STATE_MAGIC
REROT_STATE_VERSION
model/rope compatibility fingerprint
Tri calibration fingerprint（Tri ON 时）
```

旧 binary 遇到新 RERoT state 必须明确拒绝。

新 binary 读取 ordinary state 时仍走普通路径。

### A.8.2 RAM demotion

不能只 demote 某个 active Lane 而让 episode tree 仍以为它 resident。

推荐两种级别：

```text
episode-level demotion
```

优先用于 server idle prompt cache；或者：

```text
subtree-level demotion
```

但必须把 logical node 与 physical state residency 分开。

v1 production 最简单可靠的是 episode-level。

### A.8.3 restore 后 virtual layout 不依赖旧 physical index

恢复到不同 physical indices 后：

```text
PAC-DFS
→ rebuild spans from logical ownership
→ recompute DDVR virtual positions
```

不得持久化旧 GPU cell index 作为语义地址。

---

## A.9 Context shift：必须支持，但 shift 的对象是“可见语义历史”

普通 context shift 假定一条线性 token tape。

RERoT 没有单一全局 tape，所以不能直接复用：

```text
seq_rm(id, n_keep, ...)
seq_add(...)
```

去随机裁某个 Lane。

### A.9.1 语义

当 reader-visible public history 接近 context 上限时：

1. 保留共同 system/user prefix 与必要 chat template；
2. 保留当前 active Lane 的最近私有/公开局部状态；
3. 在**整个 episode**上选择一个可裁剪的最老 PUBLIC semantic region；
4. 对所有 reader 一致删除该 logical run/token；
5. 删除对应 physical KV refs；
6. 更新 run table；
7. PAC-DFS 自动重新 dense pack；
8. bump `layout_epoch` / `publish_epoch`；
9. invalidate 所有受影响 MTP draft；
10. recurrent state不逆向重写。

这相当于：

```text
shared-memory log truncation
```

而不是普通字符串 shift。

### A.9.2 private history

private planner/control token 默认不属于 public context budget 的 reader view，但仍占 physical/runtime state。

若必须裁 private history，只能裁已经不再需要执行的 frozen/retired private run；活跃 Lane 的 causal private state不能任意删。

### A.9.3 和 Tri 的关系

```text
TriAttention = lossy physical residency compression
context shift = logical history deletion
```

二者必须分离。

Tri eviction 不改变逻辑文档 token tape；context shift 改变。

---

## A.10 Prompt cache / cache reuse

### A.10.1 普通 prompt prefix

请求进入 RERoT 前的：

```text
system
user
chat template
multimodal prefix
serial prelude（若已持久化完整 episode 则另论）
```

继续使用现有 prompt LCP/cache。

### A.10.2 RERoT episode cache

如果保存完整 RERoT state，cache key 不能只基于最终可读 transcript。

因为两个 transcript 相同的 episode 可能有不同：

```text
writer-contextual KV
recurrent states
tree topology
epochs
```

因此必须使用独立 cache/state fingerprint。

### A.10.3 chunk reuse

现有基于 KV shift 的 `n_cache_reuse` 只能对：

```text
普通 serial prefix
```

或者最终已经 freeze 成明确 ordinary coordinate 的安全区域使用。

活跃 DDVR PUBLIC run 不能调用普通 `seq_add()` 来“修 virtual position”。

---

## A.11 Preemption / slot scheduling：preempt 的资源单位是 episode

RERoT 内部 Lane 不能被 server 当成 N 个互不相关用户请求。

### A.11.1 active preemption

若 atomic fallback 决定抢占 RERoT 请求：

必须：

```text
freeze/cancel all active Lane writes
serialize or discard whole episode according to现有策略
release all execution seq ids
release parked refs
release pending child descriptors
release private control KV
preserve/clean archive refs consistently
```

不能只 preempt 一个 Lane 然后让 sibling 继续，除非未来显式实现“subtree preemption”并给出正式语义。

### A.11.2 idle demotion

同样以 episode 为最小可靠单位。

### A.11.3 recurrent-only pressure

保持当前项目不变量：

```text
KV capacity 足够
recurrent capacity 不足
→ 不调用 TriAttention
→ 进入原 recurrent victim handling
```

RERoT 不得因为它有很多 Lane 就把 recurrent shortage 伪装成 KV shortage。

---

## A.12 Auto-fit / memory accounting

RERoT 不能绕过当前 auto-fit。

启动 probe 必须计入 worst-case RERoT runtime scratch：

```text
span table GPU/host input
DDVR phase input
RERoT FA temporary storage
frontier query rows
parked recurrent metadata
episode runtime host metadata
MTP + RERoT simultaneous scratch
Tri scorer/pack scratch
```

真正的大块 recurrent state 已经属于现有 recurrent capacity，不应重复 reserve。

### A.12.1 seq-id 虚拟化（对照分支；共享正式路径已脱钩）

协议允许无限逻辑 child，但 `LLAMA_MAX_SEQ` 当前有限。共享黑板正式路径 logical child 不占 recurrent seq-id，无需虚拟化即可无限排队。

对照分支仍必须明确：

```text
logical node id != llama_seq_id
```

`llama_seq_id` 是 execution/residency handle，可回收复用。

若 parked child 需要长期保持 COW lineage 而 seq-id 不够，应实现：

```text
parked lineage descriptor
```

或可恢复 state handle，而不是把逻辑 child 上限硬绑到 256。

---

## A.13 外层 `n_cmpl` / shared-prefix 与内层 RERoT

当前 server 已经支持 parent prompt + 多 completion child。

RERoT 必须支持嵌套：

```text
shared user prompt
├── completion 0 → RERoT episode 0
├── completion 1 → RERoT episode 1
└── completion 2 → RERoT episode 2
```

不能把：

```text
outer completion child
```

和：

```text
inner RERoT Lane
```

混成同一层树。

### A.13.1 ownership

公共 prompt physical cells 可以继续被所有 completion shared refs 共享。

fork point 后：

```text
episode_id
```

成为 RERoT visibility domain。

不同 completion episode 的 PUBLIC KV **绝不可互见**。

### A.13.2 Tri shared union

必须额外覆盖：

```text
outer shared prompt cell
× inner RERoT archive/exec refs
```

确保一次 reclaim 不会删除另一个 completion 仍需要的 physical cell。

---

## A.14 Streaming / SSE / response parser

RERoT streaming 仍由 episode root request 独占一条 assistant response。各 Lane
已提交的 PUBLIC reasoning 复用现有 chat parser 的 `reasoning_content` /
thinking block；不得伪装成最终答案 `content.delta`。PENDING record 必须等到
整条记录原子发布后才可见，PRIVATE control 永不可见。

### A.14.1 完整行与完成时间顺序

每个 Lane 独立缓冲当前未完成行。只有读到 `\n` 后，才把包含该换行符的完整行
作为一个不可拆分的 reasoning delta 发送：

```text
Lane B 先完成一行 -> 先发送 B
Lane A 后完成一行 -> 后发送 A
```

顺序取实际 commit/完成时间，不取 node id、创建顺序或最终 PAC-DFS 文档顺序；
同一 decode frontier 内同时完成时，以 server slot commit 顺序稳定裁决。Lane
终止或 fork 时允许把最后一个非空残行补一个 `\n` 后发送，避免丢失已经公开的
尾部文本。

因此 streaming reasoning 可以在逻辑上交错；这是实时完成顺序，不是最终文档
序列化。非 streaming 响应仍按 PAC-DFS reader view 渲染完整 reasoning 文档。

### A.14.2 最终串行输出

最终 acquire fence 后：

- 已流出的 reasoning 不得按 PAC-DFS 再发送一次；
- 只发送全局 `</think>` 边界和 stock serial final answer；
- 整条响应只发送一次 assistant role/message-start；
- `finish_reason` 保持 stock 语义；
- `[DONE]` 恰好一次；
- tool-call diff parser 与 Anthropic/OpenAI Responses block lifecycle 不变。

### A.14.3 reasoning trace 可选模式

`rerot.trace.*` 只承载显式 opt-in 的调试元数据，例如：

```text
rerot.trace.delta
rerot.node.start
rerot.node.stop
```

它与默认的 PUBLIC reasoning 行流相互独立，不能改变正文、顺序或终止语义。

### A.14.4 hard abort

全局预算/资源硬停止时仍必须保证 transport 层正常闭合：

```text
一个明确 finish/error reason
一个终止事件
无重复 chunk
```

但不能伪造自然 `stop`。

---

## A.15 Cancellation / retry / control task

用户取消一个 RERoT 请求时：

```text
cancel episode
→ stop all active writers
→ clear queued children
→ invalidate MTP drafts
→ release execution seq ids
→ release parked lineage
→ clear pending XML
→ remove episode-only KV refs
→ preserve shared outer prompt refs if other requests still use them
```

retry 必须建立新 episode generation/id，避免旧异步事件误提交到新请求。

所有 runtime callback 至少带：

```text
request/task id
episode generation
node id
```

防止 ABA 问题。

---

## A.16 Tool calling / grammar / JSON schema

### A.16.1 用户 grammar 与 planner grammar 分离

当前请求可能已经配置：

```text
JSON schema
GBNF grammar
tool grammar
```

RERoT planner 的 `<ol><li>...` 结构约束不能覆盖或污染用户最终 grammar state。

每个 Lane 需要独立：

```text
planner parser / temporary grammar state
```

而 final serial output 恢复原始 user sampler/grammar。

### A.16.2 tool call

并发思考中不实际执行工具，是 execution protocol 的定义，不是“不兼容工具”。

最终 fence 后：

```text
stock sampler
stock tool grammar
stock tool parser
stock MCP/server tool path
```

完整恢复。

工具结果进入模型后，若仍需 reasoning：

```text
new serial prelude
→ new RERoT episode
```

旧 episode 是 immutable history，不复活 writers。

---

## A.17 LoRA / aLoRA

### A.17.1 LoRA

同一 request 的所有 Lane 必须继承完全相同 adapter set/scale。

原因：它们共享 KV。

禁止同一 RERoT episode 内：

```text
Lane A uses adapter X
Lane B uses adapter Y
```

却还共享 PUBLIC KV，除非未来把 adapter identity 写入 memory domain 并重新定义语义。

### A.17.2 aLoRA

fork point 若发生在 invocation boundary 之后：

所有 child 继承当前 invocation state。

fork point 在之前：

后续每 Lane 的 invocation detection 仍按现有 token semantics 工作。

RERoT topology barrier 不能错误调用普通 cache reuse 跨过 aLoRA invocation start。

### A.17.3 batch compatibility

现有 `can_batch_with()` 对 adapter 的约束必须继续生效。

不能为了强行 batch RERoT lanes 把不兼容 LoRA 请求塞进一个 batch。

---

## A.18 Multimodal

当前 RERoT 不需要给图片 patch 本身建立并发 Lane。

正确语义：

```text
system/user text + image/audio/video embedding
          ↓
正常公共 prompt prefill
          ↓
serial reasoning prelude
          ↓
RERoT fork
```

因此所有 child 继承同一已经处理好的 multimodal context。

### A.18.1 M-RoPE / IMRoPE

multimodal token 本身继续使用原多轴 position。

DDVR 只重映射 RERoT reasoning text runs。

禁止对视觉 spatial position 做 PAC-DFS dense 排版。

### A.18.2 image chunk cache

RERoT 不改变现有 image chunk token/embedding cache 生命周期。

如果 context shift 需要删除早期 multimodal region，仍遵循现有“不可随意切 image chunk”的约束；必要时整块保留。

---

## A.19 Embedding / rerank / non-generation requests

这些路径不存在 RERoT reasoning 的收益或语义需求。

正式兼容定义：

```text
task.type == EMBEDDING / RERANK
→ never enter RERoT
→ same batch/graph/memory path as before
```

并加入自动化断言：

```text
rerot global enabled
```

也不能让 embedding/rerank 分配 RERoT runtime 或更改输出。

---

## A.20 Backend sampling / sampler state

每个 live Lane 都必须拥有独立 sampler state。

但不能因此退回“全部 raw logits 拉回 CPU”。

当前 backend sampling 能处理多个 seq row 时，RERoT 应继续使用。

### A.20.1 repetition/frequency penalty

正式规则保持：

```text
ancestor public tokens
+ current Lane own public/private semantic generation（按现有 sampler 定义）
```

进入该 Lane 的 sampler history。

sibling PUBLIC token 通过 attention 改变 logits，但不机械加入 repetition penalty history。

否则多个 Lane 会因为同伴说过某个关键词而被 sampling penalty 强行压制，改变我们想研究的共享注意力语义。

### A.20.2 seed

使用：

```text
seed(node) = H(root seed, tree path, episode generation)
```

保证：

- 可复现；
- queue admission 时机不改变随机流；
- RAM restore 后继续同一随机流。

---

## A.21 Graph reuse / pipeline parallel

RERoT span table 大部分内容应该是 graph input，而不是让每次布局变化都重建 graph topology。

### A.21.1 graph topology key

只把真正改变 tensor shape / op topology 的内容纳入 `can_reuse()`：

```text
number of query rows
span capacity bucket
backend kernel variant
RERoT enabled/disabled
strong/lag1 mode（若改变 op）
```

具体：

```text
virtual_pos0
storage_pos0
physical_start
count
visibility
```

尽量作为 input tensor 数据更新，不作为 graph rebuild key。

### A.21.2 pipeline parallel synchronization

现有 graph reuse 在 pipeline parallel 下会在覆盖 graph inputs 前 synchronize。

RERoT 新 input tensors 必须跟现有 input 生命周期一致，不能绕过这道同步。

尤其：

```text
span table
DDVR phase offsets
visibility mask
frontier metadata
```

不能指向下一 frontier 已经覆写的 host vector。

---

## A.22 Backend 完整兼容策略

### A.22.1 CPU 是规范 reference

CPU 必须永远保留一个慢但清楚的 materialized/segmented reference path，用于：

- 数值测试；
- backend parity；
- 新模型架构验证。

### A.22.2 Vulkan 是第一生产实现

因为当前生产是 Vulkan，RERoT release blocker 必须包括：

```text
Turbo4/Turbo2
IMRoPE
GQA
strong frontier
Tri sparse resident cells
MTP coexistence
online softmax across DDVR spans
```

不能以 non-FA fallback 作为最终生产实现。

### A.22.3 CUDA / Metal

如果项目当前对目标模型/attention op 的普通路径支持这些 backend，RERoT 不应在公共 API 层设计成 Vulkan-only。

要求：

```text
GGML op semantics backend-neutral
span table ABI backend-neutral
DDVR math backend-neutral
```

然后分别提供 backend kernel。

在某个 release 仅认证 Vulkan 时，可以标记其他 backend 的 RERoT 为“未认证”，但不能把实现结构写死到 Vulkan 私有 server path，导致后续无法补齐。

“完整兼容当前生产机制”的最低 release blocker 是 Vulkan；“完整兼容项目所有已支持 backend”的最终总目标则要求逐 backend parity。

### A.22.4 unsupported backend 不得 silent wrong

在补齐之前必须明确报 capability error，不能静默退到语义错误的 stock attention。

但最终 Definition of Done 不允许长期停留在 capability error。

---

## A.23 Flash Attention：必须有原生 DDVR variant

正式生产实现不能：

```text
foreach span:
    launch independent attention
    softmax separately
sum outputs
```

也不能为了 DDVR materialize 一份完整 rephased K。

正确 fused kernel：

```text
for each KV tile:
    load span descriptor
    compute reader-relative Q phase
    QK tile
    apply visibility mask
    update global online-softmax m/l/O
```

所有 span 共享同一个 softmax normalization。

### A.23.1 pipeline variant

推荐新增：

```text
FLASH_ATTN_EXT_REROT
```

或等价清晰 op/flag，而不是把 stock FA inner loop 塞很多动态 `if (rerot)`。

这样：

- ordinary FA 完全零回归；
- shader specialization 清楚；
- Vulkan/CUDA/Metal 可各自实现；
- RPC/op metadata 可以 version。

---

## A.24 Shared-prefix physical union

必须专门覆盖至少三层 sharing：

```text
chat/prompt shared prefix
    ↓
outer n_cmpl sharing
    ↓
inner RERoT fork ancestry
```

一个 physical KV cell 可能同时被：

- 多个 ordinary completion seq；
- 某 episode ancestor；
- active Lane；
- archive handle；
- parked child lineage；

引用。

删除任何一个 ref 都不得提前 free physical cell。

测试必须分别核算：

```text
references_removed
physical_freed
```

不能混为一个指标。

---

## A.25 State serialization 与 sparse/Tri fingerprint

当前 Tri sparse state 已经要求未来持久 metadata 包括 policy/fingerprint。

RERoT 要在此基础上扩展，而不是另起一套互不兼容 state container。

推荐 state capability bitmap：

```text
STATE_CAP_SPARSE_KV
STATE_CAP_TRIATTENTION
STATE_CAP_REROT
STATE_CAP_REROT_TREE
STATE_CAP_REROT_PRIVATE_RUNS
STATE_CAP_REROT_MTP_EPOCH
STATE_CAP_HYBRID_RECURRENT
```

load 时先检查：

```text
model fingerprint
rope config
Tri config/calibration fingerprint
RERoT state version
backend-independent tensor layout requirements
```

不匹配必须拒绝，不得“尽量加载”。

---

## A.26 Metrics / logging / observability

RERoT 新 metrics 至少包括：

```text
rerot_episode_total
rerot_episode_active
rerot_nodes_created
rerot_nodes_started
rerot_nodes_retired
rerot_nodes_queued
rerot_queue_max
rerot_forks_total
rerot_max_depth
rerot_max_live_lanes
rerot_public_tokens
rerot_private_tokens
rerot_pending_tokens
rerot_completed_episode_total
rerot_completed_model_tokens
rerot_parallel_model_tokens
rerot_completed_episode_seconds
rerot_parallel_seconds
rerot_frontiers
rerot_topology_barriers
rerot_refresh_total
rerot_mtp_invalidations
rerot_context_shifts
rerot_hard_aborts
rerot_final_fences
rerot_ddvr_seconds
rerot_span_count
```

并且所有现有 Tri metrics 含义保持不变。

日志至少能关联：

```text
request id
episode id
node path/id
seq handle
frontier
publish epoch
```

但默认 INFO 不打印完整 private reasoning。

---

## A.27 安全失败与 fallback 顺序

RERoT 不能破坏 atomic 已经验证的 pressure fallback。

正式顺序：

```text
1. RERoT 当前原子 frontier 能否完整执行？
2. KV pressure → Tri drain/maintenance
3. refresh physical usage
4. 若 Tri floor exhausted → atomic idle demotion / active preemption
5. recurrent-only pressure → 直接原 recurrent fallback，不调用 Tri
6. 若整个原子步骤仍无法执行 → 按请求/episode 失败语义处理
```

不能：

- 半个 frontier 成功、半个失败；
- 只 commit 一部分 peer token；
- KV allocation fail 后偷偷改成 lag1；
- 为了活下来提高 Tri residency；
- 只杀一个 Lane 后让最终答案继续伪装完整。

---

## A.28 Compatibility Test Matrix：最终必须自动化

不能只做几个 smoke。

至少建立下面的矩阵。

### A.28.1 RERoT OFF 回归矩阵

```text
Tri OFF / ON
MTP OFF / ON
Turbo OFF / ON
prompt cache OFF / ON
stream OFF / ON
n_cmpl 1 / 3
LoRA OFF / ON
multimodal OFF / ON（有对应模型时）
```

对同 seed greedy 路径要求输出一致或符合当前机制原有容差。

### A.28.2 RERoT ON 功能矩阵

至少：

```text
RERoT + FullKV
RERoT + Tri
RERoT + Turbo
RERoT + Tri + Turbo
RERoT + MTP
RERoT + Tri + MTP
RERoT + Turbo + MTP
RERoT + Tri + Turbo + MTP
RERoT + RAM save/restore
RERoT + context shift
RERoT + preemption
RERoT + shared-prefix
RERoT + n_cmpl
RERoT + streaming
RERoT + LoRA
RERoT + tool call after final fence
```

### A.28.3 压力组合

最关键的是：

```text
RERoT
+ 6 outer server slots
+ recursive forks
+ queued children
+ Tri pressure
+ Turbo4/Turbo2
+ MTP
+ streaming
+ at least one RAM demotion/restore or preemption event
```

要求：

- 无 5xx；
- 无 OOM；
- 无 Vulkan validation error；
- 无 deadlock；
- 无 orphan seq refs；
- 无 orphan recurrent cell；
- 无重复 SSE content；
- 每请求恰好一个终止事件；
- hard abort 与 natural final 可区分；
- Tri fallback 顺序正确；
- MTP rollback 正确；
- 最终 answer/tool call 来自 final fence 后的 serial continuation。

---

## A.29 完整兼容版实施顺序

仍然应该分阶段开发，但**阶段只是开发顺序，不是最终删功能借口**。

推荐：

```text
Phase 0  OFF zero-regression shell
Phase 1  tree / scheduler / PAC-DFS
Phase 2  CPU DDVR + IMRoPE correctness
Phase 3  hybrid recurrent COW + queued child
Phase 4  strong frontier + final fence
Phase 5  Vulkan DDVR FA + TurboQuant
Phase 6  TriAttention sparse/compaction integration
Phase 7  MTP epoch-aware integration
Phase 8  RAM save/restore + checkpoint/rollback
Phase 9  context shift + cache reuse boundaries
Phase 10 outer n_cmpl/shared-prefix/preemption/streaming matrix
Phase 11 LoRA/aLoRA/multimodal/tool grammar integration
Phase 12 cross-backend parity + long soak
```

每一 Phase 合并前：

```text
RERoT OFF regression suite
```

必须继续通过。

---

## A.30 对正文 Definition of Done 的正式覆盖

正文第 43 节最后一句：

```text
TriAttention 和 MTP compatibility 可以作为 v1.1 / production integration gate
```

从本附录加入后，解释为：

```text
可以晚于核心科研原型开发，
但不能晚于“完整可部署 RERoT”交付。
```

正式 production Definition of Done 追加以下硬门：

1. RERoT OFF 全机制零回归。
2. RERoT ON + unified KV 正常。
3. RERoT ON + hybrid recurrent 正常。
4. RERoT ON + TurboQuant K/V 正常。
5. RERoT ON + Vulkan FlashAttention/DDVR 正常。
6. RERoT ON + TriAttention 3/32 正常。
7. RERoT ON + backend-native compaction 正常。
8. RERoT ON + MTP 正常，epoch invalidation/rollback 正确。
9. RERoT ON + prompt cache / RAM save/restore 正常。
10. RERoT ON + checkpoint / partial rollback 正常。
11. RERoT ON + context shift 正常。
12. RERoT ON + idle demotion / active preemption 正常。
13. recurrent-only pressure 不错误调用 Tri。
14. RERoT ON + outer `n_cmpl` / shared-prefix 正常。
15. RERoT ON + streaming 无重复、恰好一个终止事件。
16. RERoT ON + cancellation / retry 无 orphan state。
17. RERoT ON + LoRA/aLoRA 正常。
18. multimodal prompt 可进入共同 prelude 后 fork。
19. tool calling 在 final fence 后恢复现有完整能力。
20. embedding/rerank 等非生成路径在全局开启 RERoT 配置时仍零回归。
21. graph reuse / pipeline parallel 无 input lifetime bug。
22. auto-fit 包含全部 RERoT/Tri/MTP scratch。
23. production-scale full-slot pressure 通过。
24. shared physical cell union 压力测试通过。
25. RAM/checkpoint/context-shift/MTP/Tri 组合矩阵通过。
26. 长稳测试无 GPU/CPU leak、deadlock、stale tensor 或 state corruption。
27. Vulkan 当前生产配置下性能没有不可接受断崖。
28. 所有新增 state format 有 version/fingerprint，错误版本明确拒绝。
29. 所有已有 Tri metrics 语义未变，RERoT metrics 可观测。
30. build/deployed binary/runtime library checksum 与最终验收 artifact 一致。

只完成前文“科研核心 v1”而没有这些兼容门，可以称：

```text
RERoT research prototype
```

不能称：

```text
atomic-llama-cpp-turboquant production-compatible RERoT
```

---

## A.31 完整兼容后的代码量修正

正文 `8K–10.5K` 是以：

```text
核心 scheduler/DDVR
+ Vulkan
+ Tri
+ MTP
```

为主的估算。

如果严格执行本附录要求，把：

```text
RAM serialization
context shift
preemption
outer n_cmpl/shared-prefix
LoRA/aLoRA
multimodal integration
完整 metrics
cross-backend abstraction
完整组合测试
```

一起纳入正式交付，代码量应上调。

比较现实的范围：

| 部分 | 预计新增/修改 LOC |
|---|---:|
| RERoT tree/runtime/PAC-DFS | 1,200–1,700 |
| DDVR graph/input/reference | 1,000–1,500 |
| KV visibility/metadata integration | 500–800 |
| hybrid recurrent shared-RBB + COW 对照/parking | 500–950 |
| server frontier/scheduler/fork/final + N-row batch | 1,600–2,400 |
| Vulkan DDVR FlashAttention + multi-reader scan + RBB + Turbo | 1,500–2,300 |
| TriAttention integration | 500–850 |
| MTP/spec epoch integration | 450–750 |
| RAM/state/checkpoint/restore | 700–1,100 |
| context shift/cache reuse/preemption | 650–1,000 |
| outer n_cmpl/shared-prefix/streaming/control | 450–750 |
| LoRA/aLoRA/multimodal/tool/grammar glue | 300–550 |
| metrics/logging/capability | 250–450 |
| CPU/unit/property tests | 1,000–1,500 |
| server integration/pressure tests | 800–1,300 |
| cross-backend abstraction/parity glue | 400–800 |

合计不是简单逐项最大值相加，因为很多测试/metadata/调度代码共享。

建议把完整 production-compatible RERoT 的中心估算更新为：

```text
约 11,500–16,000 行新增/修改代码（含测试）
```

其中真正 runtime / kernel 产品代码大约：

```text
7,400–10,200 行
```

其余：

```text
4,100–5,800 行
```

主要是 unit/property/integration/pressure/state-compatibility tests。

如果进一步要求 CUDA + Metal 都在同一交付里达到与 Vulkan 同级 optimized fused kernel，而不是只保证 backend-neutral 架构和后续可实现性，则再预留：

```text
+ 2,000–4,000 行 backend-specific kernel / dispatch / tests
```

因此“所有当前机制完整兼容 + 当前生产 Vulkan 完整优化”的合理计划数是：

```text
11.5K–16K LOC
```

“再加 CUDA/Metal 同级优化”的上界计划数是：

```text
13.5K–20K LOC
```

这应替代正文最后的 `8.4K–11.3K` 前缀数字，作为**完整兼容交付**的预算基准；正文数字继续保留作为核心机制开发量参考。Parallel block-delta 融合进 RBB 行内，不单独放大上界（mean 先行验证通过后才做）。

---

## A.32 一句话冻结本附录

最终原则只有一句：

> **RERoT 是 atomic-llama-cpp-turboquant 现有推理栈上的一种新增执行语义，而不是一个要求关闭现有优化和可靠性机制才能运行的实验旁路。开发过程中可以逐项隔离，正式交付时不能永久牺牲任何已经存在且语义上可组合的能力。**

---

# Changelog

## 2026-09-04：recurrent 共享黑板 + multi-query 执行（替代 lane-local 默认）

- §0 / §1.2 / §2.2：正式语义由 `recurrent lane-local/COW` 改为 `GDN matrix 全局共享黑板（RBB/RSV）+ conv 全员私有`；旧方案降级为 Scheme 0 对照基线。
- §14：整节重写。新增 §14.0（仿射算子、可组合 transition、耗散时间、记忆分工、conv 3-token 边界、dense 存储警告）、§14.1（RBB 冻结算法、27 层首版、风险表、Parallel Delta 终版形态）、§14.2（旧 lane-local 对照 ladder）、§14.3（transition sharing 问题定义与 oracle）、§14.4（state-only ingest 降级）。
- §6 / §7 / §16：queued child 去 parked recurrent seq，admission 接入最新 global brain；COW/parking 仅对照分支保留；logical child 与 recurrent seq-id capacity 脱钩（§6.5、A.12.1 同步标注）。
- §10 Phase C-2 / §13.4 / §19 / §40 / §41：新增 multi-reader 共享扫描与 `Shared Memory / Multi-Query Execution`（N-row batch、K/V 与 S 只读一次、权重 batching、单 resident list）。
- 门与量：Gate C、§32.3、§35、§28 / §29 / A.31、metrics（新增 `rerot_rbb_*`）、A.3 / A.7 同步更新。

---

# 附录 B：总人数 / 总笔数 / 总 KV 三容量改造路线图

> 2026-09-04 追加冻结。本附录覆盖正文和附录 A 中所有把 `server slot`、
> `physical slot`、`recurrent slot`、Lane 数混成一个容量的旧表述。旧文中的
> “三笔”“2–3 physical slots”“3 fitted slots”均只代表当时的示例或历史验证，
> **不构成每人笔数上限**。

## B.0 最终容量契约

RERoT 最终只有三个并发/显存主容量：

```text
总人数 B = device-resident people / independent brains
总笔数 P = device-resident pens / Lane execution states
总 KV  K = unified physical KV cells
```

生产模式继续只暴露：

```text
--total-kv auto
```

它不再只是“把剩余显存全部做成 KV”，而是 **一次性联合求解 `B/P/K`**。用户不设置
人数、笔数或 KV token 数；三者都是 auto-fit 输出。外部容量输入只有 backend 报告的
device memory budget。模型 tensor shape、实际 recurrent/KV bytes、模型解析出的 context、
Tri ratio、MTP 和 graph scratch 都由 loader/probe 获得，不是用户调参，也不得写死某张
显卡的显存大小或某个模型的 context。

对任意 resident person `e`，其当前持有的笔数为 `p_e`：

```text
0 <= p_e <= P
sum(p_e) <= P
resident_people <= B
physical_KV_used <= K
```

**不存在：**

```text
max_pens_per_person
max_lanes_per_episode
固定三笔
按 child 数预留 recurrent state
按人数或笔数静态切分 KV
```

一个人可以在没有其他竞争者时使用全部 `P` 支笔。若该人产生的 runnable child
超过 `P`，只 admission `P` 个，其余保留 logical descriptor 并排队。若同时有多个人，
调度器只受全局 `B/P/K` 和公平/吞吐策略约束，不得再引入隐藏的 per-person quota。

这里的“人”是独立的请求状态机：

```text
普通串行请求      = 1 人，运行时通常持有 1 笔
一个 RERoT episode = 1 人，可动态持有 0..P 笔
```

这里的“笔”是 Lane-local 执行状态：

```text
exec identity
Lane-local conv tail
首版 R0-R2 private recurrent matrix
hidden/Q/logits/sampler/control state
本 frontier 的一个或多个 decode rows
```

queued child 尚未 admission 时不是一支笔。它只持有：

```text
logical node descriptor
title / parser seed / deterministic sampler seed
fork-time hand-state seed 的共享引用（如需要）
```

不得为每个 queued child 分配完整 GPU conv/R0-R2 state。若多个 sibling 需要相同
fork-time hand seed，应由一次 snapshot + COW reference 表达，不能按 child 复制。

`n_batch`、`n_ubatch`、`n_outputs_max`、shader reader tile width 都是执行形状或 tiling，
不是第四种并发容量，也不得伪装成 per-person 笔数上限。

## B.1 三种容量分别拥有哪部分资源

### B.1.1 按人数增长

每个 resident person 独占：

```text
HTTP/root response ownership
RERoT tree / runs / queue / frontier epochs
episode-global GDN recurrent S
PUBLIC document namespace
archive/keeper identity
final fence / serial-tail state
RAM persistence identity
```

一个 person 即一份独立 brain。不同 person 的 global `S` 永不 mean、merge 或
block-delta 到一起。

### B.1.2 按笔数增长

每支 resident pen 独占：

```text
Lane node binding
exec sequence handle
全部 GDN conv tail
首版 R0-R2 private S
sampler / grammar / MTP draft binding
reader-specific DDVR view
hidden / residual / Q / logits rows
per-reader online-softmax accumulator
```

一支笔若被暂停但仍保留 device-local hand state，仍计入 `P`。只有把 hand state
完整保存到现有 RAM/state 路径并释放 device handle 后，才归还 pen credit。

### B.1.3 按实际 token 增长

Unified KV 继续由 `llama_kv_cells` 唯一拥有：

```text
一个 PUBLIC token 只存一份 physical K/V
PRIVATE/PENDING token 按实际 writer 各存一份
同 frontier 有 N 支笔写 token 时新增 N 个 token cells
reader 数不复制历史 K/V
```

`K` 是全局物理 cell 上限，不按 person 或 pen 预留子区。TriAttention 仍只处理
这一维压力。

### B.1.4 最终内存公式

首版 `R0-R2 private + R4.. shared` 的 device memory 应按：

```text
M_recurrent
  = B * M_global_S_R4_plus
  + P * (M_private_S_R0_R2 + M_conv_all_layers)

M_total
  = M_model
  + M_recurrent
  + M_unified_KV(K)
  + M_graph_scratch(P)
  + M_Tri_scratch(K)
  + M_MTP(B, P)
  + headroom
```

禁止继续使用：

```text
M_recurrent = n_parallel * M_full_recurrent_sequence
```

也禁止通过扩大 `n_seq_max` 间接扩大 recurrent tensor。

## B.2 当前代码与目标模型的差距

以下是改造起点，不得重复已有 tree/DDVR/KV metadata 工作：

| 当前位置 | 当前含义 | 必须改成 |
|---|---|---|
| `common/common.h::common_params::n_parallel` | server request、Lane、recurrent cell、decode row 混用 | full-auto 的人数结果；不再是 RERoT 用户输入 |
| `src/llama-cparams.h::n_seq_recurrent` | 完整 recurrent sequence rows | auto 派生的 brain rows 与 hand rows 两个容量 |
| `common/common.cpp::common_context_params_to_llama()` | RERoT 时 `n_seq_recurrent = n_parallel` | 从 auto-selected `B/P` 派生 grouped recurrent layout |
| `common/common.cpp::common_dynamic_recurrent_target()` | 从 KV 估算同一种 recurrent slots | 删除单轴推导，交给三容量 fit |
| `common/fit.cpp::common_fit_recurrent_cache()` | 探测完整 recurrent slot 数 | 探测 `B` brain rows、`P` hand rows和相应 scratch |
| `tools/server/server-context.cpp::slots` | 一个 `server_slot` 同时是 request owner 和 Lane executor | `server_slot` 只保留 person/request ownership；另建 pen pool |
| `server_context_impl::rerot_episode_id` | 一个 context 只能有一个 active episode | active people/episodes 集合 |
| `server_rerot_node_runtime::physical_slot` | Lane 借一个外部 server slot | `pen_id`，不再等于 request slot |
| `server_rerot_runtime::slot_to_episode_` | physical server slot 到 episode | `pen_id -> (person_id,node_id,exec_seq)` |
| `server_batch::token::id_slot` | request slot id 同时充当 seq id | 显式 `exec_seq/person_id/pen_id` |
| `llama_rerot_ctx_state` | 每个 `llama_context` 单 episode | 每 context 多 episode map |
| `llama_memory_recurrent::{r_l,s_l}` | R/S 共用同一 sequence-row arena | shared-S brain arena + Lane-local hand arena |
| `llama_memory_recurrent::tails` | logical seq 到完整 R/S cell | exec seq 分别映射 brain handle 与 pen handle |
| `LLAMA_MAX_SEQ=256` | 隐藏的 exec/archive id 天花板 | 由 `B+P+临时 keeper` 派生的动态 handle capacity |
| Vulkan RERoT FA single-row workgroup | 每 reader 独立扫 indexed entries | person-grouped multi-reader tile |
| `scripts/rerot-throughput-gate.py` | 只测 1 person 的 single-vs-multi Lane | 测 `B × P × 分配形状` |

可直接复用、不得重写：

```text
server_rerot_runtime::episodes_
rerot tree / PAC-DFS / run metadata
PUBLIC/PENDING/PRIVATE publication transaction
llama_kv_cells RERoT metadata
unified KV + backend-native compaction
DDVR effective-position数学
episode_id / run_id visibility域
TriAttention physical-cell owner
root response ownership / line mux / final fence
```

## B.3 配置与命名迁移

### B.3.1 用户侧全自动契约

RERoT 生产基线只有一个容量模式：

```text
--total-kv auto
    -> auto-select total people B
    -> auto-select total pens P
    -> auto-fill unified KV K with the remaining feasible memory
```

`--total-kv auto` 应成为 RERoT 默认，用户无需再提供 `-np`、总笔数或 context-specific
容量参数。不得新增：

```text
--parallel-pens
--rerot-max-people
--rerot-max-lanes
--rerot-lanes-per-episode
--rerot-max-pens-per-person
```

兼容规则：

```text
RERoT OFF：
    -np / --parallel 语义和当前行为完全不变。

RERoT ON + --total-kv auto：
    B/P/K 全部由 fit 产生；
    common_params.n_parallel 在 fit 后回填 B；
    server 按回填结果创建 people/pen arenas；
    默认值或旧 service 中的 -np 不得偷偷成为 B/P 的约束源。
```

若调用者在 RERoT full-auto 下显式提供与自动容量冲突的 `-np`，应 fail-fast 并提示删除
该参数，不能静默忽略，也不能退化成旧 slot heuristic。focused tests 可以通过内部
test-only constructor 注入 `B/P`，但不得为此增加生产 CLI 或环境变量。

这份契约只保证“同一 total/device memory budget + 同一模型布局得到同一容量选择”。
两张显存相同但算力/带宽不同的显卡，在不运行 benchmark 的约束下无法仅凭 VRAM 推导
各自真实吞吐峰值；本附录采用 §B.10 的保守先验，不伪称这是硬件自适应 autotune。

### B.3.2 内部派生结果

建议新增明确字段，禁止继续复用 `n_seq_recurrent` 表达两种资源。它们是 fit 输出，
不是用户配置：

```cpp
struct llama_cparams {
    uint32_t n_person_max; // auto-selected B
    uint32_t n_pen_max;    // auto-selected P

    uint32_t n_brain_rows; // 由 B 派生
    uint32_t n_hand_rows;  // 由 P 派生

    uint32_t n_ctx_kv;     // K
};
```

`n_seq_max` 只表示底层 execution/archive handle capacity，并由以下资源派生：

```text
active exec handles    <= P
archive keeper handles <= B
temporary transaction handles = 有界内部常数或可证明的 B/P 函数
```

最终实现不得让 `LLAMA_MAX_SEQ` 成为第四个用户可见容量。过渡阶段若仍受 256 限制，
只能在启动时对 `B/P` 做明确的 representability 检查；正式 clean cutover 必须把
固定 bitset/array 改成按派生 handle capacity 分配。

新增查询应语义明确：

```text
llama_n_person_max()
llama_n_pen_max()
llama_memory_get_brain_usage()
llama_memory_get_hand_usage()
llama_memory_get_kv_usage()
```

旧 `get_recurrent_capacity()` 在 RERoT grouped layout 下语义不再唯一，不得继续供
pressure planner 做决策。

## B.4 Server：把 request owner 与 Lane executor 拆开

### B.4.1 Person owner

现有 `server_slot` 最终只表示一个独立外部请求/person：

```cpp
struct server_person_slot {
    int id;
    server_task response_task;
    uint64_t episode_id;
    brain_handle brain;
    person_state state;
};
```

普通请求也走同一 person admission，但不需要 RERoT tree。

### B.4.2 Pen pool

新增全局固定容量的 pen arena：

```cpp
struct server_pen {
    pen_id id;
    person_id person;
    uint64_t episode_id;
    llama_rerot_node_id node_id;

    llama_seq_id exec_seq;
    hand_handle hand;

    common_sampler_ptr sampler;
    llama_rerot_view_stamp view_stamp;

    pen_state state;
};
```

数量严格等于 `P`。一个 pen 在任意时刻只能绑定一个 node。`server_rerot_node_runtime`
改为：

```text
physical_slot -> optional pen_id
exec_seq      -> 由 pen arena 提供
parked_seq    -> 正式 shared-RBB 路径删除
```

Scheme 0 对照所需 parked COW 必须放在独立实验路径，不能污染正式 pen accounting。

### B.4.3 多 person 生命周期

删除 `server_context_impl::rerot_episode_id` 单例门，改为：

```text
active_people: person_id -> episode_id
rerot_transport: episode_id -> response transport
pen_bindings: pen_id -> episode/node
```

当前 `server_rerot_runtime::episodes_` 已经是 map，应继续作为 logical episode
owner；不要再创建第二套 episode registry。

每个 person 独立拥有：

```text
frontier
publish/topology/layout epoch
ready_queue/running/starting
hard abort
final fence
serial tail
HTTP/SSE termination
```

不同 person 可以进入同一个 model batch，但不得共享 frontier number、RBB commit、
final condition 或错误状态。

### B.4.4 Batch token identity

`server_batch::token` 必须从：

```cpp
{id_slot, token, pos, output}
```

迁移为至少：

```cpp
{person_id, pen_id, exec_seq, token, pos, output}
```

`common_batch_add()` 使用 `exec_seq`，response/metrics 使用 `person_id`，Lane-local
state 使用 `pen_id`。禁止再依赖三者数值相等。

## B.5 Context：从单 episode gate 改为多 episode

当前：

```text
g_rerot_states[ctx] = one llama_rerot_ctx_state
```

目标：

```text
g_rerot_states[ctx].episodes[episode_id] = episode_state
g_rerot_states[ctx].exec_bindings[exec_seq] = (episode_id, node_id, pen_id)
```

现有 API 大多已携带 `episode_id`，应迁移实现而不是增加平行 API：

```text
llama_rerot_episode_begin/end
llama_rerot_set_write_tag
llama_rerot_set_frontier_views
llama_rerot_publish_run
llama_rerot_refresh_barrier
llama_rerot_mtp_is_stale
```

必须改掉以下单例假设：

```text
一个 context 只有一个 active bool
一个 context envelope 只有一个 episode id
refresh_barrier 隐含当前 episode
preemption/shift 只找一个 active RERoT episode
memory active 判断只返回 bool、不返回 owner set
```

多 person 状态保存以 person/episode 为原子单位。单个 person save/restore 不得暂停、
覆盖或改写其他 person 的 epoch/view bindings。

## B.6 Recurrent：一份 brain arena，多份 hand arena

### B.6.1 物理布局

不得新增与 `llama_memory_recurrent` 竞争的 recurrent owner。应在现有 owner 内增加
gated grouped layout：

```text
ordinary layout:
    R/S row 都按普通 sequence

RERoT grouped layout:
    shared layers:
        S_global[layer][brain_row: B]
        R_conv[layer][hand_row: P]

    private prefix layers R0-R2:
        S_private[layer][hand_row: P]
        R_conv[layer][hand_row: P]
```

映射表：

```text
person_id  -> brain_row
pen_id     -> hand_row
exec_seq   -> (person_id, pen_id)
```

普通请求可视为：

```text
一个 person
一支 pen
该 pen 的 shared-S group width = 1
```

因此 `N=1` 必须退化到原生 recurrence，不另走近似路径。

### B.6.2 Graph 输入

每个 decode row 显式携带：

```text
person_index
pen_index
frontier_group
visibility class
```

每个 batch 再提供 ragged group：

```text
person_offsets[n_active_people + 1]
row_indices[n_active_pens]
```

同一个 person 的本 frontier PUBLIC rows 必须从同一 `S_old` 出发，完成后只提交一个
`S_new`。PRIVATE/PENDING rows 只更新相应 hand/private state，绝不进入 global commit。

### B.6.3 任意 N，不写死三笔

正式 kernel 与 reference 必须支持：

```text
1 <= N_person <= P
```

任何内部 `reader_tile=2/4/8` 只允许是 shader tiling，不得成为语义上限。

RBB mean baseline：

```text
同一 S_old
-> N candidates
-> 对有效 PUBLIC rows 做确定性 reduce
-> one S_new
```

必须使用固定 reduction order 或可复现的 pairwise/tree reduction，避免 pen 分配顺序
改变数值结果。

Parallel Delta 不得继续假定 inverse 永远是 `3x3`。对任意 `N <= P`：

```text
禁止显式求逆
优先 Cholesky/LDLT solve
在 dual N×N 与 primal state_dim×state_dim 之间选较小维度
reader 数大于 kernel tile 时分块，但最终数学结果仍是一个 frontier block update
记录 condition estimate / regularization，不稳定则整个 frontier 明确失败
```

是否启用 Parallel Delta 仍由质量/性能门决定；mean path 必须先成为任意 N 的可靠基线。

### B.6.4 Queued child hand seed

正式路径不再给 queued child parked recurrent seq。若 child admission 需要继承
fork-time conv/R0-R2 hand state：

```text
每个 fork event 生成一个 immutable hand_seed
全部尚未 admission 的 sibling 共享 seed reference
admission 时才从 seed materialize 到一个 pen row
最后一个引用释放 seed
```

这样 logical child 数不会乘以 GPU hand memory。若 hand seed 放到 host/RAM，其字节
必须进入现有 episode hard-resource accounting 和 state format fingerprint。

## B.7 Attention：按 person 分组、按 pen 读

### B.7.1 Ragged multi-reader layout

现有 DDVR `query_offsets` 已能表达每 reader 的全局 softmax范围；最终还需增加
person grouping，使一个 physical KV tile 服务同一 person 的全部 readers：

```text
active_people[]
person_reader_offsets[]
reader_query_offsets[]
entries[]
per-reader DDVR delta/group
```

不能构造 `[B, P, ...]` 的最坏 padding。metadata 与计算量必须按实际：

```text
B_active
P_active = sum(p_e)
visible entries
```

增长。

### B.7.2 Vulkan reader tiling

目标 dispatch：

```text
for each active person
  for each KV tile
    load/dequant K/V tile once per feasible reader tile
    serve this person's reader rows
    keep independent (m,l,O) per reader/head
```

GPU shared memory装不下全部 readers 时，将 reader 维分成固定硬件 tile；这只会增加
若干次 tile load，不改变 person 可持有的总 pen 数。

必须保持：

```text
一个 reader 一个 global softmax
不同 person 永不互看 KV
同 person 的 PRIVATE/PENDING 仍按 owner visibility
Turbo K/V 直接 dequant
Tri compaction 后 run->cell resolution 仍正确
```

### B.7.3 全网 N-row 执行

进入模型的所有实际 pen rows先 flatten：

```text
R = sum(p_e)
```

以下 op 按 `R` 做统一 batch：

```text
projection
MoE routing
shared expert
routed expert regroup
RMSNorm
LM head
backend sampler
```

在 Full Attention 与 shared GDN 层再按 person offsets分组。禁止先按 person 调
`llama_decode()`，再按 Lane 调第二层循环；那会丢掉全局 weight batching。

## B.8 全局 person/pen 调度器

### B.8.1 两级队列

保留每个 episode 内已有 FIFO/tree-path 顺序，再增加全局 person readiness：

```text
global ready people
  -> per-person ready_queue
```

分配 pen 时不得读取标题、模型置信度或答案内容。

### B.8.2 无 per-person cap 的工作保持策略

第一版使用确定性的两遍分配：

```text
Pass 1:
    按 person 等待年龄，为每个 runnable 且当前无 pen 的 person 分配一支 pen，
    直到 P 耗尽。

Pass 2:
    对仍有 ready child 的 people 做轮转；
    每轮每人取一个 child，重复直到 P 耗尽或无 runnable work。
```

这不是“每人最多一支/每轮一支”的配额；它只定义当前 fill order。若只有一个 person，
Pass 2 会把全部 `P` 支笔给它。

后续允许用纯资源信息改进吞吐亲和性：

```text
共享 history 长度
预计 KV/S bytes
当前 reader tile occupancy
MoE batch shape
等待时间
```

不得用 prompt/title/输出质量做隐式 judge。任何 affinity 优化都必须保留 aging，
证明不会让其他 resident person 无限饥饿。

### B.8.3 Frontier 原子性

每个 person 的一个 frontier 是最小提交单元：

```text
该 person 本轮选中的全部 PUBLIC pen rows
-> preflight
-> one grouped forward
-> one RBB commit
-> visibility commit
```

多个 person 的 frontier group 可进入同一个物理 graph。资源 preflight 必须覆盖整批；
若 backend 本身失败且无法判定部分完成，批内所有受影响 person 明确 abort，不能假装
只有某一 Lane 失败。

### B.8.4 Pen 回收与公平

首轮可保持当前“不抢占 RUNNING Lane”，但这只能作为过渡门。最终为了在
`B > P` 或后来 person 到达时避免长期垄断，必须支持 frontier-boundary pen yield：

```text
保存该 Lane 的 hand/private state + sampler/MTP binding
释放 pen row / exec handle
node -> READY_SUSPENDED
同 person 其他 writers 可继续
恢复时接最新 global S + 最新 PUBLIC KV
执行 no-commit logits refresh
继续采样
```

yield quantum 只是一项调度参数，不是 per-person pen cap。不得在一个 frontier 中途
收笔，也不得只保存 conv 而丢失 R0-R2/sampler状态。

### B.8.5 Final 条件按 person 独立

某个 person 是否 final，只看该 person：

```text
ready_queue empty
starting empty
suspended empty
running size == 1
最后 Lane exit intent 已经 stable fence
```

其他 person 是否仍运行与此无关。一个 person 的 `[DONE]` 不能结束其他 response。

## B.9 三种压力的唯一合法行为

### B.9.1 人数耗尽

```text
resident_people == B
新 root 到达
-> 留在 server root admission queue
-> 不创建 brain
-> 不创建 pen
-> 不写 KV
```

若外层请求队列也达到现有明确上限，才返回 server busy；不得偷占另一 person 的 brain。

### B.9.2 笔数耗尽

```text
allocated_pens == P
新 child ready
-> child 保持 queued
-> person/episode 不 abort
-> TriAttention 不运行
```

若公平调度需要收笔，只能走 §B.8.4 frontier-boundary yield。

### B.9.3 KV 耗尽

```text
physical KV pressure
-> Tri drain/maintenance
-> refresh physical usage
-> floor exhausted
-> person-level demotion/preemption/abort
```

多 person RERoT target 按 semantic episode history 计算，不按 reader/pen 数重复：

```text
每个 person 一份 PUBLIC semantic union
每支 active pen 只贡献自己的合法 PRIVATE/PENDING/hard tail guards
physical keep = 所有 person/pen keep reference 的并集
```

brain 或 hand arena shortage 是 recurrent-only pressure，永远不得调用 Tri。

### B.9.4 牺牲单位

KV/RAM/preemption 的可靠最小牺牲单位仍是一个完整 person/episode，不能留下 orphan
pen。单支 pen yield 只有在 global brain 保留、hand state 可精确恢复且走 refresh 时
才是普通调度，不等于错误恢复或 KV victim preemption。

## B.10 VRAM-only 三容量 Auto-fit

### B.10.1 约束与可知边界

生产调用只有：

```text
--total-kv auto
```

算法不得读取或学习：

```text
线上 prompt 长度分布
当前请求队列
planner 产生了多少 child
用户指定的人数/笔数
历史输出质量
模型输出内容
```

算法允许使用且必须动态读取：

```text
每个 backend device 的 free/total bytes
实际 model/context/compute allocation probe
模型解析出的 n_ctx（未指定时使用模型 metadata，绝不写死）
实际 K/V tensor type/ne/nb 和 alignment
实际 grouped recurrent brain/hand tensor bytes
Tri ratio/recent window/ubatch 等当前构建参数
MTP target/draft context bytes
backend scratch 与 allocator granularity
```

“只和总显存有关”的严格含义是：用户侧没有 workload/capacity knob；容量可行性由实际
device memory budget 决定。多 GPU 时仍须逐 device 验证，不能把不可迁移 buffer
错误地按显存总和平均。

### B.10.2 不再使用单轴 `ctx/2 -> recurrent slots`

当前近似：

```text
expected sequence length = n_ctx / 2
expected recurrent concurrency
    = physical_KV / expected_KV_per_sequence
```

把一个 sequence 同时当成一个人、一支笔和一份 KV history。新模型应拆成：

```text
people expected KV demand  -> 仍可用动态 n_ctx/2 作为无 workload 信息时的先验
pens expected KV demand    -> 不乘完整 history；同 person 的 PUBLIC history 共享
brain memory               -> 按 B 实际 probe
hand/scratch memory        -> 按 P 实际 probe
```

设：

```text
C   = loader 最终解析出的 per-person logical context
rho = TriAttention ON ? triattention_ratio : 1
E_K = rho * C / 2
```

`E_K` 只是选择 people 数时的期望 physical KV/person，不是 runtime residency target。
runtime Tri target 仍严格按每个 person 的实际 logical length 计算。

最低 KV 继续从模型参数动态得到：

```text
Tri ON:
    K_min = align(max(ceil(rho * C), n_ubatch + recent_window))

Tri OFF:
    K_min = align(C)
```

不得出现固定 `262144`、固定某张卡的 KV token 数或固定 bytes-per-cell。

### B.10.3 用 allocation probe 获取真实边际成本

新增联合 probe：

```text
common_fit_rerot_capacities()
```

对候选 `(B,P)`：

1. 构造真实 grouped recurrent layout。
2. 构造该 `P` 所需的 graph/output/metadata/MTP/Tri scratch。
3. 调用现有 `common_get_device_memory_data()` 获取每 device 的实际
   `model/context/compute/free/total`。
4. 在该 `(B,P)` 下用现有 aligned binary search 求最大的 `K(B,P)`。
5. 要求 `K(B,P) >= K_min`，否则候选不可行。
6. 记录真实字节差，不使用“每 brain 约多少 MiB”“每 pen 约多少 MiB”的常量。

headroom 应由 `device total/free`、allocator granularity 和实际 probe 波动比例派生。
可以保留无量纲安全比例，但不得用某张显卡容量区间或某个模型 context 写分支。

必须纳入：

```text
B brain rows
P hand rows
P outputs/logits/sampler rows
P-sized graph reserve
person-group and reader metadata
RBB candidate/reduction scratch
Vulkan multi-reader scratch
MTP target + draft state
Tri score/pack scratch
archive/keeper metadata
state-save staging（若常驻）
```

RBB scratch 按逐层复用，不能预留 `layers × P × state`。

### B.10.4 无实跑条件下的总笔数先验

已知本机早期普通并发的吞吐甜点是 `np4..np7`。在禁止启动时 benchmark、又只允许
VRAM auto 的前提下，能做的最合理猜测是把它解释为 **全局 active rows 的宽平台**，
而不是 per-person Lane 上限。

首版无实跑先验：

```text
preferred P = 6
acceptable sweet band = 4..7
```

候选 pen 顺序：

```text
6, 5, 7, 4, 3, 2, 1
```

含义：

- `6` 取已知甜点区中部，给误差和额外 DDVR/RBB metadata 留余量；
- 显存更多时不自动越过 `7`，因为更多 VRAM 不能证明更多 rows 更快；
- 显存不足时按 probe 向下退；
- 这只是全局 `P`，一个 person 可拿全部 `P`，不存在每人 6 笔上限。

若未来更换 GPU，但仍坚持“只看 VRAM、不跑 benchmark”，同样只能继续使用这一保守
先验；不能从显存容量推断新 GPU 的算力/带宽甜点。要改变 sweet band 必须由一次代码
版本级离线验证修订默认算法，而不是让用户配置。

### B.10.5 总人数先验与联合选择

对每个候选 `P`，枚举：

```text
1 <= B <= P
```

`B > P` 只会常驻无法同时拿笔的 brains，在没有 workload 信息时不划算，首版不选。
这不是 per-person pen cap。

给定 probe 得到的 `K(B,P)`，估计其平均 KV 可支持人数：

```text
B_kv = max(1, round(K(B,P) / E_K))
     = max(1, round(2*K(B,P) / (rho*C)))
```

同 person 多 pens 不乘 `E_K`。为兼顾三类收益，首版 person 目标为：

```text
B_balance = ceil(P / 2)
B_target  = min(P, B_balance, B_kv)
```

也就是满载时先验平均约两支笔/人：

- 保留同 person KV/S 共享收益；
- 又允许多个独立 response 并发；
- lanes 多于 pens 时自然排队；
- 单 person 空闲时仍可独占全部 `P`。

联合搜索按以下优先级选候选：

1. 所有 device 均 fit，且 `K >= K_min`。
2. 优先总笔数顺序 `6,5,7,4,3,2,1`。
3. 在该 `P` 下选择不超过 `B_target` 的最大可行 `B`。
4. 同分时选择更大的 `K` 和更大的最小 device margin。
5. 若没有候选，连 `(B=1,P=1,K_min)` 都放不下则 fail-closed。

这是一轮有限枚举 + 现有 KV binary search，不执行模型 token，不做在线 autotune。

### B.10.6 对当前机器的无实跑预测

只根据已知 `np4..np7` 甜点和“一个 person 的 lanes 可排队”，首选候选应是：

```text
总笔数 P = 6
总人数 B = 3
总 KV  K = 在 B=3/P=6 的真实 brain/hand/scratch reserve 后，
            对剩余显存做 aligned auto-fit
```

若 grouped recurrent 和新 graph scratch 使 `K(3,6)` 的 `B_kv` 低于 3，算法应自动
落到：

```text
B = 2
P = 6
K = 剩余显存的最大可行值
```

而不是要求用户调参，也不是先砍掉 P 的吞吐宽度。只有 `P=6` 本身不能保住动态
`K_min` 时，才按候选顺序降低 P。

这里故意不给 K 写 token 数：新 grouped memory、模型、K/V type、context、MTP 和未来
GPU 都会改变实际结果；写一个当前机器数字会立刻成为错误常量。

### B.10.7 启动回填与日志

fit 成功后统一回填：

```text
params.n_parallel = B
cparams.n_person_max = B
cparams.n_pen_max = P
cparams.n_ctx_kv = K
derived handle/output/graph capacities
```

最终启动明确打印：

```text
capacity mode = VRAM-only auto
person capacity = B
pen capacity = P
person heuristic = min(ceil(P/2), expected-KV support)
derived exec handles = ...
brain recurrent bytes = ...
hand recurrent bytes = ...
RERoT graph/scratch bytes = ...
automatic unified KV capacity = K
minimum device margin = ...
```

禁止再用一个 `n_slots` 或 `recurrent capacity` 数字暗示三种容量相同，也禁止日志暗示
该选择经过了运行时吞吐 benchmark。

## B.11 State、MTP、Tri 与普通请求兼容

### B.11.1 State format

State version 必须升级并记录：

```text
person id / episode id
brain row state
每个 resident/suspended pen 的 hand state
pen -> node binding
global and per-person queues
frontier/publish/topology/layout epochs
archive handle
B/P/K capacity requirements
model/RoPE/Tri fingerprints
```

restore 可以映射到不同 physical brain/hand row，但 semantic person/node/pen lineage
必须保持。目标实例若 `B/P/K` 不能容纳该 state，明确拒绝或先 demote/queue；不得截掉
某个人的部分 child。

### B.11.2 MTP

MTP draft/checkpoint 绑定：

```text
(person_id, pen_id, node_id, view stamp, frontier)
```

scratch 与 persistent draft state 按 `P` 计。一个 person 的 PUBLIC commit 只使该
person 的相关 pen drafts stale，不能全 context 清空其他 people 的 drafts。

### B.11.3 TriAttention

当前 `rerot_active_episode()` 单例调用全部改为遍历/索引受影响 people。Tri reclaim
hint 必须一人一份 semantic archive，不按 pen 生成重复 target。

Tri OFF/ON 固定产品契约不变；`B/P` 解耦不能改变 3/32 floor、fill-first、sticky
maintenance 或 recurrent-only gate。

### B.11.4 普通请求

普通请求进入同一 people/pen accounting：

```text
admitted ordinary request = 1 person
device-resident ordinary decode = 1 pen
```

但 RERoT OFF graph、KV mask、recurrent state、sampler和输出必须继续走现有路径。不能为了
统一名字强制普通请求进入 PAC-DFS/RBB。

## B.12 性能模型与容量标定

### B.12.1 VRAM-only 先验不能冒充硬件 autotune

设当前 batch 有 `B_a` 个 active people，总 pens 为：

```text
P_a = sum(p_e)
```

理想时间模型：

```text
t(B_a, {p_e})
  = global_weight_cost(P_a)
  + sum_e shared_KV_and_S_cost(e)
  + P_a * lane_local_compute
  + grouped_reduce_and_metadata
```

同样的 `P_a`，分配形状也会影响性能：

```text
1 person × 9 pens
3 people × 3 pens
9 people × 1 pen
```

前者复用一份 KV/S，后者拥有更多独立 response，但要扫更多 memory。MoE expert union、
context 长度和 reader tiling 会改变最优点，所以只测总 rows 不够。

### B.12.2 必跑二维/分配形状矩阵

新增脚本，替代单一 `rerot-throughput-gate.py` 作为容量标定门：

```text
B_active:
    1, 2, 3, B

P_active:
    1, 2, 3, 4, 6, 8, 9, P
    （只取 <= full-auto 所选 P 的值）

allocation shapes:
    all pens on one person
    balanced across people
    one pen per person
    uneven long/short-context mix

context:
    short
    production median
    production p95
    Tri floor附近
```

记录：

```text
model tokens/s
completed responses/s
quality-qualified goodput
p50/p95 response latency
frontier time
weight/MoE time
DDVR attention time and HBM bytes
RBB time and S bytes
conv/private recurrent time
reader tile count
VRAM peak
queue wait / pen utilization
```

这组矩阵用于验证和修订代码里的全局 sweet-band 先验，不成为启动时 benchmark，也不
产生用户参数。若 `P=9` 相对 `P=6` 总吞吐不升或下降，继续保持 §B.10 的保守上界；
若未来新 backend 的证据表明 sweet band 系统性变化，应在新代码版本中整体修订先验。
无论总 `P` 为多少，都不产生 per-person 上限，单 person 可使用全部 `P`。

### B.12.3 容量 gate

对 full-auto 选出的 `B/P/K`，必须验证：

```text
一个 person 可独占全部 P 支笔
多个 people 的 sum(p_e) 永不超过 P
child > P 时不丢 descriptor、只排队
B 个 people 可同时 resident
第 B+1 个 root 不分配 brain/KV
K 压力只走 unified KV/Tri 路径
三种分配形状下无吞吐断崖或 starvation
```

## B.13 分阶段改造顺序

以下顺序以当前代码为基线。每阶段都必须保持 RERoT OFF 零回归，不允许并行维护两套
长期 scheduler。

### Phase 0：冻结词汇与 full-auto 契约，不改运行行为

修改：

```text
common/common.h
common/arg.cpp
src/llama-cparams.h
tests/test-arg-parser.cpp
tools/server/README.md
```

工作：

1. 增加内部 `B/P` fit-result 字段，不增加生产 pen CLI。
2. 将 `--total-kv auto` 冻结为 RERoT 的 `B/P/K` 联合 auto 模式。
3. 记录 `-np` 是否由用户显式指定；RERoT full-auto 下冲突参数 fail-fast。
4. 新增 capacity-mode 日志和 metrics，过渡期仍执行旧 runtime。
5. 不改变 batch、memory 或输出。

验收：

```text
RERoT OFF 参数和输出不变
RERoT full-auto 无需人数/笔数参数
不存在 per-person 或 total-pen 生产 CLI
冲突的显式 -np 不被静默采用
日志明确标记过渡期尚未启用 grouped auto
```

### Phase 1：纯控制面 people/pen scheduler

修改：

```text
tools/server/server-rerot.h
tools/server/server-rerot.cpp
tests/test-rerot-runtime.cpp
```

工作：

1. 增加 `person_id`、`pen_id` 和固定 `P` 的 pen allocator。
2. `server_rerot_node_runtime::physical_slot` 改为 pen binding。
3. 保留现有 per-episode ready queue，新增 global people admission/pen fill。
4. fake-memory tests 中允许多个 episode 同时推进。
5. 删除 logical runtime 对 `slot == exec_seq == node executor` 的假设。

此阶段 production graph 仍可保持单 episode gate，不提前声称多 person 可运行。

验收：

```text
B=3,P=9：一个 person 可取 9 pens
B=3,P=9：三个 people 可按 3/3/3 推进
B=3,P=5：分配总和始终 <=5
一个 person 无 ready work 时 pen 立即归还
child 数远大于 P 时无 descriptor 丢失
```

### Phase 2：Context 多 episode 化

修改：

```text
include/llama.h
src/llama-context.cpp
src/llama-context.h（若最终把 registry 收回 context owner）
tests/test-rerot-runtime.cpp
新增 focused context-state test
```

工作：

1. `g_rerot_states[ctx]` 改为 episode map + exec binding map。
2. begin/end、views、write tags、refresh、MTP stale 全部按 episode 定位。
3. state envelope 升版。
4. preemption/shift/caps 不再依赖单 active bool。
5. 移除 `server_context_impl::rerot_episode_id` 单例门的 core 前置依赖。

验收：

```text
同一 context 同时 begin 3 episodes
同 seq 不能绑定两个 people
结束 A 不清除 B/C views/tags/stamps
A topology bump 不使 B/C MTP stale
每个 episode 可独立 save/load/end
```

### Phase 3：拆 recurrent brain/hand arenas

修改：

```text
src/llama-memory-recurrent.h
src/llama-memory-recurrent.cpp
src/llama-memory-hybrid.h/.cpp
src/llama-memory-hybrid-iswa.h/.cpp
src/llama-memory.h
src/llama-model.cpp
tests/test-rerot-recurrent.cpp
```

工作：

1. 保留 ordinary sequence-local layout。
2. 新增 grouped layout 的 `B` brain rows 和 `P` hand rows。
3. shared layer `S` 按 person，conv/R 与 private R0-R2 S 按 pen。
4. 增加 brain/hand usage API。
5. queued child 正式路径去 parked full recurrent seq，改用 shared fork seed。
6. 先实现 CPU/reference mean commit。

验收：

```text
N=1 bitwise/容差等价原生 recurrence
N=1,2,3,5,P permutation 对称
不同 person 的 S 永不互改
PRIVATE/PENDING 不进入 global S
一个 person 使用 P pens 只占一份 global S
hand seed COW 不按 child 复制
```

### Phase 4：Variable-N grouped graph

修改：

```text
src/llama-batch.h/.cpp
src/llama-graph.h/.cpp
模型 architecture graph builder
ggml CPU reference/op
tests/test-rerot-attn.cpp
tests/test-rerot-ddvr.cpp
```

工作：

1. batch row 显式携带 person/pen/exec identity。
2. 增加 ragged `person_offsets`。
3. 全网权重 op flatten 全部 pens。
4. attention/recurrent 在相应层按 person regroup。
5. 支持同一 graph 内多个独立 frontier groups。
6. graph reuse key 纳入实际 people/pens capacity bucket，不纳入具体 id。

验收：

```text
多 person CPU E2E
ragged allocations 7/1/4 等不需要 padding 到 B*P
strong/lag1 各 person 独立
每 reader 一个 global softmax
graph reuse 后 id/epoch 不串
```

### Phase 5：Server 真正启用多 people

修改：

```text
tools/server/server-context.cpp
tools/server/server-task.h/.cpp
tools/server/server-common.cpp
tools/server/server-rerot.h/.cpp
server integration tests
```

工作：

1. `server_slot` 回归 request/person owner。
2. child 不再 clone 成完整外部 slot。
3. 新 pen pool 负责 sampler、exec seq、hand state和 batch row。
4. 一次 `pre_decode()` 收集多个 person frontier groups。
5. post-decode 按 person 原子 commit/sample/finalize。
6. response、SSE、cancel、error全部按 person 隔离。
7. 普通请求占一人一笔并能与兼容 RERoT pens 做全网 weight batch。

验收：

```text
3 个 RERoT HTTP 请求同时 active
每个请求恰好一个 response/finish/[DONE]
一个请求 fork/abort/final 不改变另两个
普通请求与 RERoT 请求并存
无 orphan pen/exec/archive refs
```

### Phase 6：Vulkan multi-reader + grouped RBB

修改：

```text
ggml/src/ggml-vulkan/ggml-vulkan.cpp
ggml/src/ggml-vulkan/vulkan-shaders/flash_attn_base.glsl
相关 shader generation/tests
RBB mean/block-delta kernels
```

工作：

1. reader tile 内 K/V load/dequant once。
2. 每 reader 独立 online softmax。
3. shared S tile 一次 load，服务同 person N 个 q/k/v。
4. mean commit 任意 N。
5. mean 质量通过后再加稳定的 Parallel Delta solve。
6. 普通 FA/GDN pipeline 保持单独 variant，无 inner-loop RERoT branch。

验收：

```text
CPU/Vulkan parity: N=1,2,3,5,8,P
Turbo4/Turbo2 parity
多 person ragged group parity
无 per-person N specialization
无 Vulkan validation error
P 增大时 scratch/occupancy 有明确上限和日志
```

### Phase 7：Tri/MTP/RAM/preemption 多 person 化

修改：

```text
tools/server/server-context.cpp pressure planner
src/llama-kv-cache.cpp
src/llama-context.cpp state/shift glue
MTP integration
server persistence tests
```

工作：

1. Tri hints 从 single active episode 改为所有受影响 people。
2. 一人一份 PUBLIC semantic union。
3. MTP stale 精确到 person/pen。
4. RAM save/restore 以 person 为单位。
5. 实现 frontier-boundary pen yield/resume。
6. KV floor 后 victim 为完整 person；recurrent-only pen/brain shortage不调用 Tri。

验收：

```text
多 person Tri drain
一个 person demote/restore 不改变其他 people
pen yield 后从最新 global S/PUBLIC KV refresh
MTP rollback 不跨 person
KV/reference/brain/hand usage 全归零
```

### Phase 8：VRAM-only 三容量 auto-fit

修改：

```text
common/common.cpp
common/fit.cpp
common/common.h
tools/server/server-context.cpp startup
fit tests
```

工作：

1. 实现 §B.10 的 `(B,P)` 有限枚举和每候选 `K` binary search。
2. 所有 bytes 来自真实 allocation probe，不写死显存或 context。
3. 用动态 `rho*C/2` 只估计 people 的 KV 支持数，不再估计 pens。
4. grouped recurrent breakdown 分 brain/hand。
5. MTP、Tri、multi-reader/RBB scratch 全部纳入。
6. 删除 RERoT 对 `common_dynamic_recurrent_target()` 的依赖。
7. fit 后回填 `params.n_parallel/cparams B/P/K`。
8. P 使用 `np4..np7` 的保守 sweet-band 先验，不拉到显存允许的最大值。

验收：

```text
同一 memory budget/model layout 选择稳定
模型 context 改变时无需代码常量修改
device memory 改变时通过 probe 自动重选
B/P/K 无用户参数
K 始终是剩余显存的 aligned 最大可行值
(B=1,P=1,K_min) 放不下时 fail-closed
启动日志精确报告选择过程和三容量
RERoT OFF auto-fit 结果不变
```

### Phase 9：吞吐面标定与 clean cutover

修改：

```text
scripts/rerot-throughput-gate.py 或新增 matrix runner
metrics
生产 service unit
旧 slot/COW scaffolding
```

工作：

1. 跑 §B.12 矩阵，验证或按代码版本修订 VRAM-only sweet-band 先验。
2. 生产仍由 `--total-kv auto` 选择 `B/P/K`，不把矩阵结果写进 service 参数。
3. 用 quality-qualified goodput 而非只看 internal tokens/s。
4. 删除正式路径的 single-episode gate、Lane-as-server-slot、parked full recurrent COW。
5. Scheme 0 若继续保留，必须只存在明确实验开关和独立测试中。
6. 更新 state version、部署 checksum、service unit，并删除旧 `-np` 容量参数。

验收：

```text
代码中无 per-person pen cap
一个 person 可使用全部 P
所有运行 people 的 pens 总和 <= P
所有 physical KV <= K
三容量 metrics 与真实 allocator 一致
production B×P 压力无 5xx/OOM/deadlock/restart
最终 binary/library/calibration checksum 一致
```

## B.14 必加 metrics

禁止高基数 person-id label；只暴露聚合值：

```text
rerot_people_capacity
rerot_people_resident
rerot_people_runnable
rerot_people_waiting

rerot_pens_capacity
rerot_pens_allocated
rerot_pens_running
rerot_pens_suspended
rerot_pen_queue_depth
rerot_pens_per_person_max_observed
rerot_pen_utilization

rerot_batch_people
rerot_batch_pens
rerot_frontier_rows
rerot_pen_wait_seconds
rerot_person_wait_seconds

rerot_brain_bytes
rerot_hand_bytes
rerot_grouped_scratch_bytes
```

现有 unified KV / Tri metrics 继续作为第三维，不复制 `rerot_kv_capacity` 事实源。

日志至少能区分：

```text
person admission blocked
pen admission queued
pen frontier yield/resume
KV pressure / Tri drain
brain pressure
hand pressure
person-level demotion/preemption
```

## B.15 最终硬不变量

### Capacity

```text
resident_people <= B
allocated_pens <= P
sum(person.allocated_pens) == allocated_pens
physical_KV_used <= K
```

### Ownership

```text
每个 pen 绑定至恰好一个 person/node 或为空
每个 active exec seq 绑定至恰好一个 pen
每个 RERoT person 恰好一份 global brain
不同 person 的 brain/KV visibility/final state 不交叉
queued child 没有 physical pen
```

### No per-person limit

```text
不存在 per-person pen capacity 字段
单 person 在资源空闲时可获得全部 P
任何 reader/kernel tile 只影响 dispatch，不影响 admission
N_person 的唯一硬上界是全局 P
```

### Recurrent

```text
shared S 每 person 每 frontier 只 commit 一次
conv 永远 per pen
R0-R2 首版永远 per pen
PRIVATE/PENDING 永不写 global S
N=1 退化原生
```

### KV

```text
llama_kv_cells 仍是唯一 physical metadata owner
PUBLIC history 不按 reader复制
Tri target 不乘以 pen 数
shared physical keep 是所有 person/pen refs 的并集
```

### Scheduling

```text
person final/abort/stream termination 相互独立
一个 frontier 不部分提交
有 runnable work 且有 free pen 时不得让 pen 空闲
aging 保证 resident person 不无限饥饿
```

## B.16 本附录 Definition of Done

只有以下全部成立，才能宣布 slot 模型已完成 clean cutover：

1. RERoT `--total-kv auto` 在无人数/笔数用户参数时联合选择 `B/P/K`。
2. 没有任何 per-person pen 上限、固定三笔假设或生产 `--parallel-pens`。
3. 一个 person 能安全使用全部 `P` 支笔。
4. 同一 context 能同时运行 `B` 个独立 people。
5. 全部 pens 可跨 people ragged batching，并按 person 共享 K/V 与 S。
6. recurrent memory 实际按 `B * brain + P * hand` 分配。
7. queued child 不占 physical pen/full recurrent state。
8. `LLAMA_MAX_SEQ` 不再构成独立运行上限。
9. people/pen shortage 不触发 Tri；只有 KV shortage触发 Tri。
10. RAM/MTP/context shift/preemption 都以正确 person/pen 粒度工作。
11. §B.12 吞吐面验证了 VRAM-only sweet-band 先验，启动时没有运行 benchmark，也没有把
    “能放下最多多少 rows”当成吞吐最优值。
12. RERoT OFF 的 atomic/Tri/unified KV/recurrent/MTP/server 行为零回归。
13. production `B/P/K` 全压通过，无 5xx、OOM、deadlock、orphan state 或重复 stream。
14. 最终 build、部署 binary、runtime libraries、calibration checksum 一致。

最终口号改为：

> **人是独立 brain/response 的容量单位，笔是 Lane-local state/decode row 的容量单位，KV 是全局 token memory 的容量单位；一个人拿多少支笔没有单独上限，只受全局总笔数约束。**

